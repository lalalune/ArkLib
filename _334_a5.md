## A3 RESOLVED by a better theorem (`e03c2dd5f`): the Parseval threshold — s=64 prize rows open UNCONDITIONALLY. Plus: A5 exact n=12 verdict, A1 δ=0 brick, and a fleet warning

### A3 — the resultant maxima question answered, not by computation but by Parseval+AM–GM
The measurement probe (`probe_kkh26_resultant_maxima.py`: exact maxima at s=8,16 over the full {−2..2} coefficient superset, honest lower bounds at s=32) showed the worst-case bound `s^{s/2}` is off by 2^33 already at s=32, with per-conjugate geometric mean μ ≈ 2^2.4–2.9 — i.e. the conjugates behave like an **ℓ² mean, not an ℓ¹ max**. That observation is a theorem:

```
|Res(R, Φ_{2^m})|² ≤ (Σ_{z^{2^m}=1} |R(z)|² / 2^{m−1})^{2^{m−1}} = (2·‖R‖₂²)^{2^{m−1}}
```
(finite Fourier Parseval over the FULL root group + unweighted AM–GM over the primitive ones). **The ℓ¹ exponent is halved**: `natAbs_resultant_cyclotomic_le_parseval : |Res| ≤ (2·‖R‖₂²)^{2^{m−2}}`, and for collision differences (‖R‖₂² ≤ 4r) the [KKH26] Lemma 1 prime threshold drops from `p > (2r)^{2^{m−1}} ≈ s^{s/2}` to **`p > (8r)^{2^{m−2}}`** (`kkh26_lemma1_parseval`, wired through the wave-2 divisibility route `kkh26_lemma1_of_not_dvd` — that refactor paid off immediately).

**Reach-table consequence** (verified numerically, 80,623 checks, 0 violations): at s=64 the threshold drops 2^192 → 2^101–2^115 (rate-pinned r=10/18), the ε*=2^{−128}, |F|<2^256 window `(2^115, 2^175)` is **nonempty — the s=64 rows open with NO Thorner–Zaman input**. s=128 stays closed (2^229 > 2^198): the TZ conditional route (`KKH26PolyFieldCeiling.lean`) remains the only path there, its price tag now sharpened to exactly the s ≥ 128 rows. File verified: exit 0, all 6 theorems `[propext, Classical.choice, Quot.sound]`; a post-verification deprecation-warning fix is re-verifying now.

### A5 — the n=12 EXACT profile landed (orbit-reduced probe, ~3.5h compute)
`probe_epsmca_orbit_exact_n12.py` (exit 0; reduction validated exactly against the n=4/n=6 ladder): RS[F₁₃,12,6] exact worst-case bad-γ counts: δ=0 → 1, **δ=0.083 → 2 and δ=0.167 → 3 (sampling had under-reported 1)**, δ=0.25 → **exactly 12** (sampled 12 CONFIRMED), δ≥1/3 → 13/13 total breakdown. The below-Johnson regime at p=13 is now exact, not sampled: numerator growth 1→2→3 below UDR-ish radii, the 12-at-δ=0.25 plateau, breakdown above Johnson. p=37 exact is honestly INFEASIBLE for this method (595 GB table / ~months) — the sampled 12/37 stands as the field-drift evidence.

### A1 — the δ=0 forced-equality brick (`Jo26DeltaZeroForcedEquality.lean`)
The adversarial-generator probe's structural discovery is now a theorem: `epsMCAGen_interleaved_eq_deltaZero` — at δ=0, interleaving exactness holds for **every** coefficient generator (any |Ω|, no covering lemma: the no-joint clause at the full domain is seed-independent and one column projection dominates). First A1 slice; the positive-radius dichotomy stays open with all toy evidence pointing at hollow.

### ⚠️ Fleet warning: race-deletions
Commit `c990bfa68` collaterally deleted three in-flight lane files (`KKH26ParsevalThreshold`, `Jo26DeltaZeroForcedEquality`, sibling lane's `Jo26MarkedCurveDecodability`) in a bad merge resolution. All three restored verbatim in `e03c2dd5f`. Lanes: check your files after any conflicted rebase, and re-pull before assuming your last push survived.

**Ledger state: B1 ✓ · B2-opener ✓ · B3 ✓ (+ s=64 now unconditional via A3) · B4 blocked-on-literature · A1 δ=0 slice ✓ · A2 ✓ · A3 ✓ RESOLVED · A4 ✓ · A5 probe ✓ (Lean equivariance pin = remaining stretch item).** Full verification build of all #334 files running; will report any failures.
