## EXACT RESULT (char 0): the ladder list at agreement rm is EXACTLY N_fib — the m=2 tower wall DISSOLVES via the 2-power Mann theorem (already in-tree)

Decisive structural finding (`probe_agreement_rm_structure.py`, `probe_rm_coset_char0.py`, both landed; exhaustive char-0 at n≤32, randomized clean to n=64):

**At agreement exactly rm**, a codeword `c` is in the ladder word `w = x^{rm}+λx^{(r-1)m}`'s list iff `D = X^{rm}+λX^{(r-1)m} − P_c` (deg P_c ≤ (r−2)m) splits completely over the domain ⟨g⟩, i.e. its root set `R` (size rm) is a **GapBand solution**: elementary symmetric functions vanish in the two windows `e_1..e_{m-1}=0`, `e_{m+1}..e_{2m-1}=0`, pivot `e_m = ±λ`. The fibre-unions are special GapBand solutions.

**The exact question — are all GapBand solutions fibre-unions? — is YES in characteristic zero, and it's already a theorem in-tree:**
- `fiberUnion_gapBand` (KKH26FiberStructural): fibre-unions ⟹ GapBand (every field). [⊇]
- `gapBand_antipodal_charZero` (CensusClassificationCharZero): GapBand ⟹ antipodal-closed, via **`subset_neg_mem_of_sum_zero`** = the 2-power Lam–Leung/Mann theorem (∑ of 2^m-th roots = 0 ⟹ the set is −closed). [⊆]

For the m=2 (squaring) tower, antipodal pairs `{x,−x}` ARE the squaring fibres, so GapBand ⟺ squaring-fibre-union **both ways in char 0**. Hence:

> **the ladder list at agreement rm = N_fib EXACTLY** (char 0, squaring tower) — the upper bound MEETS the proven fibre lower bound (`ladder_list_ge_fibre`). No constant gap, no wall.

**Why the wall is char-0-dissolved but F_q-open:** the p=97 adversarial run showed the ladder list INFLATING to 13 > N_fib=11 — that is precisely the char-0→F_q transfer FAILING at a small field (coincidental degree-2 agreements over a tiny field create extra "valid R" that are not fibre-unions). The exact law holds over F_q **above the resultant/discriminant threshold** (the lift of the char-0 Mann relation), which is the campaign's studied transfer object. So:

- **char-0 / large-field (q above threshold): EXACT, L_list(rm) = N_fib, fully provable from in-tree Mann.**
- deployed finite q: exact up to the transfer threshold; below it, small-field inflation (genuine, measured).

This pins the EXACT sub-Johnson list value (not just bracket) for the squaring-tower ladder family in the idealized/large-field regime — the regime where δ* itself is defined (ε* = 2^{-128} needs q ≥ 2^{128}, comfortably above threshold for fixed n). Landing the exact char-0 count theorem next.
