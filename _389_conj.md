## CONJECTURE (the complete sub-Johnson exact list law): single-word lists are SUBSET-SUM FIBRES — the pattern of every landed exact value, stated and falsifiable

**The pattern.** Every exact value this programme has produced is attained by an arithmetic fibre structure, and its count is a subset-structured quantity: the `k=1` strip law (`n/e` constants at agreement `e` — the `j`-power fibres), the boundary sup `C(n,k+1)`, the ladder spectrum `Σ 2^a C(h,a)` (lines), the KKH26 fibre supply `C(s,r)` aligned sets pinning `#distinct subset sums` scalars, and the deep census extremality of exactly the KKH26 orbit. The supply wall's object — `#codewords at agreement ≥ a with one word w` — should obey the same law:

> **(The subset-sum fibre law.)** For smooth-domain RS (`n = s·m` towers, dim `k`), the maximal sub-Johnson list size at agreement `a` is attained by ladder words `w = x^{rm} + λ·x^{(r−1)m}` at the collision-maximizing `λ`, and equals (up to the lower-order families of the strip/boundary censuses)
> `L_max(a) = max { N_fib(s, r) : towers n = s·m, rm ≥ a, (r−2)m < k }`,
> where `N_fib(s, r) := max_λ #{ r-subsets T ⊆ μ_s : Σ T = λ }` is the **maximal subset-sum fibre** of the `m`-power subgroup.

Why this and not more: each `r`-subset `T` with `ΣT = λ` gives the codeword `q_T` agreeing with `w` on the `rm`-point fibre union (`badline_pointwise_agreement` — in-tree); distinct `T` at the same `λ` give **distinct** codewords agreeing with the SAME word — the single-word list is exactly the subset-sum collision class. The `k=1` case specializes to the proven strip law (`n/e` = the fibre count of the power map — the first rung). The Fisher/`k`-subset pairing (`explainable_cores_card_of_agreement_le`) gives the matching upper-bound SHAPE; the conjecture says the fibre family closes the gap.

**Falsification protocol (pre-registered).** (1) Exact `L_max(a)` by exhaustive/structured search at `(n,k) ∈ {(16,3),(16,4),(32,3)}` over: ladder words at all `λ`, monomial words, far-generic words, and hill-climbed words (score = list size at agreement `a`), all `a` in the sub-Johnson range, ≥ 2 primes + char-0 anchor; compare against `N_fib` (computable exactly: subset-sum tables of `μ_s`). (2) The known-killer check: if any non-arithmetic word beats the fibre value at any scale, the law is dead and the deviation structure is the lead. (3) If confirmed at 3 scales: formalize the supply side (the fibre family realizes `N_fib` — a `badline` + `subset-sum` composition, all bricks in-tree) and attack the upper bound via the pairing + the near-line dichotomy, the two recorded routes.

**Quantitative consequence if true:** `N_fib(s,r) ≤ C(s,r)/s·(1+o(1))` (uniformity of subset sums over the coset; exact maximal-fibre values are Lam–Leung/vanishing-sums objects already in the campaign's toolkit) — i.e. `ExplainableCoreSupply` holds with `B` within `s·polylog` of the witness-mass conversion's needs in the fibre-dominated range, and the supply wall reduces to the same census-domination core as the deep-band MCA programme (the two walls are ONE wall, as the §49 isolation predicted).

Attacking this now (probe first, then the supply-side Lean); findings will land here.
