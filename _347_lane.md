**Lane claim (in progress, this session)**: settling this residual via the **rbr → soundness union-bound lift**.

Status of the work:
1. The residual's docstring already says the shell residual is "open-and-likely-false" at sub-1 budgets. I'm **formalizing the dichotomy**:
   - **TRUE side** (written, compiling): `stirMultiRoundRbrSoundness_of_one_le_first` — the shell residual holds outright for any budget allotting ≥ 1 to the round-0 challenge (same canonical state-function gate as the landed checking-verifier counterpart; the construction never reads the verifier).
   - **FALSE side** (the math, in progress): the **union-bound probability lift** `rbrSoundness → soundness` with error `∑ᵢ εᵢ` — the named missing piece called out in #302 ("the rbr union-bound probability lift") and consumed by the #340 composition surfaces. From it: the shell verifier accepts every run, the honest prover accepts with probability 1, so **any** rbr budget family must satisfy `∑ᵢ ε_rbr i ≥ 1` whenever a δ-far statement exists — the residual is **provably false** for all sub-1 total budgets. This is the honest "narrow to the exact missing theorem" resolution: the genuine STIR soundness obligation lives on the checking verifier (`stirCheckingCABridge`), not on this shell residual.
2. New infra file in flight: `ArkLib/OracleReduction/Security/RbrToSoundnessLift.lean` — the marginal bridge (prefix-marginalization of the full run against the per-round games): support-pointwise event transport, `continueFromTo` prefix preservation, the `RejectStateOblivious` state-threading hypothesis (discharged for the `σ = Unit` regime all in-tree consumers use), per-round flip marginal ≤ rbr game, and the public `rbrSoundness_implies_soundness` + knowledge-form bound.

Will push to main as pieces verify. Census impact: the TRUE-side gate is a concrete provider for `stirMultiRoundRbrSoundnessResidual` (flips it to discharged); the FALSE-side theorem documents why no sub-1 discharge can exist.
