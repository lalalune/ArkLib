{
  "id": "C071",
  "title": "full_tower budget 2^{n/2^s} (count F4) and eta_halving house cocycle (F2) as one s-indexed squaring recursion: the shared x->x^2 fold is REAL and proven, but the two 'branchings' are 2^{n/2^s} vs 2^s (opposite monotonicity), the count budget never bounds the magnitude B -- relabeling that welds to BGK/Paley",
  "verdict": "OPEN",
  "method": "probe",
  "reduced_to": "",
  "wall": "W-BGK / W-Paley sqrt-cancellation for thin mu_n. The house side eta_halving unfolds to 2^s additive 'butterfly' terms eta(mu_{n/2^s}, b*zeta^j); the open core is sqrt-cancellation AMONG those 2^s phases (B = max_{b!=0}|eta(mu_n,b)| <= 2sqrt(n) <=> Ramanujan = Paley Graph Conjecture, BGK best proven n^{1-o(1)}). The count side counts CLASSES not characters and gives only the trivial triangle bound B<=2^mu*B(mu_1)=n. The s-dial does not bridge count budget -> B.",
  "evidence": "Exact probe scripts/probes/_407_conn/C071_tower_squaring_recursion.py (exact big-int class counts; machine-precision complex eta) on FOUR proper dyadic subgroups mu_n < F_q^* in the prize regime (n=2^3..2^6, q prime =1 mod n, q~n^3.2, n^2<<q, NEVER full group), plus a follow-up at n=2^8 q=16777729. Both Lean identities the connection fuses are REAL and reproduced exactly. (A) COUNT side (LamLeungTwoPow.tower_count): the number of 2^s-power CLASSES of D0=mu_n is exactly n/2^s=2^{mu-s} (squaring is 2-to-1 on a 2-power group), verified for all s=0..mu at every config (e.g. n=64: 64,32,16,8,4,2,1); so the budget 2^{#classes}=2^{n/2^s} is a dimensionless CLASS COUNT, and at s=mu it is 2^{n/n}=2^1=2 (the all-mu_n-closure count<=2). (B) HOUSE side (DyadicHalvingRecursion.eta_halving): unfolding the halving s levels from mu_n to mu_{n/2^s} produces EXACTLY 2^s additive butterfly terms eta(mu_{n/2^s}, b*zeta^j); the probe reconstructs the full eta(mu_n,b) from those 2^s terms with error 0..3.5e-15 (machine zero) at every level and every config -- the Walsh/FFT butterfly, confirmed. (C) THE LOAD-BEARING TEST: the count-side 'branching' exponent is n/2^s while the house-side branching (number of butterfly terms) is 2^s. These are OPPOSITE functions of s (one halves, one doubles); they are equal ONLY at the single accidental crossing s=mu/2 (n/2^s==2^s), false at s=1 and s=mu -- precisely the two depths the connection names as 'the same recursion read two ways'. So 'one recursion with 2^{n/2^s} branching interpolating between s=1 (house) and s=mu (count)' is not operative: the two branchings move in opposite directions. (D) At the window-interior depth s~mu (window t=2^s-1=Theta(n)) the count budget is 2^{n/2^mu}=2 -- a dimensionless count, bounded by 2, saying NOTHING about the MAGNITUDE B. Exact B = max_{b!=0}|eta(mu_n,b)| (max over (q-1)/n coset reps, eta coset-invariant): 6.84/11.43/25.21/32.73 for n=8/16/32/64, all in [sqrt(n),2sqrt(n)..sqrt(n ln m)], i.e. B is a sqrt(n)-scale magnitude. The house triangle-unfold gives only B(mu_n)<=2*B(mu_{n/2})<=2^mu*B(mu_1)=n*1=n -- TRIVIAL, no sqrt-cancellation. (E) Follow-up at the accidental crossing s=mu/2: count budget there is 2^{sqrt(n)} (16 at n=16, 256 at n=64, 65536 at n=256) -- super-polynomial in n, while B is sqrt(n)..n; the two are categorically different objects (a count vs a magnitude) and the crossing carries no quantitative relation between them.",
  "honest_note": "NOT a closure and NOT a refutation of any Lean lemma -- both cited files are genuinely proven and axiom-clean (LamLeungTwoPow.full_tower/tower_count via Lam-Leung-at-2 + power-class injection; DyadicHalvingRecursion.eta_halving via the antipodal split mu_{2k}=mu_k union zeta*mu_k), and the probe reproduces both to machine precision. C071 is a CORRECT structural observation: both faces sit on the SAME x->x^2 squaring fold (count: squaring 2-to-1 collapses #classes mu_n->mu_{n/2}; house: the same squaring collapses the period mu_{2k}->mu_k as a 2-term split), and at s=mu the count gives the all-mu_n-closure count 2 while at s=1 the house gives the parallelogram eta(mu_{2k})=eta(mu_k)+eta(mu_k*zeta). All of that is true. What C071 does NOT do -- and its attack_plan implicitly bets on -- is make the count budget 2^{n/2^s} and the house quantity 'one object at different window depths' in any operative sense. They are categorically different: the count budget is a dimensionless CLASS COUNT (bounded by 2 at the operative window-interior depth s~mu, super-poly 2^{sqrt(n)} at the accidental crossing s=mu/2), while the house is the MAGNITUDE B whose open core is sqrt-cancellation among the 2^s butterfly phases. The two 'branchings' (2^{n/2^s} count vs 2^s house terms) have opposite monotonicity and meet only at s=mu/2, not at the claimed endpoints. The 'Mersenne/sumset growth enters via the number of 2^s-power classes' clause is a category slip: the class count is exactly n/2^s (a clean power of two, no Mersenne anything), and Mersenne/sumset growth is a feature of the OPEN cancellation question (whether short +-1 relations of 2^mu-th roots vanish mod the prize prime), which the count side never touches. So C071 welds straight back to W-BGK/W-Paley: the residual is sqrt-cancellation among the 2^s eta_halving butterfly phases = the 25-year-open generalized-Paley non-principal eigenvalue. Labeled OPEN per the contract ('welds back to BGK/Paley; no progress beyond restating'). Probe is exact-integer for class counts and primitive roots, machine-precision complex for the eta butterfly (errors 0..3.5e-15) and B (max over all (q-1)/n coset reps, exact since eta is coset-invariant); prize n=2^30 not directly enumerated but the n/2^s class count and 2^s butterfly-term count are structural (exact for all n) and the count-vs-magnitude category gap is n-independent. Suggested one-line DISPROOF_LOG cross-reference: 'C071 -- full_tower budget and eta_halving share the x->x^2 fold (both true), but count budget 2^{n/2^s} (class count, =2 at window interior) and house 2^s butterfly terms have opposite monotonicity and the count never bounds the magnitude B; relabeling, open core = sqrt-cancellation among the 2^s phases = BGK/Paley.'"
}
