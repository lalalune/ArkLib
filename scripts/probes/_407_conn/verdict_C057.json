{
  "id": "C057",
  "title": "Power-sum window = all-ones-error syndrome (Newton bridge F10->F2/F5)",
  "verdict": "PARTIAL",
  "method": "mixed",
  "evidence": "TWO claims. (1) PROVEN part: the Newton identity 'e_1=...=e_{t-1}=0 <=> p_1=...=p_{t-1}=0 over char 0', and 'p_j(S)=sum_{x in S}x^j = all-ones-error syndrome', is a genuine already-in-tree axiom-clean identity: esymm_window_iff_psum_window (ArkLib/.../LamLeungTwoPow.lean:870, via newton_step:801), with supports subgroup_sum_eq_zero / subset_prod_sum_eq_zero / Ncount_total in SubsetSumEsymmVanishing.lean (verified axiom-clean [propext,Classical.choice,Quot.sound] via pg-iterate, 79s). (2) REFUTED part (the NEW content): the attack_plan prediction that the F10 count face inherits the SAME deep-moment/BGK content with a char-0<->char-p divergence at t*~2(beta+1)log_n p. Exact integer probes at PROPER dyadic subgroups: scripts/probes/_407_conn/C057_powersum_window_crossover.py (n=8,16; balanced a=n/2; control prime q'=1e7+; FOUR prize primes q~n^4..n^4*3 each, beta~4) shows the CONSECUTIVE-window count V(a,L)=#{S subset mu_n,|S|=a: p_1..p_L(S)=0} is char-uniform char0==charP at EVERY L=1..a-1 -> NO divergence, the predicted t* crossover never materializes in regime. C057_divergence_mechanism.py isolates the mechanism: char-p inflation IS real but only for a SINGLE NON-consecutive constraint (p_1=0 alone at mu_16/p=97: char0=70 -> charP=198, 128 spurious non-antipodal; only ODD exponents L=1,3,5,7 inflate, even L don't) -- this is exactly the Frobenius-only Galois obstruction of issue COMMENT 64 line 1785. The CONSECUTIVE Newton window (what the bridge actually governs) inflates only at L=1 and is char-uniform for L>=2 even at tiny beta=1.65 (q=97: window p_1^p_2=0 gives 6->6, 2->2): imposing the second power sum instantly collapses the spurious configs back to the char-0 count. So the count face does NOT inherit deep-moment content; the Newton-window object is char-0-faithful in the prize regime.",
  "honest_note": "The Newton-bridge identity itself is real and proven (not in dispute); C057 mislocates its consequence. The char-p inflation it points at exists only for the single-constraint / non-consecutive object and is the ALREADY-KNOWN Lam-Leung-mod-p / Frobenius propagation wall (grounding COMMENT 64, the 'all odd e_i<=k=0 => antipodal' char-uniformity gap), not a new deep-moment crossover transmitted to F10. Probes are exhaustive over all C(n,n/2) subsets at n=8 (C(8,4)=70) and n=16 (C(16,8)=12870) for multiple proper-subgroup primes; n=32 (C(32,16)=6e8) not brute-forced but the mechanism (consecutiveness heals char-p spurious configs) is robustly demonstrated at n=8,16. Refutation is of the count-inheritance CLAIM, not of the underlying char-p transfer wall, which stays open. Did NOT prove a closed-form delta*; the BGK/char-p-transfer wall is untouched.",
  "reduced_to": "",
  "wall": "char-p transfer of the char-0 power-sum/antipodal window (Lam-Leung mod p / Frobenius-only Galois propagation, issue COMMENT 64) = the same wall as open-core faces 3<->4; the Newton bridge does NOT transmit it to the F10 count face."
}
