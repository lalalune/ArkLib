{
  "id": "C025",
  "title": "Parity obstruction kills the char-0 e2=0 layer at k=0 mod 4 (production dims)",
  "verdict": "OPEN",
  "method": "mixed",
  "evidence": "Probes scripts/probes/_407_conn/probe_C025_parity_and_threshold.py, probe_C025_prizeregime_charp.py, probe_C025_charp_surplus.py (exact integer arithmetic, proper-subgroup primes). The cited Lean lemmas in ArkLib/Data/CodingTheory/ProximityGap/WindowTwoLayerThreshold.lean are GENUINELY TRUE (no sorry; only docstring 'sorry-free'): CHECK 1a confirms the parity identity coeff-sum(e2Folded) == C(|A|,2) MOD 2 on all sampled subsets (m=3,4,5), so e2Folded_ne_zero_of_odd_pairs is correct (my first integer-equality test was the bug, not the theorem). CHECK 1b confirms the threshold theorem by exhaustive search: ABOVE p>(2^(m-1)|A|^2)^(2^(m-1)), odd-pair sizes |A|=3,6,7 have ZERO e2=0 subsets, while even-pair |A|=4,5 have 10/8 solutions -> qualifying_implies_char0_vanishing + e2_ne_zero_of_production_dim are correct. BUT the decisive trap (CHECK 2 + probe_C025 prize scale): the threshold (2^(m-1)|A|^2)^(2^(m-1)) ~ n^Theta(n) is super-exponentially ABOVE the prize prime p~n^beta (beta<=5). At n=2^30: log2(threshold)=4.56e10 vs log2(prize p=n^5)=150 (ratio 3e8) -> hypothesis hp is VACUOUSLY UNSATISFIABLE at every prize prime. The kill only governs the char-0 layer / above-threshold primes; the prize lives strictly BELOW threshold where the char-p surplus (antipodal/BGK) layer is the wall and parity (a char-0 argument) gives no obstruction. The empirical '0 bad scalars at production dim |A|=6 below threshold' (probe_C025_charp_surplus, 40 prize primes, all empty) is the small-k=4 / agreement-above-delta* regime = comment-40's recorded K->0 for large q; it does NOT transfer to prize k=Theta(n), where DISPROOF_LOG round-6 (O22, ListInteriorUnconditionalT2) gives #bad >= C(n,k+2)/q^2 (near-capacity) governed by deep-moment structure, not by C(k+2,2) mod 2.",
  "honest_note": "The underlying Lean lemmas (e2Folded_ne_zero_of_odd_pairs, qualifying_implies_char0_vanishing, e2_ne_zero_of_production_dim) are a real, elegant, probe-verified true result (this part is PARTIAL-proven) and the parity insight is genuine. But the CONNECTION's claim that this empties the depth-1 prize bad-scalar set is NOT supported: the theorem's threshold is super-exponentially out of prize range, so it is silent exactly where the prize lives. I did not run a full lake build (file is already in-tree with #print axioms directives and predates this session); probes independently confirm the math. I did not refute any lemma -> not a DISPROOF_LOG entry; this is a vacuous-in-regime / welds-to-wall assessment.",
  "reduced_to": "",
  "wall": "BGK / Paley square-root-cancellation for thin mu_n (equivalently the DISPROOF_LOG '<=k-1 agreement ceiling' / char-p antipodal surplus layer). The parity+threshold law governs the char-0 layer only; the prize-regime bad scalars are the char-p surplus below the threshold, which is the recognized 25-year-open core."
}
