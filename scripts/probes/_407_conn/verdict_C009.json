{
  "id": "C009",
  "title": "The Hab25 K4 cell budget (<= n) is the EXACT governing-law budget q*eps*~n",
  "verdict": "REDUCED",
  "method": "mixed",
  "evidence": "Chain is exact and each link verifiable in-tree. (1) K4_cell_card_le_of_affine_pinning_family (Hab25K4AffinePinningCardBound.lean, PROVEN axiom-clean [propext,Classical.choice,Quot.sound]): one affine-pinned cell has <= n=|domain| bad scalars, via factorImprove_card_le_n (|improving scalars|<=|disagreeSet|<=n). (2) gsFactorIndex_card_le_natDegreeY (Hab25DegreeBudget.lean, PROVEN): the CELL COUNT c=(gsFactorIndex Q).card <= natDegreeY Q = l, the GS interpolant Y-degree = list-size bound l. Hence I(delta) <= c*n <= l*n. (3) Prize budget I(delta) <= q*eps* ~ n. So C009 closes the prize IFF l<=1 (single GS factor) in the window interior. DECISIVE NUMERICS (probe_C009_listbudget.py, exact rational): the ONLY proven q-independent list bound is below-Johnson l <= 1/((1-delta)^2 - rho) (JohnsonListBound / CandidateProofLoop9); it -> infinity exactly at the Johnson radius delta=1-sqrt(rho) and is VACUOUS (= inf) throughout the window interior (1-sqrt(rho), 1-rho-Theta(1/log n)] at every prize rate rho in {1/2,1/4,1/8,1/16}. Below-Johnson fixed-gap probes (DISPROOF_LOG O7') already exhibit l=2..5, and the in-tree falsify-probe (probe_capture_kernel_bridge.py, 839 pinned cells) found the maximal affine cell a STRICT subset in every multi-scalar bad set => c>1 occurs, c is substantive. Therefore c=l>=2 across the window: I(delta)<=c*n with c the SAME open list size is a tautology, not a closing bound. probe_C009_cellcount.py confirms exact codeword-enumeration is infeasible at the prize regime (q^k explodes for q~n^4, n>=8), so the question is structural, not numerical.",
  "reduced_to": "Bound the Guruswami-Sudan list size / GS-interpolant Y-degree l = (gsFactorIndex Q).card by a q-independent O(1) (ideally l<=1) in the window interior delta in (1-sqrt(rho), 1-rho-Theta(1/log n)] for explicit smooth-domain RS at a proper dyadic subgroup mu_n < F_q*. Equivalently: a q-independent beyond-Johnson list-decoding bound for these explicit RS codes. C009 correctly relocates the prize from 'per-cell size' (PROVEN <= n) to 'number of affine cells c=l' and proves the per-cell side at the EXACT prize resolution n (not the coarser Johnson scale); the residual is the cell count.",
  "wall": "BGK / Paley generalized-eigenvalue + capacity beyond-Johnson list-size wall: a q-independent (O(1)) GS list size in the window interior is exactly the unestablished beyond-Johnson capacity-list dual (DISPROOF_LOG O6'/P1; the q-independent Johnson budget diverges at 1-sqrt(rho)).",
  "honest_note": "The per-cell bound (<= n) and the budget coincidence (q*eps* ~ n = per-cell n, same resolution, not Johnson-coarse) are GENUINE and insightful and both PROVEN/verified in-tree (axiom-clean). What is NOT closed, and what C009's optimism overlooks, is that the number of affine cells c equals the GS list size l (in-tree degree-budget lemma), and l is the very open quantity: proven q-independent and finite only BELOW Johnson, diverging exactly at the window edge. So I(delta)<=c*n is a tautology in-window. No closure; this is an honest REDUCED (per-cell side proven, residual sharpened to the q-independent in-window list-size bound = the BGK/capacity wall). NOT a refutation of C009 (its proven half stands); the connection does NOT solve the prize. Probes added under scripts/probes/_407_conn/. Exact codeword enumeration was infeasible at prize-size primes, so the cell-count growth claim rests on the in-tree degree-budget identity + the exact list-budget divergence, not on a large-q brute force."
}
