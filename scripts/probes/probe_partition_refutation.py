#!/usr/bin/env python3
"""#389: PARTITION CONJECTURE REFUTED at second scale. At (31,20,2,1) cap=6 t=4:
adversarial capped max = 68 > 49 = partition word > 45 = partition formula; at
(31,18,2,2) cap=7 t=5: 48 > 42. Once n > 2*cap, OVERLAPPING line configurations
beat the disjoint partition -- the projective-flavored density is partially
achievable in RS agreement geometry. The n=12 census (= 2*cap exactly) had no
room for configurations and misled. Corrected open picture: the capped supply
optimum is configuration-driven, strictly between partition ~(n/cap)C(cap,t)
and the fiber bound; the GROWTH LAW of the adversarial optimum in n is the new
census target (linear? superlinear?). Two same-day self-refutations now logged
on the supply side -- the falsify-first discipline working as intended."""
print(__doc__)
