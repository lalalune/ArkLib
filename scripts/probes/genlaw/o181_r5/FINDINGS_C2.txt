FACET C2 — r=5 RUNG of the deep-band #bad census: PROVEN (axiom-clean Lean brick).

EXACT DATA (faithful BabyBear residual-det / divided-difference kernel; all REPRODUCED):
  r=5 deep-band #bad-scalar, maximizer line (e,f)=(n/2+1, n-1):
    n=16  #bad=89     (task exact data: 89  -> reproduced)
    n=32  #bad=1441   (task exact data: 1441 -> reproduced)
    n=64  #bad=22657  (NEW data point, this lane)
    n=128 #bad=356865 (NEW data point, this lane; ~2.5h compute)

CORRECTION to the task brief: the r=5 maximizer is NOT the full-order line (d=n).
  Measured maximizer (e,f)=(n/2+1,n-1): e-f ≡ n/2+2 (mod n), gcd(n,n/2+2)=2, so d=n/2.
  (Verified by full (e,f) sweep at n=16 -> max #bad=89 at (9,15)=(n/2+1,n-1), d=8=n/2;
   candidate lines at n=64 confirm (33,63) dominates by >20x.)

B2 ORBIT REDUCTION (the intended attack, executed):
  #bad_5 = 1 + (n/2)*full_orb = 1 + 2g*full_orb,  g=n/4
  full_orb (size-d=n/2 orbits) = 11, 90, 708, 5576 at n=16,32,64,128.
  gamma=0 is the unique singleton orbit; -1 in <g^{e-f}> so negation is internal to each orbit.

CLOSED FORM (4-point exact fit, UNIQUE degree-4; n=128 confirmed it, refuted competitor):
  #bad_5(g) = (4 g^4 + 3 g^3 - 10 g^2 + 12)/12
  full_orb(g) = (4 g^3 + 3 g^2 - 10 g)/24

BUDGET: K = 32*C(2g,5) is DEGREE 5; #bad_5 is DEGREE 4. So #bad/K -> 0, an EXTRA degree of
  headroom vs r=4 (where both are degree 4). K/#bad = 20.1, 97, 284, 684 (diverging).
  Proven: #bad_5 <= K and #bad_5 <= K/2 for all g>=3, via gap polynomials with
  all-nonneg-coefficient certificates under g=e+3:
    60(K - #bad)  = 512g^5 - 2580g^4 + 4465g^3 - 3150g^2 + 768g - 60  >= 0
    60(K - 2#bad) = 512g^5 - 2600g^4 + 4450g^3 - 3100g^2 + 768g - 120 >= 0

LEAN: DeepBandR5Bound.lean — axiom-clean ([propext, Classical.choice, Quot.sound]); 0 sorryAx.
  Key theorems: deepBandBadCount5_le_budget, deepBandBadCount5_two_mul_le_budget,
  deepBandBadCount5_descent, twelve_dvd_r5_num_even, choose_five_expand, rungs n16..n128.

HONESTY: the closed form is [COMPUTED]-calibrated (exact at n=16,32,64,128); the
  #bad = 1+(n/2)full_orb orbit reduction is MEASURED-VERIFIED (B2), not yet a field-level proof
  that the orbit count equals the polynomial for ALL n=2^k. The polynomial budget inequalities
  ARE fully PROVEN. So: budget bound on the formula = PROVEN; formula = true #bad = COMPUTED
  (k>=4 verified). This is the SAME proof status as the landed r=3/r=4 rungs.
