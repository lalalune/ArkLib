# Level-2 anatomy: C1379 — the agree-17 layer is one cyclotomic equation; the deep line at n=32

Lane `nubs/issue232-effective-pa`, 2026-06-10. Companion to `../RESULTS.md` (O87). Both halves
adversarially audited (sound, 0.95; all refinements below incorporated).

## A. The 1,344-layer anatomy (the level-2 analogue of the C19 anatomy)

Data: the O87 census's agree-exactly-17 layer (1,344 elements, independently enumerated twice at
BabyBear and regenerated a THIRD time by a full fresh `C(32,17)` sweep at a second prime
`p₂ = 3·2³⁰+1` — every invariant identical **including the literal index-level agreement sets**,
so the configuration is one `ℤ[ζ₃₂]` object reduced at split primes).

1. **The factorization law (1344/1344, both primes):** every dense element's error factors as
   `e = Π_{b∈B}(X²−z_b) · (X−x₁)(X−x₂)(X−x₃)(X−ξ)` with `(|B|,|O|) = (7,3)` universal and
   `ξ = −(x₁+x₂+x₃)` forced by `deg c < 16`. Consistency reduces to **one cyclotomic scalar
   equation**: `e₂(x₁,x₂,x₃) − e₁(x₁,x₂,x₃)² = λ + e₁(B)` — 0/1344 failures, completeness
   inherited from the exhaustive census. (Audit note: the equation's verification is the
   anatomist's, scaffold-audited; the factorization itself is audit-verified.) The level-2
   marginal count **is** the solution count of this equation; deriving 672 from it analytically
   is the named remaining step.
2. **1,344 = 2 × 672, structurally:** negation `x ↦ −x` acts freely (0 fixed points — *forced*
   by the parity law, audit-upgraded from observation to theorem-shape), giving 672
   `(B,O)`-classes. B-census: 580 distinct B's = 488(×2) + 92(×4) — **the same multiplicity
   menu {2,4} as C19's level-1 census** (16 = 4·2 + 2·4 over 6 B's).
3. **The witness identity is now structural:** `35 = C(7,4)` because the agree-18 condition is
   `e₁(S) = −λ`, forcing `z* = −λ ∈ S` and reducing to an 8-term vanishing sum in `μ₁₆` =
   4 antipodal pairs (the O50 antipodal theorem). Verified at both primes.
4. **Spread anti-poles (O63 chart):** witnesses occupy minimal branch classes (depth-3 spread
   {4: ×32, 2: ×3}); every dense element occupies **every** branch class at depths 1–3
   (maximal spread, pinned codes 16 ↦ (0,0,0), 18 ↦ (0,1,1)). Branch aliveness is total on
   the marginal layer.
5. **Witness-overlap typing:** intersection sizes `|T ∩ T_S| ∈ [5,14]`; 19 profile *types* (up
   to permuting the 7 pair-blocks; 192 as ordered vectors with multiplicities {16,8,4}); the
   n=16 rigid {5,6}-balance relaxes to this finite typing, and the n=16 union-containment
   invariant does NOT lift (960/1344 inside the witness union) — the lattice typing is the
   level-2 invariant. Region lattice: 9 atoms (sizes [2,2,4⁷]) generated by the O50 structure.
6. **Tie-class transport:** the alternate tie word's layers are the *exact image* of the
   canonical word's under the verified rotation-transport bijection (j=4 rotation, scalar) —
   equivariant, not independent; the second prime is the independent replication.

**Conjecture C1379 (C19 at level 2, char-0 form):** for `q ≡ 1 (mod 32)` prime power and
`λ = −z*`, `z* ∈ μ₁₆` max-fiber: `ℓ(w,18) = 35` and `ℓ(w,17) = 1,379` with the fixed
index-level anatomy above, for all but finitely many characteristics. Falsifiers: the same
~4-minute sweep at any further split prime (any deviation in count or index-sets), a
non-max-fiber λ (tests necessity), or an analytic derivation of 672 disagreeing.

## B. The deep line at n=32 (second level-2 family)

Same setup as O87 (g₀ = 31), deep line `u₀ = x¹⁸/(x²−w)`, `u₁ = 1/(x²−w)`, `z = 5`, `w = 25`;
calibrated bit-for-bit against the verified n=16 ground truth before n=32 was believed.

- **Witness floor (a = 18):** 11,440 bad scalars — **the full `C(16,9)`** (the map
  `S ↦ v_S(w) − w⁹` is injective at this z; measured) vs the monomial line's `N₀(16,9) = 3,280`
  (formula value; the O68 finding's n=32 analogue, gap widened 1.4× → 3.49×). **All 11,440
  per-γ lists are singletons; union = the structured `{q_S}` family exactly**; every record
  re-verified (agreement exact, NTT-confirmed codewords); emission identity
  `205,920 = 11,440·C(18,17)` exact.
- **Degeneracy is impossible a priori** (audit upgrade): `S_A = 1 + w⁹·S_B` identically, so
  `S_A = S_B = 0` cannot occur; measured 0 across all 565,722,720 subsets anyway.
- **a = 17 = k+1 is line-trivial:** the recovered γ makes the functional vanish by
  construction, so every nondegenerate subset emits — the layer was *exactly counted* by
  streaming (263,802,303 distinct γ's = 13.10% of `F_p`; 565,516,800 exact-17 pairs;
  cross-foot: `0 + 0 + 565,516,800 + 205,920 = C(32,17)` exactly). This is a structural
  triviality count, not enrichment data; the deep line's nontrivial radius at n=32 is a = 18
  only.
- Coverage: per-chunk swept counts = `C(31−i₀,16)`, summing to `C(32,17)`; n=16 calibration
  plus a pure-Python re-derivation of chunks i₀ = 6..15.

**Level-2 moral for the branch-count distribution:** on both families the distribution at the
witness floor is maximally concentrated (singletons / the structured core), and the first
marginal layer is governed by an explicit finite consistency equation with the same {2,4}
multiplicity structure at both proven levels.
