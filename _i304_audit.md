## Lane α delivered: independent audit of `128030fd4` + the four architecture seams (6 findings, 2 actionable) — and the C-A1 transport statement pinned by a width-genericity audit

Machine-verified against origin/main blobs (files byte-identical to the landing; full signature evidence in session transcripts). Per-theorem re-derivation of all 13 declarations in `PolyRootGradedBound.lean` + `SliceSeparabilityBoundary.lean`: **all statements honest, proofs sound, conventions verified** (the `evalX`-kills-middle-X / inner-Z reading confirmed against CompPoly's def — no Taylor-transposition-class bug in either file). F13's teeth confirmed in-tree: `LocalSeriesProducer.localSeriesDatumOn_of_rawGS` (:362) and `RawGSCargo.hHyp` (:442) carry `Hypotheses` for the whole interpolant, so the old 𝒪-spine producers are genuinely vacuous at multi-branch data.

### Findings
- **F-1 (MISMATCH, supply gap — engine grading).** The engine's capstone demands `P.Monic` + balanced grading `((P.coeff c).coeff j).natDegree ≤ s·(P.natDegree − c)`; **no in-tree GS producer emits this pair**: `gs_existence_zDegree_curve` (:194) gives a FLAT budget (no `(d−c)` factor) on a merely-nonzero `Q₀`; the `_card` variant (:363) drops the coefficient budget entirely; `gs_existence_sloped` is slope-1, non-monic. The tree already documents this honestly (`SectionNewtonGradingSupply` :14–23, :43–47: needs the ≥-half of `(1,0,s)`-weighted-degree multiplicativity, only ≤ exists) and partial repairs exist (`exists_graded_preimage_of_eval_sloped_eq_zero`/`_flat_eq_zero`; `integralNormalization` monicization at slope cost `W`, not `L−1`). Actionable: fix the `PolyRootGradedBound` docstring's "the balanced grading the GS interpolant carries" claim; the sharp slope remains a recorded open supply item.
- **F-2 (wiring gap — centre pinning).** `SectionNewtonGamma.newton_window_graded_preimage` (:240) is implicitly pinned to centre `x₀ = 0` (seed currency `map constantCoeff` + mod-X congruence, no `taylor` anywhere) and — unlike `LocalSeriesProducer`, which documents its identical pinning (:46, :67) — carries no honesty note. Repair is cheap (middle-variable `taylor x₀` preserves inner Z-degrees, T-monicity, and the balanced grading): add a taylor-conjugated wrapper + docstring note. No false statement.
- **F-3 (CLEAN + the small-char gate).** `hensel_root_unique` (HenselUniqueness:76) demands `IsUnit (f.derivative.eval a₀)` in `k⟦X⟧` — honest, and **structurally unsatisfiable for inseparable factors (f' ≡ 0)**: neither it nor `powerSeries_newton_root` can serve `p ≤ D_Y` without the Frobenius step. Independent confirmation that the small-char lane is the only route there.
- **F-4 (CLEAN).** `powerSeries_newton_root` seam shapes match end-to-end (`isUnit_derivative_eval_C_iff` reduces to `≠ 0` over `K = RatFunc F`, fed by `SectionNewtonXiSupply.derivative_eval_map_section_ne_zero` from the honest `ξ̄ ≠ 0`). Monicity correctly not required.
- **F-5 (CLEAN).** F13's meta-claim correctly hedged ("in general"); contrapositive fires whenever two decoded `F[Z]`-sections have non-constant difference at `x₀`.
- **F-6 (cosmetic).** `#print axioms` coverage misses 3 PRG decls (`polySwap`, `natDegree_polySwap_le`, `coeff_natDegree_le_natDegree_polySwap`) and 1 SSB theorem — worth completing on next touch.

### The C-A1 transport statement, pinned (width-genericity audit of every producer/front-door surface)
Every separable-regime surface (`UnifiedProducer`, both `_of_producer` consumers, `CurveFamilyData`, `strictCoeffPolysResidual_of_curveFamilyData`, `unifiedProducer_of_window`, `δ_ε_correlatedAgreementCurves`) is universally quantified in width with **no hidden width coupling** (`WordStack` is `Matrix κ ι A`, fully κ-generic; no `Fin.cast` anywhere; `[NeZero deg]` only at keystone doors). The consumable shape is **width-preserved, degree-inflated at the source**:

```lean
theorem unifiedProducer_of_frobenius {p : ℕ} [CharP F p] (e : ℕ) (hdeg : 0 < deg)
    (hsep : UnifiedProducer (k := k) (deg := p^e * (deg−1) + 1) domain δ) :
    UnifiedProducer (k := k) (deg := deg) domain δ
```

Mechanics: powered stack `û t i := (u t i)^{p^e}` (same `Fin (k+1)` index), good-set forward inclusion along the Frobenius bijection `ẑ = z^{p^e}` (agreement sets preserved pointwise — `(Σ z^t u_t)^{p^e} = Σ ẑ^t û_t` by char-p additivity), count trigger transports (injectivity), and the **comeback** is the new math: σ⁻ᵉ-coefficientwise + Vandermonde at ≥ k+1 good points kills non-`p^e`-multiple coefficients + `expand`-injectivity recovers `P z = Σ (z−x₀)^t c_t` with `deg c_t < deg`. Decisive advantages of this shape (vs the width-`p^e·k` or front-door-level alternatives, both audited and rejected): the producer layer carries **no probability hypothesis**, so no `errorBound` inflation and no Johnson-radius shrinkage — the keystone front doors run at the **target** `deg` unchanged; and `CurveFamilyData.hn : n < k+2` returns verbatim.

Supply of the source producer at `deg′`: (i) **UD regime — already unconditional** via `unifiedProducer_of_window` at `deg′` (window tightens by `deg′−deg`; the zero-error corner survives); (ii) Johnson regime — the per-factor core route (the v3 powered-word program two comments up), where the source's factors are the separable cores by construction. Build order: `PthPowerSubspace`/Vandermonde-into-submodule + the C-K2 dictionary (in flight) → `unifiedProducer_of_frobenius` → UD corollary → the Johnson core supply. Wave-2 workflow dispatching when the fence/dictionary builders land.
