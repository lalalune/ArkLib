## Round-15 complete — the verified GS ladder, top to bottom, `3767f758b`

Three deliverables close out the Guruswami–Sudan formalization arc (one agent stalled mid-round and was taken over by hand; all results re-verified):

**1. `SudanListBound.lean`** (posted earlier) — the end-to-end Sudan (m=1) list bound for general RS: `|L| ≤ (D−1)/(k−1)` under the explicit hypotheses `n < ∑_{j<D}(D−(k−1)j)` and `D ≤ t`, with concrete instance and a genuine multi-codeword witness. The Table-1 positive bound the issue's §7 requests.

**2. `GSExactCountWall.lean`** (own grind) — the exact-lattice-count wall, completing `GSJohnsonWall` (which covered the `D²/(2(k−1))` sufficient form). New content: the exact **upper** bound `2c·gsCount(c,D) ≤ (D+c)²` (Gauss sum over ℤ + AM–GM with `u = cq ∈ [D, D+c−1]`), giving the wall `√(n·c·m·(m+1)) < t·m + c` — same `√(cn)` asymptotics, with the `+c` slack made explicit. Concrete bracket at `n=100, c=25, m=1` (Johnson = 50): exact count **feasible at t=60**, **infeasible at t=59** — genuinely below the D²-form's `t=72`, still strictly above Johnson. **Both constraint accountings of the GS certificate provably stop above Johnson.**

**3. `GSHasseMultiplicity.lean`** (Fable agent, one-line fix by hand) — the **full multiplicity-m GS machine**, beyond Sudan:
- `hasse_interpolation_exists`: order-`m` vanishing interpolant (shifted-coefficient Hasse encoding) exists when `n·C(m+1,2) < #gsSupport(D,k)` — the m≥2 front end.
- `pow_X_sub_C_dvd_eval_of_hasseVanish`: order-m vanishing + `f(a)=b` ⟹ `(X−a)^m ∣ Q(X,f(X))` — the multiplicity payoff, via the inner-shift ring homomorphism.
- `factor_of_order_agreement`: `m·#(agreements) ≥ D` ⟹ `(Y−f) ∣ Q` — multiplicity-weighted root counting.
- **`gs_decoder_pipeline`**: one nonzero weighted-degree-bounded `Q` such that *every* degree-<k polynomial with `m·agree ≥ D` factors out. With the in-tree Y-degree cap this is the complete GS list bound at any multiplicity — the machine whose parameter optimization reaches the Johnson radius, and (per the walls) provably no further.
- Concrete ZMod 5 instances; all `[propext, Classical.choice, Quot.sound]`, 0 sorry.

**The verified ladder now stands complete:** Sudan end-to-end (m=1) → the multiplicity-m machine (full GS) → the two walls (exact-count and D²-form: no multiplicity certifies below `√((k−1)n)`-ish agreement) → the isolated open interior, capped from above by the constant-gap averaging bracket. Every formalizable rung of the GS route is now machine-checked; what remains is precisely the open research question — an *explicit* certificate system beating Johnson for smooth-domain RS — which no amount of formalization of known mathematics can close. Issue stays open per the maintainer's standing instruction.
