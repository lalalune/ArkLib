## THE SIMPLE-STRATUM EXACTNESS CONVERSE LANDED — the wide-circuit classification is a theorem (3 files, axiom-clean)

The multiplicity-free converse program closes. Three artifacts (full chain compiled + gates green):

**1. `SecondLayerConverseCore.lean` — the no-antipodal branch, MACHINE-GENERATED.** `secondLayer_of_no_antipodal`: over any commutative ring with a half-period (`h+h = 0`, `h ≠ 0`, doubling kernel `{0,h}`), a `Distinct6` stack with **no** antipodal pair, pairwise-distinct products, injective and `+h`-closed shifted 12-stack satisfies **one of the eight second-layer seed systems** (the supply systems of `SecondLayerSeedFamily`, completing them to an exact characterization). The file is emitted by `gen_noantipodal_lean.py` from the exact certificate data of `probe_noantipodal_branch_tree.py` — the canonical-matching tree has 10395 pairings, 10387 killed, 8 survivors; the emitted proof walks 82 nodes with 813 one-`linear_combination` kills and 8 early-exit conclusions (each branch concludes the moment its three system congruences are derivable). **The certificate-to-Lean generator is the industrial method one level up: the probe's integer linear algebra IS the proof, mechanically transcribed.** One generator bug (missing parens in nested `Or.inr`) — fixed at the generator, re-emitted, clean.

**2. `ChordConverseWrapper.lean` — the Balanced-side weld + the relabelings + THE ASSEMBLY.**
- `cast_signedExp` (the frame's `signedExp` casts to the core's `chordStack`), `closure_of_balanced` (balance ⟹ `+h`-closure of the cast stack, the fiber-count argument), `MultFree` + injectivity bridge, half-period/kernel bridges (via the landed `double_eq_zero_iff`).
- **The relabeling transport**: odd point-swaps act on the shifted stack as an explicit index involution composed with the global `+h` translation (`stack_swap₁₂`/`₂₃`, `swapPerm₁₂ = (0 4)(1 5)(2 8)(3 9)(6 10)(7 11)` etc.) — so injectivity and closure transport directly (`inj_of_swap`/`closed_of_swap`), no re-derivation through `Balanced` needed; the chord converse instantiates at all three antipodal labelings (`chord_form_of_balanced_antipodal₁/₂/₃`).
- **`simple_wideCircuit_classification`** — THE THEOREM: every balanced, multiplicity-free, `Distinct6` exponent-triple over `μ_{2^m}` is **horizontal ∨ vertical ∨ a two-plus-antipodal chord-law triple (one of three labelings) ∨ one of the eight second-layer systems** — assembled from the trichotomy (landed this session) by antipodality case-split into the three chord wrappers and the second-layer wrapper. Uniform in the scale, every disjunct in closed congruence form.

**3. The collision-branch chart (increment 4, probe).** `probe_collision_branch_tree.py`: the non-multiplicity-free stratum (profiles (2,2,1⁸)/(2⁴,1⁴)/(2⁶)) enumerated with the same certificate machinery — quartet structures + singles-matchings, each killed / routed to the chord side (antipodal-pair derivable) / landing in a second-layer system. The Lean collision branch is the remaining piece of the FULL converse; the generator extends to it directly.

**What this closes:** combined with the supply side (chord law + seeds I/II + doubling, all previously landed), the wide-circuit census of `Γ_n` on the simple stratum is now characterized **both directions, at every smooth scale, uniformly in n** — the four families are provably the only shapes. The fleet's census closed forms (horizontal `Σ C(η_s,3)`, vertical `C(n/2,3)`, chord `n(n−4)²/8`, second-layer cubic) now sit on an exact classification rather than a probe-verified one, modulo only the collision stratum (charted, certificates in hand).
