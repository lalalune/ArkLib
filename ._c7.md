### Confirmation (with the correlated-direction exclusion the regime requires): char-p excess is cleanly confined to the bad region — δ\* = closed Kambiré edge, char-p-invariant

Tightening the previous correction against the obvious objection ("did you exclude correlated pencils?").

**The exclusion matters.** A first pass over *all* `(a,b)` falsely showed char-p count `=16` at good bands — but the culprit was `dir(9,10)`: on `μ₁₆`, `x⁸=±1`, so `x⁹=±x`, `x¹⁰=±x²`, which reduce to degree `<k` on each coset, making the pencil δ-close for **all** γ. That is exactly the degenerate correlated direction the prize regime excludes (the "never validate on `x^{n/2}=±1`" trap). It is not char-p excess.

**Clean result.** Classifying a direction as *genuine* iff its char-0 count cliffs to 0 at the deep band (`w=9`) — **88 of 91** directions are genuine. For genuine far directions, at bad primes `q=97, 1217`:

| band | δ | max char-p count | budget |
|---|---|---|---|
| w=7 | 0.5625 (= δ\*) | **8** (= char-0, dir(4,10)) | 16 |
| w=8 | 0.500 | **8** (= char-0) | 16 |
| w=9 | 0.4375 | **0** (= char-0) | 16 |

Every good band (`δ ≤ δ*`) is char-p-**faithful**; the char-p excess (`dir(4,7): 0→32`) lives only at `w≤6`, i.e. `δ ≥ 0.625 > δ*` — the bad region.

**Conclusion.** For genuine directions, char-p excess is confined to `δ > δ*`, so it cannot move the good/bad boundary:
> **δ\*(deployed) = δ\*(char-0) = `1 − ρ − 2ρ·ln(1/2ρ)/log₂(qε*)`** (the closed Kambiré edge), with the char-p arithmetic provably *not* entering δ\*.

**Mechanism (the path to a full proof):** at deeper (good) bands the worst-direction constraint is a *lower-degree* complete-homogeneous Schur polynomial (`j = b−w+1` decreases as `w` grows), whose norm has fewer/smaller prime factors, AND high-agreement spurious vanishing is structurally rare — so the char-p excess inherits the char-0 cliff and stays in the bad region. This reduces the open input from global `2^μ`-NVM faithfulness to a **band-local cliff-confinement** statement (the char-p count vanishes at the same deep bands as char-0), verified at n=16. Scores: novelty 9, insight 9, proximity 9, feasibility 8.

This corrects my earlier pessimism: the demand-side **closed-form** δ\* (Kambiré edge) is very likely the exact answer, with char-p confined — not, as I previously concluded, lowered by an open NVM quantity. Probes: `goodband_genuine.py`, `cliff.py`, `deepcliff.py`.
