## Round-20 — NEW THEOREM: the clique double-block kernel is the twisted evaluation pencil, `3fe265577`

Pushing past the beachhead into Conjecture 41's actual rank question, I worked out and verified the kernel structure of the conjecture's universal obstruction.

**The mathematics** (derived fresh; `Conjecture41CliqueKernelStructure.lean`, axiom-clean, strict-flags-verified):

1. **Duality.** Under the coefficient pairing `⟨P, s⟩ = Σ_j P_j s_j`, the clique locators `Λ_{E_α}` and the vertex evaluation syndromes `ev_β = (1, β, β², …)` form a **dual system**: `⟨Λ_{E_α}·X^r, ev_β⟩ = β^r·Λ_{E_α}(β)` — zero off-diagonal on W, nonzero on the diagonal.
2. **`clique_kernel_mem`** — the **twisted evaluation pencil** `s₁ = −Σ_β γ(β)b(β)·ev_β`, `s₂ = Σ_β b(β)·ev_β` satisfies **all** `(w+1)·c` kernel conditions of the double-block matrix `[N|γN]` — at every codimension `c`, over every field, for every twist assignment (the α-condition collapses by diagonality to `(−γ(α)+γ(α))·b(α)·α^r·Λ(α) = 0`).
3. **`evalSyndrome_family_injective`** — the pencil has full dimension `w+1` (duality reads off `b(α)·Λ(α)`; no Vandermonde determinant needed).

**Consequence — a structural correction to the conjecture's landscape:** the universal obstruction is **unconditionally rank-deficient** (`rank A_clique ≤ 2D − (w+1)`, always). Conjecture 41's "full rank" branch *always fails* for cliques, so the conjecture lives entirely in its **degeneracy branch**: every kernel syndrome must be degenerate. And the pencil syndromes are *exactly* the syndromes of error vectors supported on the vertex set `W` — the paper's Remark-31 "false positives". The conjecture is now sharply re-posed on an explicit object: **when does the twisted pencil contain a non-degenerate syndrome?** (At `p = 61, 113` it does — their counterexamples; the effective threshold is the integer-determinant divisor set from the Round-19 transfer engine.)

Also derived (paper math, in the docstring, partial fractions): the single-block relation module is exactly `{((x−α)v_α) : deg v_α < c−1, Σv_α = 0}`, giving `rank N_clique = D` and the complete kernel count — formalization queued.

The wall is being converted into explicit, attackable algebra. Issue stays open.
