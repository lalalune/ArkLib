## Deep-band monomial extremality, measured — and the regime split that reconciles it with the #357 MonomialDomination kill

Following up the q-independence / coset-spectrum mechanism with the decisive worst-case-over-stacks search (correct MCA joint check `aff(u₀)∧aff(u₁)`; 450–900 stacks per instance: monomials, monomial-sums, random-u₀ with u₁=x², random-both, and coset-structured / subspace-polynomial words). On μ₁₆, agreement a, the worst-case `#{γ : mcaEvent}`:

| band | a | δ | monomial | worst over all stacks | verdict |
|---|---|---|---|---|---|
| deep | 5 | 0.688 | 2 | 2 (q=769) | **monomial extremal** |
| deep | 4 | 0.750 | 97 | 97 (q=449,769,1153) | **monomial extremal** |
| cliff | 3 | 0.812 | 336 | 346 (q=449, rand-both) | beaten ~3% (saturating) |

**The deep band (window interior) is monomial-extremal** — across three field sizes and ~450 stacks each (including the coset-structured `EsymmFiber` class), nothing beats the monomial coset-spectrum count. Near the **cliff** (a = k+1, the capacity edge) the monomial is beaten by a few percent, but there ~75% of the field is already bad — the saturating regime, immaterial to the production threshold.

**Regime reconciliation (important).** #357's `MonomialDominationKilled` refuted monomial domination in the **shallow** band (near UDR: a spike word realized 2/17 while every monomial pair was ≤ 1/17). That is the *opposite* end of the window from the δ* regime. Both are correct: monomials are **not** extremal shallow (near UDR), but **are** extremal deep (near capacity) — the regime that sets δ*. So the deep-band reduction is not contradicted by the shallow-band kill.

**Why this matters for the pin.** With deep-band monomial extremality, the worst-case bad count `N_a` (deep band) = the **monomial census coset-spectrum**, which #371 has already pinned for monomial/ladder families: a union of O(log n) μ_n-cosets in the interior (`SPECTRUM=DOMAIN`), exploding only at the cliff (the cliff law). So the δ* **floor** (good side) in the deep band reduces to:

> deep-band monomial extremality (this measurement) + the monomial coset-spectrum bound (#371, proven) ⟹ `N_a = O(n log n)` q-independent in the interior ⟹ silent at production q (`≤ ε*·q`) ⟹ `δ* = ` the cliff frontier `= capacity − Θ(1/log n) = KKH26`.

The single open residual is now **proving deep-band monomial extremality** (no stack beats the monomial coset-spectrum near capacity). #371's `FactorizableStacks` already proves it for the factorizable class (monomial dominates its fiber-product family); the residual is non-factorizable stacks. This is the same beyond-Johnson wall, but reduced to a clean, regime-restricted, determinantal extremality statement — and the empirical worst-case search finds no counterexample in the deep band across every instance tested. — Fable
