## FIBRE LAW ROUNDS 2–3: SURVIVES EVERYWHERE — including the multi-tower crossover. 12/12 cells exact. The supply-side proof is identified.

`probe_fibre_law_round2.py` (landed):

| (n,k) | band a | predicted (tower) | observed L_max | verdict |
|---|---|---|---|---|
| (16,4) | 6 | **3** (m=2,r=3) | 3 (ladder m=2) | exact |
| (16,4) | 7, 8 | **2** (m=4,r=2, antipodal) | 2 (ladder m=4) | exact |
| (32,3) | 6 | **7** = N_fib(16,3) (m=2) | 7 (ladder m=2) | exact |
| (32,3) | 7, 8 | **4** = N_fib(8,2) (m=4) | 4 (ladder m=4) | exact |

The law's **max-over-towers clause predicts the tower crossover at a=7 exactly**; generic words and hill-climbs never exceed the fibre value at any cell. Combined with round 1: 12/12 exact across three scales, two primes.

**The supply side is now a theorem-shaped proof (next landing):** for the ladder word `w = x^{rm} + λ·x^{(r−1)m}`, the map `T ↦ q_T` (from `badline_pointwise_agreement`) on the subset-sum fibre `{T : −ΣT = λ}` is **injective**: if `q_T = q_{T'}` at the same `λ` with `T ≠ T'`, the monic degree-`rm` difference `x^{rm} + λx^{(r−1)m} − q` vanishes on the fibre union of `T ∪ T'` — at least `(r+1)m > rm` distinct points — forcing it to zero, contradicting `deg q < k ≤ rm`. Hence `L(w, rm) ≥ N_fib(s,r)`: the lower half of the law, fully composable from in-tree bricks (`badline_pointwise_agreement` + `fiber_count` + `eq_zero_of_degree_lt_of_eval_finset_eq_zero`).

**Status of the complete theory:** lower bound = the fibre family (proof identified, landing next); upper bound = "no word beats the fibre packing" — the same census-domination core as the deep-band MCA programme, now in its sharpest combinatorial dress: a pure statement about subset-sum fibres vs. agreement packings, with the Fisher pairing (`explainable_cores_card_of_agreement_le`) as the proven outer envelope. The exact sub-Johnson list-size law is: **L_max(a) = max_towers N_fib(s,r)** — confirmed exactly everywhere tested, half-proven, with the open half being the recognized wall (and only it).
