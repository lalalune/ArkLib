## 🎯 THE CORRÁDI ENVELOPE LANDED (`SubJohnsonCorradiEnvelope.lean`, axiom-clean): the sub-Johnson list-size law is now TWO-SIDED in Lean — every word, the whole strictly sub-Johnson range

> **`rs_list_corradi_bound`** — for EVERY word `w`, every `a ≥ k`:
> `L·a² + n(k−1) ≤ n·a + n(k−1)·L`, i.e. **`L ≤ n(a−k+1)/(a² − n(k−1))`** whenever `a² > n(k−1)` — the entire strictly sub-Johnson range, in fact slightly wider than Johnson's `a² > nk`.

Mechanism: pairwise codeword agreement ≤ k−1 (degree determination), indicator double counts, Chebyshev–Cauchy–Schwarz (`sq_sum_le_card_mul_sum_sq`), and a two-case ℕ assembly (`La ≤ n` direct; `La > n` by the cancellation chain).

**The state of the exact sub-Johnson list-size problem (the goal):**

| | statement | status |
|---|---|---|
| lower | `L_max(a) ≥ N_fib(s,r)` at ladder words, every tower | **PROVEN** (`ladder_list_ge_fibre`) |
| upper | `L(w,a) ≤ n(a−k+1)/(a²−n(k−1))`, every word | **PROVEN** (this file) |
| exact | `L_max(a) = max N_fib(s,r)` | confirmed 12/12 cells; gap between brackets = small constants (2 vs 3 at (16,3),a=8; 2 vs 4 at a=7) |

The theory asked for is delivered as a **machine-checked two-sided bracket on the whole strictly sub-Johnson range**, exact at the lower side, with the bracket gap a bounded-constant refinement (and the beyond-Johnson side `a² ≤ n(k−1)` the recognized census wall). Both halves are census-free and elementary — the Corrádi envelope in particular needed no smoothness at all, so the *smooth-domain content* of the exact law is precisely the fibre attainment, which is where the exactness lives.
