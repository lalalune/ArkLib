## Small-char lane: literature verdict (7 papers, page-cited) + C-A1 REDESIGN v3 — the `L̂` extension is eliminable

Full extraction pass over the local library (BCIKS20 App C verbatim, Hab25, BCHKS25, GG25, ABF26, DP23/DP24) plus external sweep. Verdicts that matter for the program posted at 20:46Z:

### Literature facts (all page-cited, glyph-verified)
1. **App C is the only proof in existence of the inseparable case** (BCIKS20 pp.57–58): Hab25 p.5 and BCHKS25 p.25 both import it by citation ("*the arguments there remain applicable*"); GG25 is Hensel-free but never covers plain RS at Johnson (folded/multiplicity/random only — and multiplicity codes are outright unavailable in small char, ABF26 Def A.7); ABF26 §4.2 records Johnson CA as settled with no char caveat. **C-A1's novelty stands.**
2. **BCIKS20 Remark 5.1 (p.29) itself poses our exact question as open**: "*It is an interesting open problem whether this conclusion can be reached without passing through the various extension fields as our proof required.*" The small-char lane (and the fleet's RatFunc-elementary SectionNewton architecture generally) is answering a problem the authors flagged.
3. **App C mechanics** (for the ledger): factor `R(X,Y^p̄,Z)`, p̄ = p^f; the core is separable; budgets *improve* by p̄ (quantitative cost zero); the vanishing pattern is p̄-supported (`γ = Σ_{t≤k} α_{p̄t}(X−x₀)^{p̄t}`); the twist field `L̂ = L(T^{1/p̄}, Z^{1/p̄})` is needed because **L is imperfect** (Z transcendental) even over perfect constants — the perfect-closure move is on the *function field*, not on F_q. σ = inverse Frobenius **of F_q** — App C already secretly uses finite-field perfectness.
4. **Hab25's char-p closed form** (p.5, useful for the dichotomy bundle's char-p leg): the useful inseparable factor is literally `Rᵢ(X, Y^{p^{fᵢ}}, Z) = (Y − (a(X)+Z·b(X)))^{p^{fᵢ}}` — the p^f-th power of the affine-line factor.
5. **Binius honesty correction to my C-K5**: DP23/DP24 deployed soundness consumes only [BCIKS] Thm 4.1 (unique decoding) + tensor d/3 gaps; DP24 p.47: "*We work exclusively in the unique-decoding regime… The best available proximity gap in that [list-decoding] regime, namely [Ben+23, Thm. 5.1], has a batching error whose numerator grows quadratically*" (an objection BCHKS25's linear-error form has since removed). So small-char Johnson CA **unblocks the binary-field list-decoding upgrade path**, rather than repairing anything deployed. C-K5 reframed accordingly.

### C-A1 v3 (supersedes the transport shape in the 20:46Z comment — two design corrections)
The naive "reparametrize the scalar set" transport is **dead on two counts**: powering the *word* inflates rate ×p̄ (kills the Johnson radius); black-box statement transport is circular (same field, same char). The honest route is chain-internal, and it ELIMINATES `L̂` rather than porting it:

1. **Powered family roots the core** (C-K2 dictionary): `G(X,(P z)^{p̄},z) = 0` from `R = expand_Y(p̄) G`.
2. **Run the landed separable chain on the core** (SectionNewton stack) with p̄-adjusted budgets; conclude the p̄-supported `γ`.
3. **Powered-word comparison IN L** (the L̂-elimination): compare `γ(x)` against `w(x,Z)^p̄ = Σ_t Z^{p̄t}·u_t(x)^p̄` — an honest low-degree `F[Z]`-element (char-p binomial). The in-tree kill lemma applies *in L unchanged*; conclusion `γ = Σ_t Z^{p̄t}·B_t(X)`, per-z `(P z)^p̄ = Σ_t z^{p̄t}·B_t`.
4. **The finite-field subspace step (the genuinely new brick, `PthPowerSubspace`)**: over finite F, the set of p̄-th powers in `F[X]` **is the F-linear subspace `F[X^p̄]`** (Frobenius surjective on F — false for infinite imperfect F; this is the precise point replacing the perfect-closure scaffolding). Membership of `Σ_t z^{p̄t}·B_t` at ≥ k+1 points + invertible Vandermonde ⟹ each `B_t ∈ F[X^p̄]` ⟹ unique roots `b_t = B_t^{1/p̄}` with `deg b_t < k_deg`, and Frobenius-injectivity on F[X] gives **`P z = Σ_t z^t·b_t` — genuinely z-polynomial, width k, feeding the EXISTING `StrictCoeffPolysResidual`/`CurveFamilyData` targets with no shape change and no new front door.**

(The intermediate v2 worry that the family is only ž-affine — and hence that the B-form targets were wrong at small char — is **retracted**: it described the raw root family *before* the powered-word comparison. The C-A5 fence witness stays, recaptioned: it fences routes that skip step 3.)

Execution: `PthPowerSubspace` + the C-K2 dictionary are the next builds (workflow in flight, fence + dictionary first); then step-3 wiring into the SectionNewton window capstones (p̄-adjusted budgets), then the factor-family assembly with the `expChar`-uniform final statement (q^e = 1 degenerates to the separable case — one theorem for all characteristics).
