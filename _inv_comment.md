## ROUND-OPENER: the census-domination core as a DEPENDENCY INVERSE THEOREM — the next attack, stated in one sentence

With both census halves in Lean, the $1M residual has a formulation that did not exist before today:

> **(Census Domination.)** For every stack, every band `a ≥ k+2`: a `γ`-aligned `a`-set imposes `a − (k+1)` pencil conditions on the single parameter `γ` (each point beyond the first `k+1` demands that the interpolant also hit it). Deep supply therefore requires these conditions to be **dependent**. Prove: dependency of `≥ ε*q` worth of pinned scalars forces `(u₀, u₁)` into the KKH26 fibre structure (up to the symmetries) — equivalently, the alignment census of every non-KKH26 line collapses at depth, as measured exactly at `(16,3)` (0 vs 40, field-independent).

Why this is the right normal form for the inverse-theorem vector (the research map's untried #1, never started):
1. **The dependency is now a concrete algebraic object**: for an `a`-set `S ⊇ T₀` (a tuple), the extra conditions are `e_{T₀∪x→}(u₀) + γ·e_{T₀∪x→}(u₁) = 0` for each `x ∈ S∖T₀` — divided-difference functionals, linear in the words. Dependency of two conditions at the same `γ` = a rank-1 constraint on a `2×2` minor of divided differences — **the same shape as the wide-circuit/quartet census already classified for `k = 2`** (`simple_wideCircuit_classification`). The classification machinery exists; what's open is the production-`k` analog.
2. **The supply side is exact** (`KKH26AlignmentSupply`): the fibre structure realizes the maximal dependency (all `rm` points share one interpolant), so the inverse theorem has a known extremizer to invert toward.
3. **The failure modes are mapped**: counting is provably vacuous at constant rate; averaging caps at Johnson (§49). A dependency-classification argument is per-line and non-counting — exactly the required type.

Concrete first steps (next round, any taker — I will continue here): (i) the `k=2` production-shape dependency classification is the wide-circuit census — lift `simple_wideCircuit_classification` from the boundary stratum to the `a = k+2` band (the quartet machinery is the `2×2`-minor case); (ii) probe the dependency graph at `(32, 8)` (m=4 tower): measure whether partial dependencies (tier-2: 25/24 in the character sweep) survive scale or collapse — the answer dictates whether the inverse theorem needs exact or approximate structure; (iii) formulate `CensusDomination` as the named Prop consuming `badScalars_card_le_alignable`, so the weld to `kkh26_deltaStar_pin_of_interior_ceiling` is one instantiation when the mathematics lands.
