# Ranked ledger of novel δ* routes that AVOID Johnson + character-sums (2026-06-13)

Per the directive: generate novel closed math avoiding (a) the Johnson reduction and (b) the open
square-root-cancellation/additive-energy problem; rank novelty/insight/proximity/feasibility 1–10;
keep only ≥9-on-all, else move on. This session tested THREE genuinely-new routes (none pairwise-Fisher,
none a field character sum). Honest scores:

| route | nov | ins | prox | **feas** | verdict |
|---|---|---|---|---|---|
| **RCW / Frankl–Wilson** (bound list by # distinct ∩-sizes `s`, `#≤C(n,s)`) | 7 | 6 | 7 | **2** | DEAD — realizable system has full ∩-spectrum `{0..k−1}` ⟹ `s≈k`, bound = trivial `C(n,k−1)`. Reduces to a *triviality* (also forbidden). |
| **ℤ/n free-orbit decomposition** (`\|list\| = n·#orbits`, rotation+scaling action) | 8 | 7 | 8 | **4** | PARTIAL — factor `n` is closed/proven/formalizable; but `#orbits` is field-dependent (erratic 0–7, spikes at collision primes) = the spurious-collision count `/n`. Residual = the core, `/n`. |
| **m-interleaved `C^{≡m}` list** (collaborative decoding) | 7 | 7 | 8 | **3** | DEAD-END — reduces to **GG25 curve-degree** (open for plain RS; proven only folded/random). Same hard family. |

## The meta-finding (why no route reaches feasibility 9)
All three independent novel routes — extremal set theory, group equivariance, interleaving — **converge
to the same irreducible core**: the additive/collision structure of `μ_n` mod `p` (RCW→trivial bypasses
it but loses everything; equivariance peels the closed `n`-factor but the orbit count IS it `/n`;
interleaving repackages it as curve degree). This is now strong *structural* evidence that the core is
genuinely irreducible: **any route either (i) reduces to Johnson, (ii) reduces to a triviality, or
(iii) reduces to the spurious-collision / square-root-cancellation core.** No fourth exit has appeared.

## The one genuine keeper (closed, novel, but partial)
> **Free-orbit theorem (closed):** the beyond-Johnson worst list of a deep-hole word is a disjoint union
> of free `ℤ/n`-orbits ⟹ `|Λ| = n·#orbits`. Exact, field-independent, formalizable.

It does not close δ* (the orbit count is the residual). It is the most novel *closed* structural fact of
the session and strictly shrinks the open core (by the proven factor `n`), but per the ≥9-on-all bar it
fails feasibility (4) — so by the directive's own rule it is recorded and we move on, not promoted.

**Honest status:** no conjecture meets 9/9/9/9. The prize core survives every novel route tried, now
with three independent reductions documenting its irreducibility. No closure claimed or fabricated.
