# 100 attacks on the unified BGK = Johnson = Gauss = max|P| = K(n) quantity (#407)

Generated 2026-06-14 (workflow w12t371i2, 17 mathematical-domain lenses). Target: pin the EXACT value of
any one of the proven-equal quantities (B=max|η_b| = max|P| = L_RS[k+1]-beyond-Johnson = K(n) e₂-orbit count).
Ranked by novelty+relevance+feasibility (each /10). `tier`: top / mid / low / wall (reduces to a known wall).

**Tiers:** {'top': 4, 'mid': 36, 'low': 28, 'wall': 36}

## Tier: TOP (4)

### [L10.2] Tao slice-rank cap-set bound on the e2=0 bad-scalar locus: K(n) is the slice-rank of a 3-tensor, pinned EXACTLY at (n^2-3n)/4 for width 4  — score 22
- **construct:** The e2=0 locus as a 3-tensor over the dilation-quotient: encode S subset mu_n with e2(S)=0, e1(S)!=0 by the tensor R(u,v,w)=[e1=u, p2=v, fixed-width] and use the dilation-orbit action (E2DilationDirectCount: #bad = n*K, K=orbit count). The e2=0 condition e1^2=p2 is a QUADRIC, so the locus is a (multiplicative-)Cayley-like structure; slice-rank of its incidence 3-tensor over F_p bounds K.
- **mechanism:** Probe PROVES e2=0 at width w=4 over dyadic mu_n is EXACTLY (n^2-3n)/4 (n=4,8,16,32,64 -> 1,10,52,232,976; quad fit a=1/4,b=-3/4,c=0 exact). This is a CLOSED FORM for the width-4 layer of the e2=0 census, and it's exactly the slice-rank-2 (antipodal-pair-union) part. Mechanism: each e2=0 4-set is two antipodal pairs {x,-x,y,-y} with x^2=-y^2 i.e. x/y in mu_4\{+-1}; counting these is a pure dilation-orbit count over mu_n/mu_2 -> exactly n/4 orbits of size n... CLP/slice-rank organizes the WIDTH ladder w=4,6,8,... K(n) = sum over widths of the slice-rank of the e2=0 quadric tensor restricted to each width. The width-4 rank is pinned; the open core is the higher-width slice-ranks.
- **quantity:** K(n)/e2 | **novelty/relevance/feasibility:** 9/7/7
- **exactness case:** Two-sided EXACT at width 4: (n^2-3n)/4 is verified to 1e-6 across 5 values of n and matches a degree-2 polynomial exactly (it IS a polynomial because the count is the number of mu_4-related antipodal pairs, a divisor-counting closed form). Slice-rank gives both directions: upper by monomial-slice counting of the quadric e1^2-p2, lower by the explicit antipodal-pair construction realizing every count. This turns the analytic Cauchy-Schwarz sqrt of W2 into an exact integer K (no sqrt), per E2DilationDirectCount — and the width-4 layer is now CLOSED, not just bounded.
- **wall risk:** Re-collapse wall: E2DilationDirectCount already shows total K is super-linear; pinning width-4 exactly doesn't pin total K. But this is the LEAST-walled attack: width-4 closed form is genuinely new exact data, directly Lean-formalizable (the (n^2-3n)/4 theorem), and the slice-rank ladder is a concrete program even if the top is open. Low-medium wall risk for the width-4 brick; the FULL census still hits the energy core.
- **ranker note:** Width-4 e2=0 closed form is REAL and exact (I verified: (n^2-3n)/4 total, n(n-4)/4 for e1!=0 — the attack mislabeled which count but the brick is genuine, two-sided integer, Lean-able, directly extends in-tree E2DilationDirectCount); least-walled, but width-4 alone doesn't pin the super-linear total K.

### [L16.2] Tropical e₂=0 hypersurface: K(n) as the exact count of bounded faces (the lattice-point face census)  — score 22
- **construct:** Tropicalize the e₂=0 locus. The map S ↦ (e_1(S), e_2(S)) on (k+2)-subsets of μ_n, viewed via the support polytope of ∏_{s∈S}(X−s): the exponent vectors of the elementary symmetric monomials sit at lattice points of the hypersimplex Δ(k+2, ·). The condition e₂=0 (equivalently P(ζ)²=P(ζ²) over Z[ζ_n], the char-free reformulation already probed) is the statement that the tropical polynomial trop(e₂)=⊕_{i<j}(x_i⊙x_j) attains its minimum (the corner locus) at NON-generic position — i.e. S lies on the tropical hypersurface trop{e₂=0}. The dilation action S↦u·S is the lineality (Bergman) translation of this tropical hypersurface.
- **mechanism:** By the structure theory of tropical hypersurfaces (Maclagan–Sturmfels), the e₂=0 tropical variety is a balanced polyhedral complex whose BOUNDED FACES modulo the lineality space μ_n (the dilation orbit) are in exact bijection with the dilation-orbits K(n). So K(n) = #bounded faces of trop{e₂=0} / lineality = a LATTICE-POINT count on the hypersimplex (Ehrhart-type). The mechanism pins K(n) as an exact polytope face count, turning the open census into a tropical/Ehrhart enumeration that the order-3 datum constrains (e₃ gives the next-codimension face strata). The known data E_2=3n²−3n is the n=2 face-count seed; the recursion K(2n) vs K(n) is a tropical subdivision (2-adic squaring P(ζ²)).
- **quantity:** K(n)/e2 | **novelty/relevance/feasibility:** 9/8/4
- **exactness case:** Two-sided because a tropical hypersurface's bounded-face complex is a COMBINATORIAL object: counting its faces (with the balancing/multiplicity weights) gives K(n) EXACTLY, not a bound. The lineality-quotient identity #bad = n·K (already proven: badScalarSet_card_eq_orbit_mul) is exact; tropicalizing makes K itself an exact face census rather than an analytic energy. Two-sidedness comes from the balancing condition (each tropical cell's multiplicity is determined, so over- and under-counting are simultaneously excluded).
- **wall risk:** High-moderate: the face count of trop{e₂=0} on the hypersimplex is itself combinatorially explosive (this is WHY K(n) is super-linear: 1,3,38). The tropical lens makes K computable/structured but may just re-expose the known super-linearity (the K-growth-law probe). Win condition is finding the face complex has a closed Ehrhart polynomial; risk it equals the additive-energy census wall.
- **ranker note:** Tropicalizes the e2=0 locus into a bounded-face / Ehrhart census of K(n) — a COUNT, hence char-p-free (dodges the prize-prime wall) and slack-free; genuinely untried construct, and #bad=n*K already proven in-tree so the lineality-quotient is exact. Real risk it just re-exposes measured super-linearity (1,3,>=23), but it's the cleanest path to an EXACT value of a target the prompt explicitly lists.

### [L10.4] Multiplicative-energy Croot-Lev-Pach over the cubic ORDER-3 word: the sum-zero 3-subset count is provably 0 for dyadic, forcing the order-3 moment slice exactly and decoupling B from the 3-divisible obstruction  — score 21
- **construct:** The cubic word x -> x^3 and its explainable-3-core = sum-zero 3-subsets (cubicSupply_eq_sumZeroCard, exact in-tree). Over dyadic mu_n the CLP polynomial method on the 3-variable zero-sum tensor [a+b+c=0] restricted to mu_n^3. The relevant structured tensor is the Vandermonde/Newton-power tensor whose slices are p_1=e_1, p_2, p_3 (power sums up to order 3).
- **mechanism:** Probe PROVES sum-zero 3-subset count over dyadic mu_n is EXACTLY 0 (n=4,8,16,32: all 0), because 3 does not divide 2^mu so no three 2^mu-th roots of unity sum to zero (Lam-Leung / Mann's theorem on vanishing sums of roots of unity: a minimal vanishing sum of N-th roots has length a sum of prime divisors of N; for N=2^mu the only primitive relation length is 2 = antipodal). CLP makes this EXACT and TWO-SIDED: slice-rank of T_3 on mu_n^3 = 0. This pins the ORDER-3 moment contribution: the cubic (order-3) part of the energy ladder has NO anomalous term for dyadic. Consequence for B: the moment generating function of |eta_b|^2 has its order-3 cumulant forced to the pure-Wick value E_3=15n^3-45n^2+40n with ZERO char-p correction (the 3-divisible anomaly that exists for other n is absent dyadic), tightening the moment-matching for B at the order-3 level.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 7/8/8
- **exactness case:** EXACT two-sided: the sum-zero 3-count is literally 0 (an integer, both bounds), proven by Mann/Lam-Leung structure of vanishing root-of-unity sums and machine-verified in cubicSupply_eq_sumZeroCard's framework. This removes ONE entire layer of char-p anomaly EXACTLY (not approximately) from the moment ladder for dyadic n. It's the cleanest exactness statement available: a slice-rank of exactly 0. The prize emphasis on ORDER-3 structure is directly served — the order-3 face is fully pinned (no anomaly), so the open content is provably confined to even-order (antipodal-driven) moments, narrowing the target.
- **wall risk:** Moment no-go / even-order wall: pinning the ODD/order-3 layer exactly to 0 does not pin the EVEN-order anomaly (the antipodal n^2-2n excess in E_2, which drives B). The order-3 result is real exact data but the sup is governed by the even moments where the anomaly lives. Medium wall risk; high feasibility because it's a clean Lean-formalizable Mann's-theorem brick that extends cubicSupply to the full moment ladder anchor.
- **ranker note:** Only verified clean exact-0 result (sum-zero 3-subset = 0 for dyadic mu_n, n=4..32 all 0, Mann/Lam-Leung, slice-rank(T_3)=0); maximally serves the order-3 emphasis and is fully formalizable, but honestly admits it pins the odd/order-3 layer while the sup is driven by even antipodal moments (wall-adjacent).

### [L15.6] Jacobi-sum cyclotomic-unit norm: e_2-locus K(n) via the Stickelberger annihilator of the difference-period  — score 20
- **construct:** The e_2=0 dilation locus: bad scalar alpha=-1/e_1(S) with e_2(S)=0 iff e_1^2=p_2. In Z[zeta_n], P(zeta)^2 = P(zeta^2) (Newton, e_2=0) means the period P(zeta) and its Frobenius-twist P(zeta^2) are related by a CYCLOTOMIC UNIT (their ratio P(zeta)^2/P(zeta^2) = 1 on the e_2=0 locus). Stickelberger's theorem: the ideal (P(zeta)) is annihilated in the class group by theta, so e_2=0 forces (P(zeta))^2 = (P(zeta^2)) as IDEALS -- a class-group relation counting the locus.
- **mechanism:** The count K(n) = #{e_2=0 dilation orbits} = #{S subset mu_n : P(zeta)^2 = P(zeta^2)}. By A4CyclotomicGaloisAntipodal (in-tree), e_2=0 => sigma(P)=+-P. The ideal equation (P(zeta))^2=(P(zeta^2)) together with Stickelberger's annihilation says P(zeta) generates an ideal whose class is 2-torsion AND theta-annihilated. The number of such period-generators in the cyclotomic unit group modulo the relation is an EXACT class-number / regulator-index quantity: K(n) = (index of cyclotomic units satisfying the antipodal functional equation) = h^+ * (2-torsion factor). #bad = n*K(n).
- **quantity:** K(n)/e2 | **novelty/relevance/feasibility:** 9/6/4
- **exactness case:** Two-sided: K(n) is a COUNT (a cardinality), so any exact algebraic-number-theory expression for it (class-number formula for the antipodal-fixed cyclotomic-unit subgroup) is automatically two-sided -- counts have no slack. The 2-torsion-in-class-group + theta-annihilation conditions are exact ideal-theoretic constraints; their solution count is h^+ times an explicit local factor. Refutes the in-tree claim that K(n) is merely super-linear/bounded -- it would give the EXACT value via the cyclotomic class number h^+(Q(zeta_n)).
- **wall risk:** Medium: this targets K(n)/e_2 (the dilation-orbit count), which the in-tree #400 result found is super-linear (max #orbits 1,3,>=23 for n=8,16,32) -- a class-number-of-Q(zeta_n) flavor IS consistent with that growth (h^+ grows fast). Risk: relating the period-square functional equation to an actual class-group element needs P(zeta) to be (a unit times) a known generator, which holds only when (P) is principal of controlled norm -- the Stickelberger exponent must be checked p-free. Mathlib has no Stickelberger/class-number-formula. But K(n) is char-p-free (over Z[zeta_n]) so it dodges the prize-prime wall entirely.
- **ranker note:** Attacks K(n) via Stickelberger annihilation of the antipodal-fixed cyclotomic-unit subgroup; same char-p-free, count-has-no-slack advantage as L16.2, and the A4CyclotomicGaloisAntipodal sigma(P)=+-P input already exists in-tree. h^+ growth is actually CONSISTENT with the observed super-linear K, which is a point in its favor; downside is no Stickelberger/class-number-formula in Mathlib and the principality check is nontrivial.

## Tier: MID (36)

### [L5.1] Mellin-transform Tannakian monodromy = exact spectral measure of the period operator  — score 21
- **construct:** The Mellin transform sheaf M = j_!δ_{μ_n} *_! L_ψ on the torus 𝔾_m/F_q (Katz 'Convolution and Equidistribution' / Sawin-Katz arXiv:2109.11961). Its geometric monodromy group G_geom ⊂ GL_r is a Tannakian fundamental group; η_b is the Frobenius trace tr(Frob_b | M) and the m periods are Frob-traces over the m cosets.
- **mechanism:** The Mellin/arithmetic-Fourier transform makes the period-multiset literally the eigenvalue multiset of one Frobenius conjugacy class acting on a fixed-rank representation of G_geom. Once G_geom is computed for the homothety-invariant (μ_n-equivariant) family — Rojas-León's regime arXiv:1010.0120 — the moments ∑η_a^k for ALL k are the Tannakian character integrals ∫_{G_geom} χ_std^k dHaar, which are EXACT rational integers (representation multiplicities), not equidistribution limits. The sup max|η_b| is then the operator norm of the std representation, a fixed number determined by the largest weight of G_geom — independent of q once monodromy stabilizes.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/8/4
- **exactness case:** Tannakian moment integrals ∫χ^k are EXACT integers (rep multiplicities), pinning every Newton sum p_k of the period polynomial Ψ from BOTH sides simultaneously (a single integral gives the value, not an inequality). The known data p₁=−1, p₂=q−n, p₃=−n² are exactly these integrals at k=1,2,3; matching them computes G_geom uniquely, and the SUP is then the (two-sided) max eigenvalue modulus of std(G_geom), pinned with no slack.
- **wall risk:** Hits √_A indirectly: if G_geom is the full unitary group (generic Katz outcome), the sup is only equidistribution-controlled (Sato-Tate → √log). EXACT pinning requires G_geom to be SMALL/finite — only happens in the finite-monodromy corner. Moderate wall risk; the prize regime may force large monodromy.
- **ranker note:** Correct modern framing (FFK arithmetic-Fourier/Tannakian): periods ARE Frobenius traces of one conjugacy class on a fixed-rank rep, and moment integrals ∫χ^k dHaar are EXACT integers independent of q (p₃=−n² is literally such an integral) — the only mechanism that escapes the moment-wrap; but exactness needs G_geom small, and Katz's own theory says the generic dyadic family has large/unitary monodromy → Sato-Tate → only √log equidistribution; honest about this, genuinely new construct.

### [L4.4] SIC-POVM / Zauner symmetry and the Heisenberg-Weyl covariance of the period frame  — score 20
- **construct:** The Heisenberg-Weyl group HW(n) acting on C^n; the period frame Φ is HW-covariant (the additive characters are the Weyl translation operators' eigenstructure, μ_n the multiplicative/Clifford part). A SIC-POVM is a single HW-orbit of n² equiangular lines with constant overlap 1/√(n+1). Zauner's conjecture: SICs exist as eigenvectors of the order-3 Zauner unitary (an element of the Clifford group of ORDER 3). The construct: the order-3 Galois/Clifford symmetry that the multiplicative subgroup μ_n carries (Frobenius/the antipodal-and-cube tower).
- **mechanism:** Exploit ORDER-3 directly: the EXACT E_3=15n³−45n²+40n is the SIC/Zauner-relevant third frame moment. For a SIC, the order-3 Zauner symmetry forces the triple-overlap product Σ_{a,b,c}⟨u_a,u_b⟩⟨u_b,u_c⟩⟨u_c,u_a⟩ (the 'triple frame potential', a Bargmann/3-cocycle invariant) to a FIXED value. The Gauss-period analogue Σ_{a,b,c}η_{a-b}η_{b-c}η_{c-a} is a triple-character sum = q·(triple convolution) whose value is EXACTLY computable via the odd-moment data (it is real, and equals q times a closed form because of the negation-closure / antipodal structure). This order-3 invariant pins the PHASE structure of the periods, and combined with the modulus-2 data (E_2), brackets B by separating modulus from phase — the phase being controlled by the order-3 Galois symmetry the way Zauner's unitary controls SIC phases.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 10/7/4
- **exactness case:** Two-sided: the triple frame potential (a degree-3 invariant) is an EXACT real number for the period frame (computable from E_3 and the odd-moment Σηᵢ³=−n²-type identities), and unlike the modulus moments it constrains the PHASES (alignment), which is what the L²→L∞ gap actually hides. A frame with pinned 2nd modulus moment AND pinned 3rd Bargmann invariant has its coherence pinned two-sided iff the order-3 symmetry is transitive on the worst cell — Zauner's order-3 covariance is exactly the transitivity needed. The exactness is inherited from SIC theory where the order-3 symmetry forces equality, not inequality.
- **wall risk:** High but DISTINCT from listed walls: it attacks the PHASE/alignment (Bargmann triple product) not the modulus moment, so it sidesteps the moment no-go (which is modulus-only) and the L²→L∞ √_A wall (which is a modulus issue). New risk: Zauner's conjecture itself is open, so transitivity of the order-3 symmetry on μ_n may fail for n=2^μ (dyadic) — but the DYADIC case has a concrete order-3 Frobenius tower to test, and even partial transitivity gives a sharper-than-Welch bracket. Genuinely unexplored, emphasizes order-3 as requested.
- **ranker note:** Verified: the order-3 Bargmann triple sum T3=-165191 is genuinely NEW data not determined by p1,p2,p3 (depends on eta's autocorrelation on Z/m), so it attacks PHASE/alignment and sidesteps the modulus-only moment no-go and L2->Linf walls; but Zauner transitivity on dyadic mu_n is unverified, Zauner's conjecture is open, and Mathlib has no Heisenberg-Weyl/SIC machinery.

### [L6.5] Stickelberger ideal annihilator pins the e2=0 orbit count K(n) via class-group module structure  — score 20
- **construct:** The Stickelberger ideal St = (theta * Z[G]) cap Z[G] inside the group ring Z[G], G = Gal(Q(zeta_{q-1})/Q) ~ (Z/(q-1))^x, acting on the ideal class group / on the divisor of the Gauss sum; specialized to the index-n quotient G/mu_n ~ Z/m and the cyclotomic units it annihilates.
- **mechanism:** The e2=0 dilation-orbit count K(n) (bad scalars: e1(S)^2 = p2(S)) is, via E2DilationDirectCount, #bad = n*K(n) where the locus is roots of a symmetric-function variety over F_p. Stickelberger's theorem gives the EXACT prime factorization of the Gauss-sum ideal (g) = prod sigma_a^{-1} P^{...}, and the Stickelberger element theta = sum <a/(q-1)> sigma_a^{-1} annihilates the relevant class. The mechanism: the e2-locus is cut out by a relation among the n-th-power-residue characters; the number of F_p-points is a CHARACTER-SUM / Jacobi-sum count whose VALUATION (and hence whose exact value mod the ideal) is pinned by which Jacobi sums J(chi^a,chi^b) lie in the Stickelberger ideal. Stickelberger congruence J(chi^a,chi^b) congruent to binomial(a+b,a)^{-1} mod P pins the count K(n) mod small primes exactly.
- **quantity:** K(n)/e2 | **novelty/relevance/feasibility:** 8/6/5
- **exactness case:** Two-sided modular pin: Stickelberger gives EXACT congruences (Jacobi sum ~ binomial coefficient mod the prime above p), and combined with the trivial archimedean bound |J|=sqrt(q) the CRT over enough small primes reconstructs the integer K(n) exactly (a count is a bounded integer, so finitely many exact congruences determine it). This is the same finite-integer-reconstruction logic as A1 but for the COUNT rather than the sup, and counts are far more amenable to exact congruence pinning than archimedean sups.
- **wall risk:** Probe shows e2(mu_n)=0 trivially for n>2, so K(n) is really about MONOMIAL codewords x^j on mu_n, not mu_n - the Stickelberger ideal acts on the WRONG group unless the monomial structure is re-encoded as a twisted character. The Stickelberger ideal is not in Mathlib. Counts are integers so CRT-exactness is plausible, but mapping the coding-theory locus to a Jacobi-sum count is the unproven bridge. Medium risk; targets the one quantity (a count) where p-adic congruences are genuinely two-sided-exact.
- **ranker note:** Smartest target-switch: aims at the COUNT K(n) (e₂=0 orbit count), not the sup — a bounded integer where Stickelberger congruences + CRT are genuinely two-sided-exact, unlike archimedean sups; but the bridge from the coding/monomial locus to a Jacobi-sum count is unproven, e₂(μ_n)=0 trivially so the real object is monomial codewords needing a twisted-character re-encoding, and Stickelberger ideal is not in Mathlib.

### [L8.2] Cyclotomic-number multiplication matrix: M as spectral radius of an explicit integer matrix  — score 19
- **construct:** The period algebra: eta_a*eta_b = Sum_c (a,b;c) eta_c + n*delta-correction, where the structure constants (a,b;c) are the CYCLOTOMIC NUMBERS of order m (the order-3 incidence data #{x in coset a, x+1 in coset b}). The multiplication-by-eta_1 operator in the integer basis {eta_a} is an explicit m x m INTEGER matrix C (companion-equivalent, verified /tmp/order3_disc.py recovers periods to 1e-14); its eigenvalues are the eta_a.
- **mechanism:** M = spectral radius rho(C) of a concrete integer matrix built from order-3 cyclotomic numbers. Apply matrix spectral-radius theory that uses EXACT entry data: (i) the trace identities tr(C^r)=Sum eta_a^r = m*p_r/... give the same Newton sums; (ii) the symmetrized C^T C has eigenvalues |eta_a|^2, so M^2 = rho(C^T C) = largest eigenvalue of an explicit integer PSD matrix; (iii) Rayleigh quotient v^T(C^T C)v / v^Tv is a LOWER bound for any test vector v and equals M^2 at the dominant eigenvector. The cyclotomic numbers satisfy Gauss's classical order-3/4 closed forms (Lehmer), pinning C exactly for the dyadic family.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/8/5
- **exactness case:** Two-sided: rho(C^TC) is squeezed by Rayleigh (lower, any test vector) and by the integer characteristic polynomial's root bounds (upper). Because C is INTEGER and its char poly is Psi (up to the trivial factor), rho is an algebraic integer of degree <= m; for the smooth dyadic family the cyclotomic numbers have CLOSED FORMS (order-3 Gauss/Lehmer, order-4 via p=a^2+b^2), so C is fully determined and rho computable exactly, not merely bounded.
- **wall risk:** Cyclotomic numbers of order m=(q-1)/n in the prize regime (m~2^128) have NO closed form for large m (only orders <=~24 are classically solved). The closed-form pin works only at small m; large-m is the same equidistribution wall. But the matrix/Rayleigh two-sided structure is wall-free as a bracket.
- **ranker note:** Genuinely new exact construct: M2 = rho(C^T C) for an explicit INTEGER cyclotomic-structure-constant matrix whose char poly IS Psi; verified the spectral object reproduces periods to 1e-14 and the Rayleigh/integer-char-poly bracket is wall-free. Gap: cyclotomic closed forms exist only for order m<=24, and prize m~2^126 has none, so exact entries are unavailable; the lower Rayleigh side needs the unknown dominant eigenvector. Best scaffolding among the 12 because it is a real new object, not a moment restatement.

### [L11.5] Martingale/Azuma on the coset-refinement filtration with exact bracket  — score 19
- **construct:** A Doob martingale M_k = E[η_b | F_k] where F_k is the filtration generated by revealing the digits of y∈μ_n in the 2-adic tower μ_2⊂μ_4⊂...⊂μ_{2^μ}=μ_n (the dyadic subgroup chain). Each level k splits a coset period into two half-periods (the in-tree GaussPeriodTower parallelogram recursion η=A+B, η^χ=A-B). The predictable quadratic variation ⟨M⟩ is the sum of squared coset-splitting increments.
- **mechanism:** Azuma-Hoeffding / Freedman's martingale concentration: P(|M_μ - M_0|>t) ≤ exp(-t²/(2Σc_k²)) where c_k is the increment bound at level k. The KEY is that the EXACT quadratic variation is computable: by the parallelogram law ||η||²+||η^χ||²=2(||A||²+||B||²) (in-tree GaussPeriodTower), the increment at level k is the TWIST (A-B) and Σ_k ||twist_k||² telescopes to the EXACT second moment P_2=q-n via Parseval. So the variance proxy Σc_k² is PINNED to n (not bounded). Azuma then gives the sub-Gaussian tail e^{-t²/2n} for the period, hence max over m frequencies ≤ √(2n log m). The 2-adic dyadic structure is essential — it gives exactly μ=log_2 n levels, each a clean ±twist increment.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/8/6
- **exactness case:** Two-sided because the quadratic variation telescopes to the EXACT Parseval value Σ_k||twist_k||²=n (an equality from the parallelogram recursion, not a bound), so the Azuma variance proxy is exactly n with no slack. The upper tail e^{-t²/2n} gives max≤√(2n log m); the lower bound comes from the Parseval floor (the same telescoped sum forces some twist to be ≥average, so some period saturates √n). The exact telescoping makes the variance proxy two-sided.
- **wall risk:** Azuma needs a uniform per-level INCREMENT bound c_k (||twist_k||_∞), but the twist at the deepest levels is itself a smaller Gauss period whose sup is the SAME open quantity at a smaller subgroup — self-similar recursion (memory note: '2-adic tower self-similar, likely fails'). The variance is exact but the bounded-increment hypothesis is not. Mitigated: Freedman's inequality uses the predictable quadratic variation (exact here) and only a WEAK increment bound (the trivial |twist|≤2^{μ-k} suffices for a crude c_k), possibly enough at fixed constant m where only O(log m) levels matter.
- **ranker note:** Genuinely novel: the parallelogram-telescoping pins the Azuma quadratic variation EXACTLY to the Parseval value n (verified in-tree GaussPeriodTower), isolating the wall to just the per-level increment bound — but the increment IS a smaller Gauss period (self-similar, the documented open recursion), and Azuma yields only a one-sided upper, not the two-sided exact pin claimed.

### [L11.3] Chen-Stein Poisson approximation for the count of large periods  — score 18
- **construct:** The point process N_t = #{a : η_a/√n > t} of periods exceeding a threshold t, as a sum of dependent indicator variables X_a = 1[η_a>t√n] on the index space Z/m with the Galois-orbit dependency graph (each X_a depends on its Frobenius neighbors). Target: a Poisson(λ_t) law with λ_t = m·P(η/√n>t).
- **mechanism:** Chen-Stein method (Arratia-Goldstein-Gordon 1990) bounds the total-variation distance between the law of N_t and Poisson(λ_t) by b_1+b_2 where b_1=Σ_a Σ_{β∈neighborhood} E[X_a]E[X_β] (local mean-square) and b_2=Σ_a Σ_{β≠a in nbhd} E[X_aX_β] (local covariance). The exact second moment P_2=q-n FIXES Σ_a E[X_a²] and the exact joint pair-distribution (mixed period moments, in-tree MixedGaussSumDiagonal) FIXES b_2. The max|η| exceeds t√n iff N_t≥1, i.e. P(max>t√n)=1-e^{-λ_t}+TV-error. Setting λ_t→0 at t=√(2 log m) (where m·e^{-t²/2}=O(1)) PINS the threshold: above it the expected count vanishes (no large periods, exact upper), at it the count is Poisson(Θ(1)) (large periods exist with positive probability, exact lower).
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/8/5
- **exactness case:** Two-sided because Chen-Stein gives the FULL distribution of the max via the Poisson limit, not a tail bound: P(max ≤ t√n) = exp(-λ_t)(1+o(1)) is an EQUALITY up to the explicitly-bounded TV error b_1+b_2. The Gumbel/Poisson edge at t*=√(2 log m) is then pinned from both sides (count→0 above, count→∞ below) provided b_1,b_2→0, which holds because the dependency neighborhood has bounded degree (Frobenius is a single generator).
- **wall risk:** Requires the marginal tail P(η/√n>t) to be sub-Gaussian e^{-t²/2}, which is itself the conjecture for a SINGLE period — circular unless the single-period tail comes from the exact even moments E_r=(2r-1)!!n^r (char-0 Wick, in tree). So this reduces the SUP to the single-period MARGINAL tail, which is the moment-no-go regime. Mitigated: the marginal tail at the FIXED threshold t=√(2 log m) needs only moments up to r≈log m, below the moment-no-go crossover r*~β, so it may be inside the provable window.
- **ranker note:** Chen-Stein on the large-period count is the most honest Stein variant: it correctly localizes the whole sup to the SINGLE-period marginal tail at the FIXED threshold t=sqrt(2 log m), which uses moments only up to r~log m — potentially below the moment-no-go crossover r*~beta; but the marginal still needs the char-0 Wick tail which my probe shows E_r/Wick falls steeply (char-p deflation), so the marginal control re-enters the Deligne wall, and bounded-degree dependency gives order not exactness.

### [L16.6] p-adic Newton polygon of ∏(X−s) mod the prize prime: e₂=0 as a forced edge slope, the char-p face that the no-go misses  — score 18
- **construct:** The p-adic (prize prime) Newton polygon of the vanishing polynomial f_S(X)=∏_{s∈S}(X−s) for S⊂μ_n, where the e_k are its coefficients. Over the prize field F_p (p=n^β), the relevant valuation is the p-adic valuation of the INTEGER lifts of e_k (via the cyclotomic integer ring Z[ζ_n] before reduction mod p). The condition e₂=0 in F_p is v_p(e_2^{lift}) ≥ 1 — a Newton-polygon EDGE-SLOPE condition. The tropical/valuation construct: the e₂=0 locus mod p is exactly where the p-adic Newton polygon of f_S has a forced lattice point on the segment, i.e. a degeneration of the Newton polygon's lower hull.
- **mechanism:** This is the ONE attack that targets the char-p transfer (the true open residual, per the substrate map) through valuation geometry rather than the analytic √. The p-adic Newton polygon's slopes are the p-adic valuations of the roots; e₂≡0 mod p forces a specific slope-multiplicity. Mechanism: #bad scalars = #{S : v_p(e_2^{lift})≥1, v_p(e_1^{lift})=0}, and the dilation u•S shifts the whole Newton polygon by v_p(u) — so the orbit count K(n) over F_p is the count of DISTINCT p-adic Newton-polygon shapes modulo translation. The known char-0 data (E_2,E_3 over Z[ζ_n]) fixes the ARCHIMEDEAN polygon; the gap to the char-p count is exactly the p-adic anomaly Anomaly_p = (char-0 face count) − (char-p face count), which is what BGK/the no-go cannot see because it is a finite p-adic valuation event, not an analytic limit.
- **quantity:** K(n)/e2 | **novelty/relevance/feasibility:** 8/9/5
- **exactness case:** Two-sided because Newton-polygon slopes are EXACT valuations (the polygon is the lower convex hull, an exact combinatorial object); v_p(e_2)≥1 is a sharp on/off condition, so the char-p bad-count is an exact valuation census. The two-sidedness: an S either lies on the e₂=0 locus mod p or not (no analytic slack), and the orbit-quotient #bad=n·K is an exact equality. Pinning the exact K(n) over F_p = counting p-adic Newton polygons of a fixed shape, a finite exact problem for each (n,p).
- **wall risk:** Moderate: this directly attacks the genuine char-p residual (the substrate's stated open core) via valuation geometry, sidestepping the analytic √ and the Bessel/Wick char-0 wrap (Q4). The risk: the count of p-adic Newton polygons of fixed shape may itself be governed by the small-integer-relations-of-2^μ-th-roots-vanishing-mod-p problem = the BGK anomaly, just restated. But the valuation framing exposes E_3 (order-3 vertex) as a NEW constraint on the polygon shape not used by energy methods — a real lever, and the most prize-relevant of the seven.
- **ranker note:** Genuinely novel valuation-geometry framing that attacks the TRUE char-p residual and uses E_3 as a new polygon-shape constraint, and it has real literature traction (Stickelberger/twisted-exp-sum Newton polygons); but p-adic valuation controls divisibility not the complex modulus B, and the e2=0 polygon-census it produces is exactly the K(n) orbit count the substrate (E2DilationDirectCount) records as super-linear / #400-REFUTED, so it reduces to the open combinatorial census rather than pinning B.

### [L4.5] Complex projective 2-design defect = exact variance, with Sidon/MUB matching the design lower order  — score 17
- **construct:** The two-design hierarchy on CP^{n-1}: the period frame restricted to its 2-design behaviour. A 2-design has FP_2 = q²·2/(n(n+1)) exactly. The construct is the projective 2-design space and the in-tree fact that μ_n is a SIDON set mod negation (additiveEnergy = 3n²−3n is the MINIMAL Sidon-with-antipodal energy), which is the additive-combinatorics certificate that the frame is 'as close to a 2-design as a real-structured frame can be'.
- **mechanism:** The Sidon property (E_2=3n²−3n is the exact extremal 'generic' value, NOT the design value n²+...) certifies that the period frame is a tight frame whose 2-design defect is EXACTLY the antipodal excess (E_2 − 2n² = n²−3n, the negation-closure term identified in memory). This antipodal excess is the EXACT obstruction to 2-design-hood, and it is a known closed form. The Welch-with-erasures / second-moment frame theory (arXiv:1801.04548) gives: when the 2-design defect is exactly known, the maximum eigenvalue of the Gram matrix (= largest |η_b|² up to the diagonal) is bracketed by interlacing: λ_max(GramOffDiag) is two-sided-bracketed by the defect-corrected Welch and the trace. Use the EXACT Frobenius norm (E_2) and EXACT trace (Parseval) to interlace λ_max = B² of the centered Gram matrix.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 7/9/7
- **exactness case:** Two-sided via matrix interlacing on the centered Gram matrix M with entries η_{a-b} (a≠b): tr(M)=−n·? known, tr(M²)=Σ_{a≠b}|η_{a-b}|²=q(qn−n²)/? = EXACT from E_2, tr(M³)=EXACT from E_3 (order-3!). With tr, tr(M²), tr(M³) all EXACT, the largest eigenvalue B² satisfies the two-sided Lal-type / power-mean bracket: tr(M³)/tr(M²) ≤ λ_max ≤ √(tr(M²)) AND a matching lower from the 3-moment Hankel. The frame's circulant/group-algebra structure (M is a μ_n-coset-invariant matrix = function of the GROUP, diagonalized by characters) makes the eigenvalues EXACTLY {η_b}, so the moments-of-eigenvalues ARE the frame moments — the bracket is on the actual spectrum, exact.
- **wall risk:** Low-moderate: M is literally diagonalized by the additive characters with eigenvalues exactly η_b (it is a group-circulant), so tr(M^r)=Ση_b^r are EXACT and these ARE the (signed, not modulus) moments — crucially the ODD moments Ση_b^{2k+1}=−n^{2k} are exact, giving SIGNED spectral data the modulus-only no-go cannot see. The 3-moment Hankel bracket on λ_max is genuinely two-sided. Wall risk: power-mean λ_max≤√tr(M²) is the same √_A; but the lower companion from signed odd moments + interlacing is new and the bracket tightness improves with each exact rung.
- **ranker note:** Genuinely exploits SIGNED odd-moment spectral data (tr(M^r)=p_r exact, M a group-circulant with eigenvalues exactly eta_b) which the modulus no-go cannot see, and is the most Lean-feasible (elementary, exact data in-tree); but verified the 3-moment bracket on lambda_max is O(sqrt q) garbage (p6^(1/6)=11.7 vs target 10.0, sqrt(p2)=64) and only reaches B at r~23 = moment no-go.

### [L8.1] Period-polynomial Newton power-mean squeeze (the exact m-point spectrum)  — score 17
- **construct:** The period polynomial Psi(X)=prod_{a=0}^{m-1}(X-eta_a) in Z[X], deg m = (q-1)/n, whose roots are EXACTLY the m distinct Gaussian periods = the distinct eigenvalues of Cay(F_q,mu_n) (each with multiplicity n). Verified in /tmp/period_poly.py: integer coeffs, p1=Sum eta_a=-1, p2=Sum eta_a^2=q-n, p3 from E_3, all EXACT.
- **mechanism:** M = max_a|eta_a| is the largest-modulus root of a degree-m integer polynomial whose low Newton sums are KNOWN exactly. Set s_r = Sum_a |eta_a|^{2r} = E_r(mu_n)/n (the in-tree moments, /n by coset reduction). Power-mean identity gives the genuinely TWO-SIDED bracket (s_r/m)^{1/r} <= M^2 <= s_r^{1/r} for every r; verified /tmp/root_powersum.py converging from BOTH sides to M^2. r=1 is exactly the Parseval floor (q-n)/m <= M^2 <= q-n; r=2,3 use E_2=3n^2-3n, E_3=15n^3-45n^2+40n. Pin M^2 = inf_r s_r^{1/r} = sup_r (s_r/m)^{1/r}: as r->infinity both meet at the dominant root.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/9/6
- **exactness case:** Two-sided by construction: (s_r/m)^{1/r} is a strict LOWER bound (sum >= max-term, so s_r >= M^{2r}; and s_r <= m*M^{2r} gives the upper) and s_r^{1/r} a strict UPPER bound, both monotone to M^2. Unlike the moment-NO-GO (which only ever gives a one-sided (qE_r)^{1/2r}>=n LOWER on the FULL frequency sum), here the /m coset reduction makes the UPPER side (s_r/m)^{1/r} a LOWER bound on M^2 itself, and the two brackets pinch. If the exact E_r have a closed form for all r (order-3 recursion below), the limit is computable in closed form.
- **wall risk:** Hits the MOMENT NO-GO half: as r grows the bracket needs E_r for r~log q, and E_r^{char0}=Bessel wraps at r*~beta into the char-p anomaly. But the /m coset-reduced LOWER side (s_r/m)^{1/r} is NEW data the no-go does not address, and the bracket is finite-r usable; the wall is only at the asymptotic limit, not the bracket itself.
- **ranker note:** Cleanest genuinely TWO-SIDED structure: (s_r/m)^{1/r} <= M2 <= s_r^{1/r} with E_2,E_3 exact, and the /m coset-reduction does make the upper-side quantity a real lower bound on M2 (new vs the one-sided full-sum no-go). Verified the brackets pinch to M2 as r grows. Gap: convergence is slow and the lower side is weak (ratio 0.06-0.3, WORSENING with n); pinning at prize scale needs r~log q which wraps into the Bessel/char-p anomaly wall (W3). Usable as a finite-r bracket, not an exact pin.

### [L13.2] Cyclic Sieving Phenomenon pins K(n) = e₂=0 dilation-orbit count exactly via q-binomial at a root of unity  — score 17
- **construct:** The cyclic group C_n=⟨rotation⟩ acting on (k+1)-subsets of μ_n≅Z/n, with the CSP triple (X = subsets, C_n, X(q)=Gaussian q-binomial [n choose k+1]_q). The number of subsets fixed by rotation-by-d equals X(ζ^d) where ζ=e^{2πi/n} (Reiner-Stanton-White CSP for subsets).
- **mechanism:** K(n)=#{e₂=0 dilation orbits} and the bad-set count #bad=n·K are EXACTLY the orbit counts of the μ_n-dilation action on the e₂=0 (equivalently e₁²=p₂, i.e. h_2-vanishing) locus of (k+1)-subsets. Because e₂ and the bad-scalar α=−1/e₁ are EQUIVARIANT under dilation (proven in-tree: e2_smul, badScalar_smul, E2DilationDirectCount), the locus is a union of free C_n-orbits, so #bad = n·(#orbits) with #orbits computable by Burnside: #orbits = (1/n)Σ_{d|n} φ(n/d)·X_{e₂=0}(ζ^d). The CSP evaluates each fixed-point count X(ζ^d) as a q-binomial at a root of unity — a CLOSED integer, giving K(n) exactly.
- **quantity:** K(n)/e2 | **novelty/relevance/feasibility:** 9/8/6
- **exactness case:** Two-sided/exact because Burnside + CSP gives an EXACT integer formula (a finite sum of φ-weighted q-binomial evaluations at roots of unity), not an inequality. The fixed-point sets under rotation are themselves coset-unions (FactorizationRigidity: μ_m-coset-union ⟺ m-sparse factorization), so each X_{e₂=0}(ζ^d) is the count of d-periodic e₂=0 subsets — a sub-problem of dimension n/d that recurses, terminating at the 2-adic tower base. The numerics already show #vanish = exact multiples of orbit sizes (8=n, 16=n, 6), the CSP signature.
- **wall risk:** CSP requires the e₂=0 locus to carry a CLEAN cyclic-sieving polynomial; the generic q-binomial CSP is for ALL subsets, and restricting to e₂=0 (an algebraic condition) may break the polynomial-at-root-of-unity miracle. Risk that K(n) is super-linear (memory: #orbits 1,3,≥23 for n=8,16,32, REFUTED O(n)) — but exactness (not O(n)) is the goal here, and Burnside still gives the EXACT super-linear value. Avoids the L²→L∞ √-wall entirely (counting, not analytic).
- **ranker note:** Genuinely new framing (period polynomial as fixed integer-symmetric reciprocal, B = max-root-modulus = an exact algebraic 'house'); the FULL coefficient vector WOULD pin B exactly, and the odd-moment leverage is fresh — but I verified low-order p_1=-1,p_2=p-n,p_3=-n^2 are near-constant while B floats 4.5->5.5, so the unknown tail e_4..e_m IS the cumulant/moment-no-go wall; valid reduction, not a closure.

### [L16.4] Amoeba & Ronkin function of the Laurent polynomial P: the spine-distance lower bound meeting the tentacle upper bound  — score 17
- **construct:** The amoeba A(P) ⊂ R of the Laurent polynomial P(b)=Σ_{χ≠1}χ̄(b)γ_χ, where χ ranges over Ĥ≅Z/m, so P is a univariate Laurent polynomial on Z/m with UNIMODULAR coefficients γ_χ (Gauss phases, |γ_χ|=1 exact). Its amoeba is the image of {|P|} under Log. The Ronkin function N_P(x)=∫log|P| (mean of log|P| over the torus) and its Legendre dual (the Passare–Rullgård spine) organize where |P| is large. Because all |γ_χ|=1, P is a TRIGONOMETRIC polynomial with flat coefficient amoeba — its Ronkin function is governed purely by the PHASES (the Gauss-sum arguments arg γ_χ = arg G(χ)).
- **mechanism:** Amoeba theory pins max|P| two-sidedly: the UPPER side via the order map / Ronkin function (the amoeba's complement components are indexed by Newton-polytope lattice points; max|P| ≤ value at the extreme tentacle = sum of |coeffs| = m−1 trivially, but the Ronkin mean refines this), and the LOWER side via the amoeba's SPINE (the tropical curve approximating A(P)): max|P| ≥ exp(distance from spine vertex). The exact lever: Parseval Σ|P|²=m(m−1) IS the second Ronkin moment ∫|P|²=m−1, and the Gauss-phase unimodularity makes the amoeba a MONOMIAL-AMOEBA perturbation whose spine is computable from arg G(χ). Sandwiching the Ronkin-mean (=0, since constant coefficient 1 in η normalization) against the L² value pins the L∞ tentacle length.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 10/7/3
- **exactness case:** Two-sided in principle: amoeba theory gives BOTH the upper bound (Ronkin/order-map, since |P|≤ value at amoeba boundary) AND a matching lower bound (the spine distance is a genuine lower bound on log max|P| because the amoeba contains its spine, Passare–Rullgård). For a flat (unimodular-coefficient) trigonometric polynomial the gap between spine and amoeba boundary is controlled by the coefficient phases, which are the EXACTLY-known Gauss-sum arguments — so the sandwich can close to the exact value if the phase distribution (Deligne equidistribution) is sharp at the extremal b.
- **wall risk:** High: amoebae of univariate polynomials are thin (just points/intervals), so the spine-vs-boundary sandwich is weakest in 1 variable; the real content (Gauss-phase equidistribution) is the Deligne/Weil √q wall in tropical clothing. Novelty is maximal (amoeba/Ronkin never tried here). Best deployed by promoting P to a MULTIVARIATE Laurent polynomial via the period-polynomial recursion (each prime factor of m a separate variable), where amoebae are genuinely 2D and the sandwich has room.
- **ranker note:** Amoeba/Ronkin spine-vs-tentacle sandwich is maximally novel (never tried here) and genuinely two-sided in principle, with Parseval = exact 2nd Ronkin moment as a real anchor. But univariate amoebae are thin so the 1D sandwich is weak, and the content reduces to Gauss-phase (Deligne) equidistribution; the multivariate promotion via prime factors of m is the only escape and is speculative.

### [L3.6] Frobenius-Schur / theta-dichotomy: order-3 trilinear Weil invariant pins p3=-n^2 and forces the cubic period relation  — score 16
- **construct:** The trilinear/cubic invariant of the Weil representation: the triple-product (theta) integral Sum_{a+b+c=0} gamma_a gamma_b gamma_c, which by the Jacobi cocycle telescopes to Sum J(a,b)*(unimodular)/q. This is the order-3 (cubic) structure the prompt emphasizes. It is the rep-theoretic origin of the EXACT odd-moment p3=-n^2 (verified p3=-64=-8^2) and the exact E3=15n^3-45n^2+40n.
- **mechanism:** The cubic Newton power sum p3=Sum eta_i^3 of the period polynomial is the trace of the triple-convolution, = Sum_{x,y,z in mu_n, x+y+z in mu_n-orbit} ... ; rep-theoretically it is the dimension of the SL_2-invariants in W tensor W tensor W (the theta-trilinear form). Lam-Leung at prime 2 forces the only cubic vanishing relations to be antipodal (proven, DyadicOddMomentVanishing), so p3 is EXACTLY -n^2 with NO field-dependence. Combined with p1=-1 and p2=q-n, Newton's identities give the elementary symmetric e1=-1, e2, e3 of Psi EXACTLY in closed form, pinning the cubic resolvent / the order-3 Hankel determinant whose vanishing locates the extreme root.
- **quantity:** multiple | **novelty/relevance/feasibility:** 7/9/7
- **exactness case:** Two-sided via the order-3 Hankel/Newton bracket: knowing p1,p2,p3 EXACTLY (all field-independent except p2=q-n linear in q) fixes the first 3x3 Hankel matrix H_1=[[m,p1,p2],[p1,p2,p3],[p2,p3,p4]] of the root measure. Hankel positivity (the measure is real, roots real — VERIFIED) forces det H_1>=0, giving an EXACT upper bracket on the root spread; the principal-representation lower bracket from the same moments gives the matching lower side. The cubic structure (3 exact moments) is the MINIMAL nontrivial two-sided squeeze, and because p1,p3 are field-INDEPENDENT the only q-dependence enters through p2=q-n and p4, isolating the q-scaling of B exactly.
- **wall risk:** The 3-moment Hankel bracket alone gives spread ~sqrt(p2)=sqrt(q-n) which is the TRIVIAL sqrt(q) scale, not the target sqrt(n log(q/n)) — need p4,p6 (have them) AND the equal-multiplicity rigidity (each root mult exactly n) to beat sqrt q. Genuinely feasible in Lean (Newton's identities + Hankel positivity are elementary; the exact p1,p2,p3 are already in-tree), and EXPLOITS exactly the order-3 known data the prompt highlights; the wall is whether 6 moments suffice to reach the log-scale.
- **ranker note:** Cleanest use of the order-3 exact data (p1,p2,p3 field-independent, in-tree, prize-regime-confirmed for n=16 at q>n^3) and most Lean-feasible (Newton + Hankel positivity elementary); but verified the 3-moment Hankel/Gauss nodes are strictly INTERIOR to the support (max node 4.56 vs true B 7.56), giving NO upper bound -- determinacy needs ~2m-1 (thousands) moments = moment no-go.

### [L5.3] Hurwitz/Jacobi-sum order-3 exactness: pin p₃=−n² as a Fermat-cubic Frobenius eigenvalue and propagate  — score 16
- **construct:** The modified Fermat curve C₃: x^d+y^d+z^d=0 over F_p (Garcia-Lorenz-Todd, Ramanujan J 67 (2025)), with Hurwitz's exact identity N(d,p)=((p−1)³+(p−1)d²∑ηᵢ³)/p. The Jacobian Jac(C₃) is isogenous to a product of CM elliptic curves / abelian varieties whose Frobenius eigenvalues are Jacobi sums J(χ^a,χ^b) of exact modulus √p.
- **mechanism:** The order-3 power sum ∑ηᵢ³ = −n² is the trace of Frob on H¹(C₃), which decomposes into Jacobi-sum characters of EXACT modulus √p. Verified numerically: ∑ηᵢ³ = −n² is q-INDEPENDENT and exact (n=8 ⟹ −64 for all primes p≡1 mod 8). This pins the third Newton sum of the period polynomial Ψ with zero slack via an étale eigenvalue computation. Bootstrapping: with p₁=−1, p₂=q−n (Parseval), p₃=−n² all étale-exact, plus the odd-moment stabilization p₅→−2^{2μ−... } (Frobenius on higher Fermat curves), the largest root of Ψ is bracketed by Newton-sum power-mean inequalities that become two-sided as more odd p_k are pinned.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 7/9/7
- **exactness case:** p₃=−n² is EXACTLY a Frobenius trace on H¹(Fermat cubic) — an étale cohomology class — hence an exact integer, two-sided. ODD Newton sums are q-independent and étale-pinned (Jacobi sums of modulus √p that conspire to cancel the q-dependence); only EVEN p_{2k} carry q. This decouples the spectrum: the sup is determined by the exactly-known odd moments plus the controlled even Fermat-curve counts, giving a two-sided bracket as the odd ladder is filled.
- **wall risk:** Partial moment-no-go exposure: pinning finitely many Newton sums gives a power-mean bracket whose width is the genus/Hasse-Weil slack of the higher Fermat curves (genus ~ k²), which grows. But the q-INDEPENDENCE of odd p_k is a genuinely new exact handle not yet exploited; the even-moment Fermat counts are the only remaining slack and are explicitly Hasse-Weil-bounded.
- **ranker note:** Real new handle (p₃=−n² is an exact Frobenius trace on H¹(Fermat cubic), the ORDER-3 datum the prompt emphasizes), but I verified the 'odd moments are q-independent' claim WRAPS: T_{2k+1}=−n^{2k+1} holds only to depth r*~β then fails (n=16: wraps at T₁₁), so the bracket from finitely many pinned odd p_k is exactly the moment-no-go power-mean (T₄^{1/4}=6.25 vs B=5.46) that does not close.

### [L7.5] Order-3 cumulant tensor / depth-3 dyadic descent recursion for the connected mass  — score 16
- **construct:** The third connected cumulant C_3 = sum_{b!=0} eta_b^3 = q*T3 - n^3 (the order-3 analogue of CumulantGaussPeriodBound.cumulant_eq), and the dyadic per-level descent N0(mu_{n}, 3) for the order-3 zero-sum tuple count, building on the proven CumulantDyadicDescent.N0 recursion at the SPECIFIC depth r=3 (order-3, where the cross terms have a known triangular structure unlike general r).
- **mechanism:** The existing CumulantDyadicDescent proves N0(mu_n, 2r) >= 2 N0(mu_{n/2},2r) and exactness N0_dyadic_two_le at the SECOND moment (r=1, R=2, cross terms vanish). At ORDER 3 (the cubed/triple count, N0 at the 3-tuple level) the cross terms between the two cosets H and zeta*H are governed by the trilinear T3 = n*#{a+b=-1}, which I showed is ZERO in the prize regime. So the order-3 descent is EXACT (not just >=): the connected order-3 mass splits cleanly across the 2-adic tower with no residual, unlike order 2 (E_2=3n^2-3n has an antipodal excess) or order >=4. This gives an EXACT recursion C_3(mu_n) = 2 C_3(mu_{n/2}) + (computable cross term =0), solving to C_3(mu_{2^k}) = -n^3 exactly, which feeds the per-frequency floor eta_b^3 <= C_3 with no slack.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 7/6/7
- **exactness case:** Two-sided at order 3 specifically because the cross-term vanishing (T3=0) makes the descent an EQUALITY, not an inequality. Order 2 gives only a lower floor (N0_dyadic_two_le is <=) and order >=4 has nonzero cross terms; order 3 is the unique depth where the dyadic split is exact in the prize regime. This pins C_3 exactly, and combined with the cube being a SIGNED quantity (so it constrains the period configuration two-sidedly, not just bounding magnitude) it isolates the extreme period via the side-law L7-A2.
- **wall risk:** Low immediate wall: order-3 exactness is real and the N0 descent is already formalized for general r in CumulantDyadicDescent. Risk: C_3 is a SIGNED cube sum, so it bounds the cube of the extreme period only after the side-law fixes the sign; the magnitude pin still needs an EVEN moment (order-2 or order-4). So this is a strong structural input feeding L7-A1/A2 rather than a standalone magnitude pin. Hits W2 if used alone for magnitude.
- **ranker note:** Strongest exploitation of the brief's ORDER-3 emphasis: I VERIFIED T3=0, C_3=-n^3 exactly, and the 2-adic descent cross-term genuinely VANISHES (0 = 2*0 + 0) at order 3 specifically, so the descent is an EQUALITY not an inequality and is already formalized for general r. Real structural input. Gap: C_3 is a SIGNED odd-cube sum (= the known odd-moment rigidity Sum eta^(2k+1) = -n^(2k)); it constrains sign/configuration but pins NO magnitude on its own, so it feeds a magnitude attack rather than being one (hits W2 if used alone).

### [L8.6] Three-eigenvalue / strongly-regular rigidity: the order-3 closed system pins M when m forces 3 distinct periods  — score 16
- **construct:** arXiv:2604.06513 classifies GP-digraphs with exactly THREE distinct eigenvalues = oriented Paley graphs (and unions). When the period polynomial Psi factors with few distinct roots (e.g. m=3, the cubic period polynomial; or the multiplier group orbits collapse the m periods to 3 Galois-conjugate values), Cay(F_q,mu_n) becomes a strongly-regular-like graph whose spectrum is determined by the order-3 cyclotomic system alone.
- **mechanism:** For m=3 (q=3n+1) the periods eta_0,eta_1,eta_2 are the three roots of the CUBIC period polynomial X^3+X^2-((q-1)/3)X - c, whose coefficients are the EXACT order-3 cyclotomic numbers (Gauss: 4q=L^2+27M^2 pins them). Then M=max|root| is given in CLOSED FORM by the cubic formula in terms of (L,M) from 4p=L^2+27M^2 (Gauss's classical cubic-period evaluation). More generally, the order-3 structure constants (cyclotomic numbers) close the system: the symmetric functions p_1=-1, p_2=q-n, p_3 (from E_3) determine ALL of Psi when m<=3, giving M exactly. EXPLOIT: the in-tree EXACT E_3=15n^3-45n^2+40n is precisely the p_3 datum that closes the cubic.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 7/6/7
- **exactness case:** Genuinely exact (not a bound) in the closed regime: for m<=3 the three known Newton sums p_1,p_2,p_3 (all in-tree EXACT) over-determine a cubic, so Psi is fully pinned and M is the explicit cubic-formula root in terms of the Gauss decomposition 4q=L^2+27M^2 — a two-sided EQUALITY, the prototype of an exact period evaluation. This is the order-3 emphasis the prompt requests: E_3 is exactly the third Newton sum that closes the m=3 system.
- **wall risk:** Exact closure only when m is small (m<=3 needs q=3n+1, NOT the prize m~2^128 regime). At prize index m is enormous so 3 Newton sums are far from determining a degree-m Psi; the order-3 closure is a small-index/provable-regime pin, structurally analogous to the known small-subgroup delta* pins, not prize-closing. But it gives a verified EXACT-value template and isolates E_3's role precisely.
- **ranker note:** Genuinely exact closed form for m=3 (verified 3 distinct periods = roots of order-3 cubic via 4q=L^2+27M^2), a real two-sided EQUALITY template that pins E_3's role — but q=3n+1 is the provable small-index corner, structurally like known small-subgroup delta* pins, not the prize m~2^128 regime.

### [L14.1] Gauss-period Verlinde fusion ring: B as second S-matrix eigenvalue, pinned by the order-3 higher Gauss sum  — score 16
- **construct:** The commutative associative C-algebra A = C[F_p^*/μ_n] of the m=(p-1)/n Gauss periods η_0..η_{m-1}, with multiplication table given by Gauss's cyclotomic numbers (i,j)_m (a nonnegative integer fusion matrix N). This is literally a Verlinde-style fusion ring; its regular-representation eigenvalues are exactly the m periods (verified). Frobenius-Perron eigenvalue = n (the 'quantum dimension', verified ≈n exactly), and B = the SECOND-largest |eigenvalue| = spectral gap below the FP/identity object.
- **mechanism:** B is no longer an opaque sup over characters: it is the subdominant eigenvalue of an explicit nonnegative integer matrix N whose row/column sums are q-controlled and whose Newton power sums tr(N^k) ARE the period power sums p_k = Σ η_i^k. I pin p_1=-1, p_2=p-n, p_3=-n^2 (all verified exact, generic prize regime), giving the first three coefficients of the degree-m characteristic polynomial of N exactly. Bounding the second eigenvalue of a matrix with known FP eigenvalue n, known trace -1, and known tr(N^2),tr(N^3) is a finite spectral-localization problem (Perron deflation: factor out the dimension eigenvalue n, then localize the remaining (m-1) roots whose elementary symmetric functions are now exactly determined through degree 3).
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/8/5
- **exactness case:** Two-sided: the characteristic polynomial of N is monic with integer coefficients fixed through degree 3 by the exact power sums (Newton's identities), and B is its second-largest root modulus. The FP eigenvalue n is exactly deflated out (it is the all-ones/identity object). After deflation the remaining symmetric functions e_1,e_2,e_3 of the (m-1) genuine periods are exact rationals, so any extremal root is squeezed between explicit Newton/Maclaurin two-sided bounds — not a one-sided cancellation estimate but an algebraic root-location of a polynomial with known low-order data.
- **wall risk:** Pinning the SECOND eigenvalue from finitely many power sums alone is generally underdetermined for large m (the wall reappears as the L²→L∞ gap once you need high-order tr(N^k)). Mitigated only if the deflated spectrum is forced near-circular (Galois transitivity), which is the genuine content to prove.
- **ranker note:** Strongest new CONSTRUCT: the cyclotomic-number fusion ring is literally in-tree (LadderSpectrumFusion), periods = regular-rep eigenvalues, FP eigenvalue = n exactly deflatable, char poly fixed through deg 3 by exact p_1,p_2,p_3 — a clean spectral-localization restatement; but pinning the SECOND eigenvalue from 3 traces of an m-dim matrix is exactly underdetermined (the L2->Linf gap), so it relocates rather than closes; honest mid.

### [L14.2] Higher-Gauss-sum order-3 closed form Ση³=−n² as an exact Anderson-Moore-Vafa anomaly relation  — score 16
- **construct:** Ng-Schopieray-Wang 'higher Gauss sums of modular categories' (arXiv 1812.11234): the n-th Gauss sum τ_n = Σ_a θ_a^n d_a² and n-th central charge. Here the periods play the role of the 'simple-object' data; the order-3 higher Gauss sum is the cubic power sum Σ η_i³. NUMERICS show Ση_i³ = −n² EXACTLY for all prize-regime p (n=8,16 verified), the lone exception being the Fermat-prime corner p=257 where m=n triggers an MDS coincidence (+515). The Anderson-Moore-Vafa theorem says such Gauss-sum ratios are roots of unity / rational — here it becomes the EXACT integer −n².
- **mechanism:** Convert the cubic identity Ση_i³=−n² into a hard quantitative constraint on B. Since Ση_i³ = Σ_{cosets} η_i³ and one period equals B in modulus, the cubic sum being exactly −n² (an O(n²) integer) FORCES near-total cancellation of the m≫n cubes |η_i|³, which both caps a single dominant period (if one |η_i|³ ≈ B³ were ≫ n² without compensators it would violate the exact total) and ties B to the order-3 structure the prompt emphasizes. Combine with p_2=p-n: by power-mean / Newton on the triple (p_1,p_2,p_3)=(−1, p−n, −n²) the maximum modulus root of the period polynomial is bracketed two-sided.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/9/6
- **exactness case:** The order-3 sum is an EXACT integer (−n²) provable as a cyclotomic-number / triple-incidence count (number of additive triples 0 = x+y+z with x,y,z in cosets), not an estimate. With p_1,p_2,p_3 all exact, the constraint set {Σx_i=−1, Σx_i²=p−n, Σx_i³=−n²} on the m period values is a closed semialgebraic system; its max-|x_i| has both a sharp lower bound (achieved when mass concentrates) and a sharp upper bound (forced by the cubic compensation), giving genuine two-sided control rather than a Johnson-type one-sided bound.
- **wall risk:** Three power sums constrain the empirical distribution of the η_i but the max can still float within a window unless a fourth-order (E_2-type p-dependent) datum is added; the gap between the cubic-anomaly window and the conjectured √(2n ln(q/n)) is the residual. The Fermat corner shows the identity is not literally universal — must carve out m=n.
- **ranker note:** Best ORDER-3 exploitation and I VERIFIED p_3=Sum eta^3=-n^2 holds exactly across n=8,16 and even at the Fermat prime p=257 (contradicting its own risk note); the cubic-compensation two-sided bracket from {p_1,p_2,p_3} is real and novel via higher-Gauss-sum/Anderson-Moore-Vafa lens, but 3 power sums leave B in a window (my probe: same p_2,p_3, B moves 4.5->5.5), so it is a bracket not an exact value.

### [L15.3] Log-unit lattice regulator + covering radius: AM-GM gap pins sup from the exact norm  — score 16
- **construct:** The logarithmic Minkowski embedding L: O_K^* -> R^{m} (or R^{r_1+r_2}) sending eta to (log|eta_b|)_b. The cyclotomic units of K form a finite-index sublattice with EXACTLY known regulator R_K (= covolume, via the cyclotomic-unit class-number formula h^+ R_K = explicit). The period eta lies on the affine hyperplane Sigma log|eta_b| = log|N(eta)| (the EXACT log-norm, Habegger). The covering radius / first-minimum of this lattice (Doroz-... 'covering radius of the logarithmic lattice of Q(zeta_n)', arXiv:2507.20544) bounds the deviation of (log|eta_b|) from its centroid.
- **mechanism:** max_b log|eta_b| = (log-norm)/m + (deviation of L(eta) from the all-equal centroid in coordinate b). The all-equal centroid is the AM-GM point (sup=geomean). The deviation lives in the trace-zero subspace = the log-unit lattice tensored R; the cyclotomic-unit regulator/covering-radius EXACTLY bounds the L^infty of the centered log-vector (covering radius rho gives max deviation <= rho; first minimum lambda_1 gives a matching LOWER deviation, since eta is NOT a unit-multiple of a totally-balanced element). Two-sided: lower from lambda_1 (Alon-Boppana/Parseval floor already proves geomean < sup), upper from covering radius rho_K.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/8/3
- **exactness case:** Two-sided because the regulator R_K (covolume) and class number h_K^+ are EXACTLY computable for the cyclotomic period subfield (cyclotomic-unit index theorem), and covering radius + first minimum give matching upper/lower envelopes for the SAME centered log-vector. The exact log-norm (Habegger) fixes the centroid; the lattice geometry fixes the spread. Unlike Cauchy-Schwarz (one L^2 number -> L^infty bound, wall W_A), this uses m-1 independent lattice constraints (the full regulator) so it is not a single-sqrt loss.
- **wall risk:** High: covering-radius bounds for Q(zeta_n) are themselves only known up to O(.) constants, and eta is a period not a generic unit (it sits at a special lattice point), so 'where on the fundamental domain' is the open question -- this is morally the same difficulty as the Paley conjecture, repackaged geometrically. Genuinely NOVEL framing though: turns sup|eta_b| into a closest/farthest-vector problem in an EXACTLY-known lattice. Avoids moment no-go (uses geometry not moments).
- **ranker note:** Log-unit regulator + covering-radius/first-minimum is a genuinely new geometric framing (closest/farthest-vector in an EXACTLY-known cyclotomic-unit lattice) that uses m-1 independent constraints rather than a single L2->Linf sqrt, so it structurally sidesteps the moment no-go. But covering radii of Q(zeta_n) are only known up to O(.) constants and 'where the period sits in the fundamental domain' is morally the Paley conjecture repackaged.

### [L1.6] BBP phase-transition spike: B as a low-rank deformation outlier of the Gaussian bulk  — score 15
- **construct:** Decompose A = A_bulk + Δ where A_bulk is the Gaussian-moment-matching part (the (2r-1)!!n^r / Bessel baseline, a soft-edge Gaussian bulk) and Δ is the char-p ANOMALY part (the genuine vanishing-sum/MDS-failure deviation, e.g. the antipodal-excess and the order-3 excess E_3-5n^3=10n^3-45n^2+40n). Treat Δ as a structured low-rank/sparse deformation and apply the Baik-Ben Arous-Péché (BBP) transition: an outlier eigenvalue detaches from the bulk edge iff the deformation strength exceeds a critical threshold.
- **mechanism:** BBP gives the EXACT outlier location: if the bulk edge is E_bulk=√(2n ln m) and the deformation has effective strength σ (read from the anomaly cumulants κ_4^{anom}, κ_6^{anom} = the proven excess terms -3n, -45n^2,+40n in E_2,E_3), then the top eigenvalue is either pinned AT the bulk edge (subcritical, B=√(2n ln m)) or detaches to E_bulk·f(σ) with f the explicit BBP function. The known prize-regime data (B/floor∈[0.8,0.98], <1, no detachment) says we are SUBCRITICAL ⇒ B = bulk edge exactly. The order-3 excess 10n^3 is the leading anomaly cumulant; computing its scaling vs the BBP critical threshold (∝ bulk-edge-curvature) DECIDES sub- vs super-critical and thus whether B sticks to √(2n ln m) or spikes.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/6/3
- **exactness case:** The BBP transition is a sharp two-sided dichotomy: below threshold the top eigenvalue CONVERGES to the bulk edge (B=√(2n ln m) exactly, equality in the limit), above it converges to an explicit outlier value. Either branch pins B to a definite value, not a one-sided bound — the exactness is the content of the BBP theorem (a.s. convergence of the extreme eigenvalue).
- **wall risk:** The deformation Δ (char-p anomaly) is NOT obviously low-rank or independent of the bulk — it is the same deterministic matrix, so the BBP independence/rank hypotheses must be replaced by a deterministic-perturbation (Weyl/Bauer-Fike) argument, which reintroduces the L^2→L^∞ sqrt. The 'strength' of Δ at depth r≈log p is the deep-moment wall. But genuinely new: it reframes the OPEN question as a sub/supercritical BBP threshold computation driven by the EXACT order-3 excess 10n^3 — a concrete, finite, char-p combinatorial quantity, and the data already says subcritical.
- **ranker note:** Freshest RMT framing: reframes the open question as a sub/supercritical BBP threshold driven by the EXACT order-3 excess 10n^3 (concrete finite char-p quantity, data says subcritical) — but deterministic Delta is not independent low-rank, so BBP hypotheses fail to deterministic Weyl/Bauer-Fike = the L2->Linf sqrt, and the subcritical branch only re-derives the i.i.d.-Gumbel floor (in-tree #37/#10 verdict: bulk-spectral, blind to the structured-prime spike).

### [L4.1] Gauss-period frame potential as higher-order Welch bound with k-design equality  — score 15
- **construct:** The complex projective frame Φ = {u_b = n^{-1/2}(e_p(bx))_{x∈μ_n} : b∈F_q}: q unit vectors in C^n. Its Gram matrix has off-diagonal entries ⟨u_a,u_b⟩ = η_{a-b}/n. The r-th order frame potential FP_r(Φ) = Σ_{a,b}|⟨u_a,u_b⟩|^{2r} = q²E_r(μ_n)/n^{2r} is EXACTLY the in-tree moment ladder ∑_b‖η_b‖^{2r}=q·E_r renormalized. The space is the projective space CP^{n-1} with the design measure.
- **mechanism:** Welch's higher-order bound (arXiv:2602.13099, Mixon): FP_r ≥ q²/C(n+r-1,r) with EQUALITY iff Φ is a complex projective r-design. I verified the exact defect: E_r(μ_n)/(n^{2r}/C(n+r-1,r)) → C(2r,r)/2^r (=1, 3/2, 5/2, 35/8, ... matching (2r−1)!!/r!), confirmed numerically for r=2,3 against the known E_2=3n²−3n, E_3=15n³−45n²+40n. So the frame's r-design defect is the EXACT rational C(2r,r)/2^r. Reverse-Welch (Levenshtein/Sidelnikov upper companion) on a frame with controlled design-defect pins max coherence: max_{a≠b}|⟨u_a,u_b⟩|² = B²/n is squeezed between the design floor and the defect-corrected ceiling.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/8/6
- **exactness case:** Two-sided because k-design theory provides BOTH directions: Welch gives FP_r ≥ floor (lower), and the design-defect C(2r,r)/2^r being EXACTLY computable (proven via E_2,E_3 and the Wick char-0 value) turns the inequality into the equality FP_r = floor·C(2r,r)/2^r. Combined with a Levenshtein-type reverse bound (sup coherence ≥ a polynomial in the FP_r), the worst overlap is pinned, not merely bounded above. The exactness comes from the frame being a 'tight frame with known design defect' rather than an unstructured frame.
- **wall risk:** Moderate: the reverse-Welch (coherence from moments) step still requires the moment ladder up to r≈ln q, hitting the MOMENT NO-GO wall (q·E_r)^{1/2r}≥n for the SUP. BUT the k-design framing dodges it partially: the defect being EXACTLY C(2r,r)/2^r (not just ≤) is new exact data the no-go does not contradict — the no-go limits sup-from-average but the design-defect equality is sup-INDEPENDENT structural info. The wall bites only if one tries to convert defect→sup naively.
- **ranker note:** Verified the exact r-design defect C(2r,r)/2^r is real sup-INDEPENDENT structural data (matches E2,E3 and Wick), a legitimately new lens; but the reverse-Welch defect->coherence conversion still needs the moment ladder to r~ln q (confirmed: moment upper bounds reach B only near r~23), so the design framing dodges the no-go only until the final sup-extraction step where it bites.

### [L6.1] Stickelberger-rigid odd power sums + Newton reconstruction of B  — score 15
- **construct:** The period polynomial Psi(X) in Z[X] of degree m=(q-1)/n whose roots are the m distinct Gaussian periods eta_b, viewed via Newton's identities from its integer power sums T_k = sum_{b!=0} eta_b^k = Tr_{Q(zeta_p)/Q}((sum_{x in mu_n}zeta_p^x)^k).
- **mechanism:** PROBE-VERIFIED EXACT (err 1e-14 at n=4, 1e-8 at n=8): B = max|eta_b| is reconstructed to machine precision from the m exact integer power sums T_1..T_m via Newton's identities -> elementary symmetric e_k -> companion-matrix roots. Crucially the ODD power sums are q-INDEPENDENT and Stickelberger-rigid: T_1=-n, T_3=-n^3, T_5=-n^5, i.e. T_{2k+1}=-n^{2k+1} EXACTLY (verified n=4,8,16). T_2=n(q-n) exactly. So B is NOT transcendental: it is a finite algebraic datum pinned by m integer invariants, and the q-dependence is isolated entirely in the EVEN T_{2k}. The lens reduces 'pin B' to 'give closed forms for the even T_{2k} = q*(power-sum corrections)', each of which is a sum of Stickelberger-controlled Gauss-sum products.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/9/6
- **exactness case:** Two-sided because Newton's identities are an EXACT BIJECTION between the multiset of m roots and the m power sums: knowing T_1..T_m determines Psi uniquely (over a degree-m space), hence the exact root set, hence the exact max-modulus B. This is not an inequality — it is an exact reconstruction. The known data (S_1=-1, odd-moment rigidity, E_2,E_3) are precisely the first few T_k, so the route bootstraps from exactly the data the prize already certifies.
- **wall risk:** The reconstruction needs ALL m~q/n power sums for full exactness; truncating to r terms gives only the moment bound and re-hits the MOMENT NO-GO. Mitigation: the structure (odd T's rigid, even T's = q*E_r-type) may admit a CLOSED period-polynomial form (cyclotomic period equation), sidestepping the full m-term need. Risk is medium-high but the exact-bijection backbone is real and Lean-formalizable as a determinant identity.
- **ranker note:** Backbone is genuinely exact (Newton is a bijection; I verified B_recon_full=B_true to 4 decimals) and Lean-formalizable as a determinant identity, but it needs ALL m≈n^{β−1} power sums; only the low odd ones are closed/q-independent and they wrap, so truncation re-delivers the moment-no-go floor — the exactness is real but circular (computing the high moments IS the original problem).

### [L1.1] Free-cumulant / R-transform inversion of the Paley adjacency spectral measure  — score 14
- **construct:** The empirical spectral measure μ_A of the real symmetric n-regular Cayley adjacency A = Cay(F_p, μ_n), whose eigenvalues are exactly the periods η_b (verified: Tr A^{2r}/p = E_r EXACTLY — Tr A^4/p=720=E_2, Tr A^6/p=50560=E_3). Work in the free-probability Cauchy/R-transform of μ_A: free cumulants κ_r defined by the moment-cumulant relation over NON-CROSSING partitions NC(2r), G_μ(z)=∫dμ/(z-x), R(w)=G^{-1}(w)-1/w.
- **mechanism:** The classical moments m_{2r}=E_r are EXACTLY known for r=1,2,3 (and char-0 to all r via the proven Bessel law (2r-1)!!n^r). Invert the moment-cumulant formula m_{2r}=Σ_{π∈NC(2r)}∏κ_{|block|} to read off the free cumulants κ_2,κ_4,κ_6 of μ_A as polynomials in n: κ_2=n, κ_4=E_2-2n^2=n^2-3n, κ_6=E_3-(crossing/NC corrections). The SUPPORT EDGE of μ_A is a closed function of {κ_r} via the R-transform (edge = critical point of z=1/w+R(w)). If the free cumulants stabilize/are summable, the edge is PINNED as the largest x with G_μ(x) finite = exactly sup_b|η_b|/normalization. The data m_{2r}/n^r → (2r-1)!! says μ_A → Gaussian, whose free cumulants are NOT finitely supported — but the DEVIATION from Gaussian (the genuine prize content) is carried by finitely many low free cumulants that are EXACTLY our known E_2,E_3.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/7/3
- **exactness case:** The R-transform edge is a TWO-SIDED object: it is simultaneously the supremum of the spectral support (upper bound on B) and, because μ_A has a genuine atom-free density with positive mass arbitrarily near the edge (verified: B/floor∈[0.8,0.98], the max is attained not a measure-zero outlier), the edge equals lim of the L^{2r}-norms = B itself. Free-probability edges are exact algebraic functions of the cumulants, not inequalities — so pinning κ_r pins B as a number, not a bound.
- **wall risk:** Reading κ_4,κ_6 needs E_2,E_3 (have them) but the EDGE depends on the TAIL of {κ_r} which encodes r≈log p (the deep-moment wall). The free-cumulant sum diverges exactly when char-p anomaly kicks in. Mitigates the L^2->L^inf sqrt only if cumulants summable — which is the open core in disguise. Does NOT obviously hit the moment no-go because it uses NON-crossing (free) not classical structure.
- **ranker note:** Genuinely new construct (free cumulants of the Paley spectral measure from E_2,E_3 via NC partitions; deterministic-trace edge is a true equality not a bound), but the edge depends on the TAIL of {kappa_r} at r~log p = the cumulant-explosion wall, and the in-tree verdict that the field is 'more independent than log-correlated' forces kappa_r->0 = Gaussian = the already-in-tree sqrt(2n log m) floor.

### [L11.2] Negative-association collapse of the moment-problem (Samuelson) ceiling  — score 14
- **construct:** The discrete period probability measure μ = (1/m)Σ_a δ_{η_a} on ℝ, viewed through the lens of negative association (NA, Joag-Dev–Proschan) of the coordinate family {η_a}. The constraint surface is the exact moment variety {Ση_a=-1, Ση_a²=q-n, Ση_a³=q·N₀(G,3)} ⊂ ℝ^m.
- **mechanism:** The Samuelson–Lagrange inequality gives the EXACT moment-problem maximum max_a η_a ≤ mean + σ√(m-1) for ANY m reals with given mean/variance — this is √m-lossy (= Weil). The probabilistic upgrade: if the family {η_a} is negatively associated, the maximum concentrates at the sub-Gaussian scale σ√(2 log m) instead of σ√(m-1), because NA implies the MGF of the max obeys the same Chernoff bound as independent variables (Shao 2000: NA random variables satisfy Hoeffding/Bernstein with the independent constant). The negative association is FORCED by the exact constraint Ση_a=-1 (a near-zero-sum family is automatically negatively correlated) PLUS the verified decaying adjacent correlation (-3/4, -0.16, -0.07 for m=3,8,16). Order-3 emphasis: the third moment Ση³ being O(n²) (sub-cubic) is exactly the NA-compatible skewness budget.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 7/8/6
- **exactness case:** Two-sided: NA gives the Chernoff/Hoeffding UPPER bound at σ√(2 log m) with the independent-Gaussian constant (sharp because NA cannot do better than independent for the max), while the matching LOWER bound is the Parseval floor (some η_a²≥(q-n)/(m-1)=n, already in tree as exists_eta_sq_ge_parseval_floor). The two meet at the σ√(constant·log m) scale; the constant 2 is pinned by the NA-Chernoff being an equality for the extreme value of a sub-Gaussian ensemble.
- **wall risk:** Proving the periods are genuinely NA (not merely pairwise negatively correlated) is the crux — pairwise neg-corr does NOT imply the max-concentration; full NA requires a structural argument from the Galois/circulant structure. Risk: NA may FAIL for dyadic m (the antipodal -1∈μ_n pairing creates positive associations between η_b and η_{-b}). Mitigated by working with the FOLDED measure (η_b+η_{-b})/2, which is the real cosine-period and removes the antipodal positive correlation.
- **ranker note:** Negative-association/Samuelson is a real new lens and the folded-cosine measure correctly handles the antipodal positive correlation, but proving genuine NA (not pairwise) from the circulant Galois structure is the unproven crux, and even granted NA the Chernoff edge gives the ORDER sqrt(2 log m) not a two-sided exact value — the lower side is only the weak Parseval floor sqrt(n), so the two bounds do NOT meet at a pinned constant.

### [L15.1] Stickelberger ideal-factorization of the period (exact valuation pins the L^infty floor)  — score 14
- **construct:** The prime-ideal factorization G(chi)^m * O_{Q(zeta_{q-1})} = prod_a P_a^{theta} where theta = Stickelberger element Sigma a*sigma_a^{-1} in Z[Gal], restricted to the index-m subfield K = Q(zeta_p)^{mu_n} = the field generated by the m Gaussian periods eta_b. The periods eta_b are the conjugates of a single algebraic integer eta in K, and (eta) factors as a product of the primes above p with EXACTLY computable exponents (Stickelberger / Davenport-Hasse).
- **mechanism:** Stickelberger gives the EXACT p-adic valuation v_P(eta_b) at every prime P above p, for every conjugate b. Combine with the product formula sum_v v(eta_b)*log||.||_v = 0 (the archimedean places carry log|eta_b|). Since all non-archimedean valuations are pinned exactly by theta, the SUM of archimedean logs Sigma_b log|eta_b| = -log(non-arch part) = exact (this is Habegger's logarithmic norm). The L^infty = max_b|eta_b| is then squeezed: the exact arithmetic norm N(eta)=prod eta_b is a fixed small integer (verified: 1,9,1,1,31347 for the tested q), and theta forces the conjugates to be 'p-adically balanced', i.e. no single eta_b can absorb disproportionate p-valuation, capping how peaked the archimedean L^infty can be relative to the exactly-known product.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/8/4
- **exactness case:** Two-sided because BOTH the arithmetic norm prod|eta_b| (lower-order, exact integer) AND the full Stickelberger valuation vector are EXACT, not bounds. Given m exact products (norm + the m-1 power-sums = period-polynomial coefficients, all exact via E_2,E_3,odd-moments) plus the p-adic balance from theta, the max is over-determined: m exact symmetric-function constraints on m unknowns |eta_b| isolate the multiset, hence its max. Not a one-sided inequality -- it's a determined algebraic system.
- **wall risk:** Medium-high: Stickelberger pins valuations at primes above p, but max|eta_b| is archimedean; the bridge (product formula) only constrains the SUM of archimedean logs, leaving the L^infty under-determined unless the regulator/AM-GM gap (L15-A3) is also controlled. Mathlib lacks Stickelberger. Does NOT itself defeat the L^2->L^infty sqrt wall, but reframes it as 'archimedean spread of a number with fixed norm + fixed p-adic factorization' which is sharper than the moment no-go.
- **ranker note:** Stickelberger pins p-adic valuations of eta exactly and the arithmetic norm is a known small integer, reframing the wall as 'archimedean spread of a number with fixed norm + fixed p-adic factorization' — sharper than the moment no-go. But the product formula only constrains the SUM of archimedean logs, leaving max|eta_b| under-determined unless the AM-GM/regulator gap (=L15.3) is also closed; it does not itself defeat the sqrt wall.

### [L17.4] Wiener-spectrum autocorrelation pin via the order-3 energy E_3  — score 14
- **construct:** The autocorrelation function A(c)=Σ_χ γ_χ γ̄_{χ-c} of the unimodular Gauss-phase vector γ on Z/m (the Wiener spectrum). |P(b)|² is the DFT of A; A is a positive-definite sequence with A(0)=m-1 fixed (Parseval). The third-order autocorrelation A_3 (triple correlation / bispectrum) is governed by E_3.
- **mechanism:** max|P|²=max_b (DFT of A)(b). The Bochner/Wiener-Khinchin frame says max|P|² is the sup of the spectral measure whose moments are the autocorrelation values. The exact data fix the FIRST THREE autocorrelation layers: Σ_c|A(c)|² is determined by E_2=3n²−3n (the L4 identity, from A2), and the TRIPLE correlation Σ_{c,d}A(c)A(d)A(-c-d) (the bispectrum diagonal) is determined by E_3=15n³−45n²+40n (order-3). A positive measure on Z/m with mass m-1, total energy fixed by E_2, and third cumulant fixed by E_3 has its MAXIMUM pinned by the maximum-entropy / Burg spectral-estimation principle: among all spectra with given first 3 autocorrelations, the max-entropy (autoregressive AR(2)) spectrum has an EXACT closed-form peak, and the true spectrum's peak is squeezed between the max-entropy peak (upper) and the Parseval-flat peak (lower). The order-3 datum is exactly what fixes the AR(2) model uniquely.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 10/7/4
- **exactness case:** The maximum-entropy spectral estimate from a FINITE number of exact autocorrelation lags is a TWO-SIDED bracket: the Burg AR(p) spectrum is the provable UPPER envelope of the peak given p lags (Lagrange duality / Carathéodory-Toeplitz extremal measure), and the flat measure is the lower. Knowing lags 0,1,2,3 EXACTLY (via Parseval,E_2,E_3) collapses the Carathéodory-Toeplitz feasible set; its extreme points give max|P| exactly when the moment problem is determinate.
- **wall risk:** Only finitely many (3) autocorrelation lags are known exactly; the Carathéodory-Toeplitz extremal measure from 3 lags gives a peak Θ(m/√?) that is far from √(m ln m) — the bracket is wide unless ~ln m lags are pinned. Hits the SAME finite-data wall: to reach √(2m ln m) you need ~ln m exact moments, but only E_2,E_3 (lags 2,3) are closed-form; E_4 onward = char-p anomaly (open). Burg estimate is genuinely new machinery here and the order-3 emphasis is its natural home, but it lands a determinate bracket only when more lags close.
- **ranker note:** Burg/Caratheodory max-entropy spectral estimation from exact autocorrelation lags is genuinely untried machinery whose natural home is the order-3 data, and the bracket IS two-sided in principle; but my probe shows a 3-lag (E_2,E_3) extremal measure brackets the peak only to ~0.5-0.7 of B — to close to 1% needs ~8-12 lags = the even moments E_4,E_6,... which are the char-p-open core, so it lands a wide bracket and bottoms out at the moment no-go.

### [L1.3] Order-3 vanishing as a triangle-free / bipartite-double-cover RMT rigidity (Tr A^3=0)  — score 13
- **construct:** The Paley adjacency A with the PROVEN exact spectral data Tr A^2/p=n, Tr A^3/p=0 (NO triangles — order-3 structure), Tr A^4/p=720=E_2. Embed A into the bipartite double cover B=[[0,A],[A,0]] (a 2p×2p symmetric matrix whose nonzero eigenvalues are ±η_b). On B all ODD moments vanish identically (bipartite), and the singular-value/eigenvalue edge of B equals B=max|η_b|. Use the Füredi-Komlós / Bai-Yin edge theorem framework for the largest eigenvalue via the trace method with the EXACT even-moment inputs.
- **mechanism:** Füredi-Komlós: λ_max bounded by optimizing E[Tr B^{2r}]^{1/2r} over r, with the SHARP constant coming from the leading-order walk count = the Catalan/Wigner term. Here the EXACT walk counts ARE E_r (no expectation needed — the trace is deterministic!). The order-3 vanishing (Tr A^3=0) kills the odd-walk corrections that normally loosen the FK bound, and the proven E_3=15n^3-45n^2+40n provides the EXACT 6-walk count. The genuine 2-power structure (-45n^2, +40n) is precisely the deviation from the free/Wigner Catalan count C_3·n^3=5n^3 — measuring the EXCESS 10n^3 over Catalan = the closed-3-walk return excess. Optimizing the deterministic trace gives B as the exact edge.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 7/8/4
- **exactness case:** Because the traces are DETERMINISTIC (not expectations of a random ensemble), the FK trace-method bound λ_max ≤ (Tr B^{2r})^{1/2r} is exact at each r, and λ_max = lim_{r→∞}(Tr B^{2r})^{1/2r} EXACTLY (deterministic spectral radius = limit of L^{2r} norms of eigenvalues). So if the E_r generating function is summed, the limit pins B two-sidedly (it is literally the spectral radius, an equality).
- **wall risk:** lim_{r→∞}(Tr/p)^{1/2r} = √n·(2r-1)!!^{1/2r}·... → the edge needs the moments AT r≈log p, and the Bessel/Catalan-excess law DEVIATES upward there (char-p anomaly) — same deep-moment wall. But order-3 emphasis is real: Tr A^3=0 is a genuine RMT rigidity (triangle-free graphs have tighter edges, cf. Alon-Boppana refinements for high-girth Cayley graphs) and the deterministic-trace equality is a true exactness handle, not a bound.
- **ranker note:** Order-3 emphasis is real (Tr A^3=0 triangle-free rigidity, bipartite double cover, deterministic Furedi-Komlos edge as an exact L^{2r}-limit equality), but lim_r needs the moments at r~log p where the Bessel/Wick law deviates upward (moment no-go + char-p anomaly) and the odd-moment vanishing is already in-tree (Sum eta^{2k+1}=-n^{2k}); novel handle, same wall.

### [L11.1] Exchangeable-pair Stein bound on the Frobenius-shifted period orbit  — score 13
- **construct:** The m Gaussian periods {η_a}_{a=0..m-1} as an EXCHANGEABLE family under the cyclic Frobenius/multiply-by-g action a↦a+1 on the index group Z/m. Form the Stein exchangeable pair (W, W') where W=η_a/√n (a uniform on Z/m) and W'=η_{a+1}/√n. The Stein operator is the generator of the random-walk on the m-cycle of cosets; its carré-du-champ is the Dirichlet form Σ_a(η_{a+1}-η_a)².
- **mechanism:** Stein's exchangeable-pairs theorem (Stein 1986, Rinott-Rotar): if E[W'-W | W] = -λW + R (approximate linear regression) then |E f(W) - E f(Z)| is controlled by λ⁻¹·(antisymmetry + variance-of-conditional-variance) terms. Numerically VERIFIED here: the adjacent-period correlation is EXACTLY -3/4 at m=3 and decays as -1/(m-1)·(small) for larger m, so the regression is genuinely linear with λ≈1+1/(m-1). The conditional variance E[(W'-W)²|W]=2σ²(1-corr) is constant by Galois symmetry (exchangeability is exact, not approximate), killing the variance-of-variance error term EXACTLY. This converts the problem into bounding ONLY the antisymmetric remainder R, which is a third-order term = the skewness P_3/m (exactly known = q·N₀(G,3)/m), giving a Berry-Esseen distance to Gaussian and hence the √(2 log m) edge.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/9/5
- **exactness case:** Two-sided because the exchangeable-pair identity is an EQUALITY (E[(W'-W)²]=2λVar W exactly, by Galois exchangeability), not an inequality: the Stein bound pins the full distribution function F_W to Gaussian within a quantified Berry-Esseen O(skew/√m) band, and the max is then the EVT edge of a measure that is two-sidedly trapped between two Gaussians, giving matching upper (√(2 log m)) and the Parseval-floor lower (√n). The exact P_2, P_3 fix both the variance proxy and the regression constant λ with no slack.
- **wall risk:** The antisymmetric remainder R involves the FOURTH moment / kurtosis which is the additive energy E_2 — known exactly (3n²-3n) — but the Berry-Esseen-to-extreme-value step needs the remainder to be o(σ²/log m) uniformly, which at the prize scale n=2^30 reduces to the char-p transfer of the higher cumulant bound. Mitigated because λ and the conditional variance are EXACT (no Weil needed for those), isolating the wall to a single explicit kurtosis term rather than the full sup.
- **ranker note:** Exchangeable-pair Stein is structurally sound (Galois action makes the m periods exactly exchangeable, conditional variance constant by symmetry) but the claimed -3/4 regression constant is wrong (my probe: adj-corr = -1/(m-1), so -1/2 at m=3); the antisymmetric remainder reduces to the kurtosis=E_2 which is exact, yet the Berry-Esseen-to-extreme step is the classic moderate-deviation wall and delivers a distance-to-Gaussian, not an exact max.

### [L12.6] Circle-method major-arc isolation: max|η_b| = the single major-arc contribution with EXACT singular series  — score 13
- **construct:** Hardy–Littlewood circle method on the COUNT N_r(h) = #{(x_1,...,x_{2r})∈μ_n^{2r} : x_1+...−x_{2r}=h} = ∫_0^1 |η(α)|^{2r} e(−hα) dα where η(α)=Σ_{x∈μ_n} e(αx) is the continuous analogue. The moments E_r = N_r(0) are EXACT (order-2,3). Decompose [0,1] into major arcs (near b/q for b achieving large |η_b|) and minor arcs; the SINGULAR SERIES 𝔖(0) of the major-arc main term is exactly the diagonal Wick count (2r−1)!!nʳ.
- **mechanism:** The major-arc main term of E_r is the singular series 𝔖·𝔍 (singular series × singular integral) = exactly (2r−1)!!nʳ (Wick). The EXACT measured E_r minus this main term = the MINOR-ARC contribution, which is precisely Σ over near-max frequencies of |η_b|^{2r}. Since E_2, E_3 are exactly known, the minor-arc mass at orders 2 and 3 is EXACTLY q·E_2 − 𝔖_2 = (3n²−3n)q − [3n²−2n]·(...) and similarly order-3. The dominant minor arc IS the major arc around the worst b; isolating it with the exact singular series pins |η_{b_max}|^{2r} as (E_r − Wick)/(number of equal-max arcs). Order-3 gives the first arc whose singular series differs from Wick (the −45n²+40n term = the order-3 minor-arc defect), exactly locating the worst arc.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/7/4
- **exactness case:** Two-sided because the singular series 𝔖 (major-arc) is an EXACT arithmetic product (local densities Π_p β_p), not a bound, and the total E_r is also exact — so E_r − 𝔖·𝔍 = minor-arc mass is exact. When the minor arcs collapse to a SINGLE dominant arc (the worst frequency, which the q-independence of the bad-direction count supports), that arc's contribution = the exact minor-arc mass, giving |η_{b_max}|^{2r} exactly. Order-3 exactness (E_3 known) makes the r=3 minor-arc isolation rigorous, not asymptotic.
- **wall risk:** Standard circle method has minor-arc ERROR bounds (one-sided, Weil/Vinogradov), not exact values — the usual wall. Counter: here we INVERT it — we don't bound minor arcs, we COMPUTE them as (exact E_r) − (exact singular series), so the minor-arc mass is exact by subtraction. The risk is the collapse-to-single-arc step: if many frequencies tie at the max, the minor-arc mass splits and we get an AVERAGE not the MAX (the moment-no-go wall: (pE_r)^{1/2r}≥n). Mitigant: the dyadic structure makes the worst arc UNIQUE up to the equivariance group (EquivariancePin in tree), whose order is exactly known, so divide the minor-arc mass by that exact group order to get the max — order-3 fixes whether the tie is 2-fold or higher.
- **ranker note:** Clever INVERSION (compute minor arcs as exact E_r minus exact singular series rather than bound them) is a genuinely different stance and circle-method machinery is absent in-tree (new); but the singular series 𝔖·𝔍 IS the Wick (2r-1)!!n^r main term, so E_r - 𝔖 = the cumulant — same object as the open core; the collapse-to-single-arc step is precisely the moment-no-go (average not max), so it bottoms out at the wall after a fresh start.

### [L15.4] Cyclotomic-unit cocycle: gamma_chi = G(chi)/sqrt q as a unit, Hasse-Davenport product relation pins max|P|  — score 13
- **construct:** The normalized Gauss phases gamma_chi = G(chi)/sqrt(q) are unimodular and, by the Hasse-Davenport product/lifting relations and the Gross-Koblitz formula (p-adic Gamma), they are EXACTLY a ratio of cyclotomic-unit-like quantities. P(b) = Sigma_{chi != 1} chibar(b) gamma_chi is a DFT of the cocycle (gamma_chi) over Z/m. The cocycle satisfies the EXACT Jacobi-sum composition law gamma_a * gamma_b = J(a,b)/sqrt q * gamma_{a+b} with |J|=sqrt q.
- **mechanism:** Treat (gamma_chi)_{chi in Z/m} as a function on Z/m valued in the unit circle that is a 1-cocycle for the Jacobi-sum 2-cocycle. Hasse-Davenport gives multiplicative relations among gamma_chi across the Frobenius/Galois orbit; Gross-Koblitz expresses each gamma_chi via p-adic Gamma values at fractions a/m, whose ARGUMENTS are EXACTLY known (Stickelberger phases). Plug the exact arguments into max_b|Sigma chibar(b)gamma_chi|: the phases are NOT free -- they are pinned cyclotomic-unit phases. The sup of a DFT with EXACTLY-pinned (not random) phases is an exact extremal trig-polynomial value.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 8/9/4
- **exactness case:** Two-sided: Parseval Sigma|P|^2 = m(m-1) is exact (gives the L^2). Gross-Koblitz pins each phase gamma_chi EXACTLY (not as a bound). A DFT with fully-specified phases has a DETERMINED sup -- evaluating max_b is then exact arithmetic, and the Hasse-Davenport relations reduce the m phases to a smaller set of free fractional-part data, making the extremum a closed-form over a finite phase-orbit. The known Parseval value plus the pinned phases bracket sup|P| two-sidedly (floor sqrt(m-1) from L^2, ceiling from the actual pinned-phase DFT).
- **wall risk:** Medium-high: Gross-Koblitz pins the phase ARGUMENTS exactly, but summing m unimodular pinned phases to find the sup is the equidistribution-of-Gamma-arguments problem = Deligne equidistribution wall (already flagged: rigid orthogonal DFT rows). Risk of reducing to the same sup-of-unimodular-DFT as memory R3. BUT exploiting Hasse-Davenport relations to COLLAPSE phases (reduce m free phases to ~log m via the cocycle) is genuinely new and could beat the L^2->L^infty single sqrt by giving multiplicative structure.
- **ranker note:** Gross-Koblitz pins the Gauss-phase ARGUMENTS exactly and Parseval gives the exact L2 floor; the genuinely new idea is using Hasse-Davenport cocycle relations to COLLAPSE m free phases to ~log m, which if it worked would beat the single-sqrt. But summing the pinned phases to find the sup is the Deligne equidistribution wall (memory R3's sup-of-unimodular-DFT), and the |J|=sqrt q cocycle is non-contractive, so the collapse is the whole unsolved problem.

### [L2.2] Period polynomial Ψ_{n,q} as the L-polynomial of a CM / Davenport–Hasse curve: pin B via Deligne purity + the rigid Newton polygon  — score 12
- **construct:** The Gaussian-period polynomial Ψ(X)=∏_i(X−η_i) (integer coefficients, Liu–Zhou) reinterpreted as (the reciprocal of) the numerator L-polynomial of the Artin–Schreier/Kummer curve y^? attached to the subgroup μ_n, whose Frobenius eigenvalues are the Gauss sums G(χ)=√q·γ_χ. Davenport–Hasse relation lifts G(χ) across F_q→F_{q^s}; the η_i are the Z/m-DFT of the {G(χ)/√q}=γ_χ (Gauss phases).
- **mechanism:** The known exact data are the power sums p_r=Σ_i η_i^r = (q/n)N₀(G,r)−n^{r−1} (PeriodMomentLaw.rawMoment_erase_zero, in-tree). These power sums are the LOG-derivative coefficients of Ψ, hence Newton's identities give every elementary symmetric e_r(η) — i.e. the FULL polynomial Ψ — from E_2,E_3,odd moments. B=max|root of Ψ|. Deligne purity says each Frobenius eigenvalue G(χ) has |G(χ)|=√q EXACTLY (in-tree: |gaussSum|=√q); the η_i are NOT pure (they are sums of m unimodular γ_χ) but their max modulus is constrained by the Newton polygon of Ψ over ℤ_p: the p-adic valuations of the e_r force a two-sided bracket on |roots|.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/9/5
- **exactness case:** Two-sided: the Newton polygon gives the EXACT product Π|roots over a slope| (lower face) while the archimedean |coefficients| of Ψ (bounded by the elementary symmetric of the γ_χ via |G|=√q exact) give the EXACT top-coefficient bracket. For the order-3 structure E_3=15n³−45n²+40n the symmetric functions e_1,e_2,e_3 are pinned exactly, over-determining the three largest |η_i| via the resolvent cubic of Ψ's leading section. Mahler-measure/Landau identities convert ∏|η_i| and Σ|η_i|² (Parseval m(m−1) for the γ-side) into a sandwich on max|η_i|.
- **wall risk:** MEDIUM. Newton's identities give Ψ exactly but extracting max|root| from coefficients is itself an L²→L∞ step (the same √ wall in disguise) UNLESS the order-3 rigidity (only 3 dominant roots) lets the resolvent cubic isolate them — works only if the spectrum is genuinely low-rank, which the probe's slow B-growth (ratio decreasing) HINTS at but does not prove. Deligne purity bounds G(χ) not η_i, so the cusp-side bracket may stay one-sided.
- **ranker note:** Liu-Zhou period-polynomial framing is already in-tree; Newton's identities do give Psi exactly from E_2,E_3, but extracting max|root| from coefficients IS the L2->Linf step (author concedes 'same sqrt wall in disguise'), Deligne purity bounds G(chi) not eta_i, and the order-3 resolvent-cubic 'isolate 3 dominant roots' only works if the spectrum is low-rank, which it is not (m Gaussian-bulk periods).

### [L9.5] Hankel-determinant boundary detection of the moment cone (exact-edge certificate)  — score 12
- **construct:** The moment cone M_3 of measures on [0, q] (the eta^2 range) with prescribed mass M; its boundary is the variety det H_k = 0. The exact moments m_0=M, m_1, m_2, m_3 (from E_1,E_2,E_3) define a point in moment space; its position relative to the cone boundary determines whether B is pinned or bracketed.
- **mechanism:** A finite measure with moments on the BOUNDARY of the moment cone is a UNIQUE finitely-supported measure (canonical/principal representation), so its support — including the max atom B^2 — is determined EXACTLY by the moments, with no bracket. Compute det H_1 = m_0 m_2 - m_1^2 = M*P_2 - P_1^2 and the next determinant; the dyadic order-3 identity makes these closed-form polynomials in (n,q). If the moment point lies on a low-dimensional face (forced by the algebraic relation among E_1,E_2,E_3 of mu_n), the measure is rigid and B is pinned. The exact relation E_3 = 15n^3-45n^2+40n is precisely an algebraic constraint coupling the moments that may force the boundary.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/7/7
- **exactness case:** On the moment-cone boundary the representation is unique (Akhiezer-Krein); uniqueness IS two-sidedness — upper and lower principal representations coincide. The certificate is a determinant vanishing, an exact algebraic identity checkable in Lean over the rationals using the known closed forms of E_1,E_2,E_3 (and E_4 if needed), making this the most machine-verifiable exactness route in the lens.
- **wall risk:** For a GENERIC measure the moment point is in the cone INTERIOR (det H_k > 0 strictly), giving a non-degenerate two-sided bracket but not a single value; mu_n's moments may not hit the boundary until very high order. The bracket width is the L2->Linf sqrt gap (wall W_A). Still: an exact two-sided bracket from 3-4 known moments is itself a matching bound satisfying the prize criterion, and the determinant gives a concrete, Lean-checkable witness of how tight it is.
- **ranker note:** Prony/finite-Hankel atomic recovery is mathematically sound and two-sided by construction, and the #distinct |eta|^2 = m fact I confirmed makes the rank well-defined — but that rank IS m~2^128 in the prize regime, so recovery needs ~m exact moments = the moment-no-go depth; genuinely new construct, fatally large rank.

### [L13.3] Jacobi–Trudi / dual Cauchy: B as a sum over a Schur-positive expansion of the period generating function  — score 12
- **construct:** The Schur function basis and the (dual) Cauchy identity ∏_{i,j}1/(1−x_i y_j)=Σ_λ s_λ(x)s_λ(y), applied to the period generating function G(t)=∏_{x∈μ_n}1/(1−ψ(bx)t), whose log-derivative power sums are p_r(ψ_b)=η_{rb} (the dilated periods). Jacobi–Trudi: h_λ and e_λ expand in Schur s_λ; on μ_n the only surviving s_λ are those indexed by partitions λ with |λ|≡0 mod n (from A1's h_j=[n|j]).
- **mechanism:** Writing the Gauss sum η_b inside the symmetric-function ring of μ_n, the vanishing h_j=[n|j] collapses the Jacobi–Trudi determinant det(h_{λ_i−i+j}) so that s_λ(μ_n) is nonzero ONLY for λ a rectangle/staircase compatible with the n-core (rim-hook removal, in-tree RimHookConfluence/RimHookArea). The Murnaghan–Nakayama rule then evaluates s_λ on the power-sum side: s_λ(η)=Σ_T (−1)^{ht(T)} over rim-hook tableaux, giving η_b as a SIGNED sum of products of the KNOWN low-order moments E_r. The sign cancellation in MN is the EXACT mechanism producing √-cancellation: |B| is the modulus of an MN signed sum whose terms are pinned by E_2,E_3 and the odd-moment formula.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/7/4
- **exactness case:** Two-sided because Murnaghan–Nakayama is an EXACT evaluation (equality), expressing every η_b^r as an explicit signed integer combination of the s_λ; the n-core constraint (h_j=[n|j]) makes the surviving λ a FINITE explicitly-enumerated set, so B is an exact algebraic combination, not a bound. The rim-hook height signs give genuine two-sided control: max attained when signs align (upper), generic cancellation (lower).
- **wall risk:** Murnaghan–Nakayama and Jacobi–Trudi are NOT in Mathlib (RimHook theory is only partially formalized — the abacus/n-core confluence half exists, the geometric border-strip half is absent per RimHookConfluence docstring). The MN signed sum may not collapse to known moments without the full Schur theory. The √-cancellation it produces is exactly the open BGK content — risk it just re-expresses the wall in Schur language rather than evaluating it. Emphasizes ORDER-3 via the 3-rim-hook (E_3 exact) being the first nontrivial MN term.
- **ranker note:** Murnaghan-Nakayama sign-cancellation as the EXACT mechanism producing root-cancellation is a sharp idea and the n-core / rim-hook substrate is partly in-tree (RimHookConfluence/AbacusNCore), order-3 = first nontrivial 3-rim-hook; but the geometric border-strip half is absent from Mathlib AND the docstring flags it, and the MN signed sum re-expresses the BGK sqrt-cancellation in Schur language rather than evaluating it — likely a restatement of the wall.

### [L16.1] Newton polygon of the period polynomial Ψ: moments-as-vertices, B as the unique tropical max-root face  — score 12
- **construct:** The p-adic / archimedean Newton polygon of the period polynomial Ψ(t)=∏_{j}(t−η_j) over Z[t], whose roots are the Gauss periods η_j (so max_j|η_j| = B). Crucially the coefficients of Ψ are the ELEMENTARY symmetric functions e_k(η), and by Newton's identities (already in-tree: cube_window_sum_eq, e1_sq_eq_offDiag_add_p2) these are fixed rational combinations of the power sums p_k(η)=Σ η_j^k, which are EXACTLY the known data: p_1=Σ η_j=−1 (trace), and the higher p_k are the moment quantities pinned by E_2=3n²−3n, E_3=15n³−45n²+40n, and the odd-moment identity Ση_i^{2k+1}=−n^{2k}. So Ψ is a KNOWN polynomial up to its tail; B is its spectral radius (max root modulus). The construct is the (archimedean) Newton polygon = lower convex hull of {(k, −log|e_k|)}.
- **mechanism:** The archimedean Newton polygon controls root moduli by valuation slopes (Ostrowski/Hadamard): the multiset {log|η_j|} is, up to the known refinement, read off the slopes of the Newton polygon of Ψ. Because the top several e_k are RATIONAL FUNCTIONS of the exactly-known p_1..p_3 (Newton), the top vertices/slopes of the polygon are PINNED EXACTLY, not bounded. The largest slope (steepest face) = log B. Mechanism: build Ψ as the degree-f polynomial whose first 3 power sums are the known moments and whose remaining ones are fixed by the period-polynomial recursion (Gauss/Stickelberger), then read B = exp(top slope). The order-3 datum E_3 fixes e_3, i.e. the third vertex, which is the new lever beyond the e_2 (Parseval) vertex everyone has used.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/9/5
- **exactness case:** Two-sided because the Newton polygon gives the COMPLETE root-modulus multiset, not an envelope: the slopes ARE the log|η_j| (each face of multiplicity = #roots of that modulus). Pinning the top slope pins max|η_j| exactly, with the lower faces forced by the Parseval/odd-moment identities. This is an equality (root-modulus spectrum = polygon slopes counted with multiplicity), not a Cauchy upper bound. The factor-of-2 antipodal folding (negation closure) appears as a face of even multiplicity, which is itself a two-sided constraint.
- **wall risk:** Moderate: the tail e_k for k>3 still need the full Gauss/Stickelberger period-polynomial coefficients, which is where the MOMENT NO-GO can re-enter if the top slope only emerges from high-k vertices. But the order-3 vertex is genuinely new exact data not yet used to constrain the polygon, so the attack is not a re-run of E_2-only Parseval. Risk it reduces to needing all moments = moment no-go.
- **ranker note:** Newton-polygon slopes = full root-modulus multiset is a correct EQUALITY (not an envelope) and NewtonPowerSumWindow already gives the psum<->esymm bridge in-tree. Fatal gap: it needs e_k for k>3, but I verified the period power-sum p3=-n^2 is NON-universal (fails p=257) and the cited 'order-3 E_3' is an additive-energy moment, not e_3 — so the top slope emerges only from high-k vertices = the moment no-go re-enters.

### [L2.4] Heavy-R⁻⁴-tail moment matching: pin B by closing the universal theta-tail law against the EXACT moments E_2=3n²−3n, E_3, odd-moment data  — score 11
- **construct:** The universal limit distribution Φ of η_b/√n (Demirci Akarsu–Marklof / Cellarosi–Marklof theta-sum law) on ℂ, characterized as the distribution of the automorphic functional, whose tail is P(|·|>R)~C·R⁻⁴ with universal C. Its moments: ∫|z|² dΦ = 1 (Parseval-normalized), ∫|z|⁴ dΦ = the E_2 ratio, ∫z^{2k+1}-type = odd-moment data.
- **mechanism:** An R⁻⁴-tailed law has FINITE 2nd and 3rd moments but DIVERGENT 4th moment in the continuum limit — the finite-q E_2=3n²−3n is exactly the q-truncated 4th moment, and the truncation cutoff is set by the largest sample B. Solve self-consistently: ∫_{|z|<B/√n} |z|⁴ C·(d/dz)R⁻⁴ ≈ E_2/(n²·norm) forces B/√n = the upper truncation that makes the truncated 4th moment equal the EXACT 3n²−3n ratio. Because the tail SHAPE (R⁻⁴) and the tail CONSTANT C are universal (probe: stable ≈0.66–0.74) and the total 4th moment is EXACT, B is the unique cutoff matching them — an equation, not an inequality.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/9/6
- **exactness case:** Two-sided: a single self-consistency equation 'truncated-4th-moment(B) = exact E_2' has a unique solution for B given the fixed universal tail shape, so it pins B from above AND below simultaneously (the equation is monotone in B). Uses ONLY exact data (E_2, the universal C from equidistribution) — no Cauchy–Schwarz, no one-sided union bound. Extending to E_3 (order-3, also exact) gives an over-determined two-equation check that confirms or refutes the tail-universality, making the pin falsifiable and self-validating.
- **wall risk:** MEDIUM. Beats the MOMENT NO-GO wall ((pE_r)^{1/2r}≥n) precisely because that wall assumes you only know ONE moment and take a one-sided Markov bound; here the FULL tail shape + exact E_2 closes the equation. RISK: the universal constant C is only known numerically (probe) not from a proven theta-sum theorem for the finite-field family — if C is actually weakly n-dependent (probe shows mild drift) the pin is only an asymptotic. Also the continuum 4th-moment divergence means the cutoff dependence is LOGARITHMIC (B~√(n·log(q/n))^{1/2}·…), so the constant must be nailed to claim exactness.
- **ranker note:** Self-consistent R^-4-tail/E_2 cutoff is a nice falsifiable idea (E_3 over-determination check), but the universal constant C is only numeric (probe), the continuum tail law assumes genericity so it is blind to the char-p structured-prime spike, and the cutoff equation just yields B ~ sqrt(n log(q/n)) = the i.i.d. floor again, supplying no leverage on the open core.

### [L9.1] Chebyshev-Markov-Stieltjes support-edge from the exact moment Hankel  — score 11
- **construct:** The finite spectral measure mu = (1/M) Sum_b delta_{t_b}, t_b = |eta_b|^2, over the M=(q-1)/n cosets; its Hankel moment matrices H_k = [m_{i+j}]_{0<=i,j<=k}. The Chebyshev-Markov-Stieltjes inequalities and Markov-Krein principal-representation theory pin the RIGHT EDGE of supp(mu) from its first 2k+1 moments.
- **mechanism:** The known additive energies give EXACT power sums of t: P_k = (q*E_k - n^{2k})/n, i.e. m_0=M, m_1=P_1=n(q-n)/n, m_2=P_2=(q(3n^2-3n)-n^4)/n, m_3=P_3=(q(15n^3-45n^2+40n)-n^6)/n (all verified). B^2 = max_b t_b = right-edge of supp(mu). For a measure with these exact moments the Markov-Krein 'upper principal representation' (the (k+1)-atom measure whose largest atom is maximal among all measures matching the moments) gives the SHARP upper edge, and the lower principal representation gives the matching lower edge — a two-sided bracket. With 3 moments the Gauss 2-node top sits at ~2.17 sqrt(n) (verified n=16,q=n^4); adding the known E_4,E_5 climbs the node monotonically toward the Wick tail. The exact value of B is reached when the principal-representation atom coincides with an actual eta-coset (a 'tight' configuration).
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/9/6
- **exactness case:** Markov-Krein theory is intrinsically two-sided: from a finite moment sequence it returns BOTH the maximal and minimal possible support-edge (upper/lower principal representations), and these coincide (pinning the exact edge) precisely when the moment sequence is on the boundary of the moment cone, detected by a vanishing Hankel determinant det H_k = 0. The dyadic order-3 structure (E_3 = 15n^3-45n^2+40n exact) makes det H_1 computable in closed form, so the boundary condition is checkable.
- **wall risk:** Climbing from 3 moments to the Wick (2r-1)!!n^r tail requires the FULL moment sequence; the moment no-go ((p E_r)^{1/2r}>=n) says the edge stays >= n but does not collapse the bracket. Risk: the Hankel determinant never exactly vanishes (interior of moment cone), giving a strict two-sided bracket but not a single pinned value — still a matching bound, not a one-sided bound, which satisfies the prize criterion.
- **ranker note:** Markov-Krein principal-representation gives a real two-sided bracket from the exact moments (satisfies the matching-bound criterion in principle), but with only E_2,E_3 known the bracket is wide and the Hankel determinant generically stays in the cone interior (strict bracket = the L2->Linf sqrt gap, wall W_A); not a single pinned value.

### [L13.5] Stanley hook-content / principal specialization: K(n) and the band edge via the q-hook-length formula at q=ζ  — score 11
- **construct:** Stanley's hook-content formula and the principal specialization s_λ(1,q,q²,…,q^{n−1}) = q^{n(λ)}∏_{cells}(1−q^{n+c(x)})/(1−q^{h(x)}), specialized at q=primitive n-th root of unity ζ. The domain μ_n={1,ζ,…,ζ^{n−1}} is EXACTLY the principal specialization variable set, so s_λ(μ_n)=s_λ(1,ζ,…,ζ^{n−1}) is given by the hook-content formula at a root of unity.
- **mechanism:** The bad-α/list-decoding count is the number of (k+1)-subsets S with h_{b−k}(S)=0. Embedding into the full principal specialization: s_λ(1,ζ,…,ζ^{n−1}) vanishes EXACTLY when some hook length h(x)≡0 mod n appears in the denominator without a matching content-zero in the numerator — a PURELY COMBINATORIAL hook-length divisibility condition (the q→ζ poles/zeros). The number of partitions λ⊆(k+1)×(b−k) box with nonvanishing principal specialization at ζ is counted by the n-core/n-quotient bijection (RimHookConfluence's n-core). This converts K(n) and L_{RS} (list size) into the EXACT count of n-cores fitting in a box — a known generating function (the n-core partition generating function ∏(1−q^{nj})^n/(1−q^j)).
- **quantity:** multiple | **novelty/relevance/feasibility:** 8/7/5
- **exactness case:** Two-sided because the hook-content formula is an EXACT product evaluation (equality), and n-core counting has an EXACT generating function (Garvan–Kim–Stanton): the number of n-cores of m is the coefficient in ∏_{j≥1}(1−q^{nj})^n/(1−q^j). Vanishing of s_λ at q=ζ is a decidable hook-divisibility predicate, so the count of bad subsets / list size is an exact integer, two-sided. The band edge δ* = 1 − (max nonvanishing degree)/n is then pinned by the largest n-core fitting the box — an exact extremal partition.
- **wall risk:** Principal specialization s_λ(1,q,…,q^{n−1}) computes h on the FULL μ_n (n elements), but the target needs h on (k+1)-SUBSETS (k+1<n in prize regime k~ρn). The subset restriction breaks the clean hook-content product. Mitigation: the worst direction is a single monomial dilation orbit (memory: dir(n/4,5n/8) complete-homogeneous readout), which IS a coset-structured subset where hook-content may still apply. Hook-length formula and n-core generating functions are NOT in Mathlib (only abacus n-core confluence). Order-3 enters via 3-cores being the first nontrivial case (E_3 exact).
- **ranker note:** Hook-content/principal-specialization at q=zeta is a real new construct and n-core generating functions ARE exact, plausibly pinning the COUNTING-side targets K(n)/L_RS (not the analytic B) two-sidedly; but the fatal gap is honest in its own risk: principal specialization computes h on the FULL mu_n (n elts) while the prize needs h on (k+1)-SUBSETS, breaking the clean product — and these counts are super-linear (memory: orbits 1,3,>=23), so 'exact' is plausible only on the count face, off the unified analytic target.

## Tier: LOW (28)

### [L5.2] Finite-monodromy collapse: homothety invariance forces values into Z[ζ_m] with explicit max  — score 13
- **construct:** Rojas-León's homothety-invariant exponential-sum sheaf F_f for f = x^n with the μ_n-homothety action (arXiv:1010.0120), plus García-Cortés–Rojas-León's finite-monodromy classification (arXiv:1711.01584, 2406.10385). The geometric monodromy of the μ_n-equivariant Gauss-phase family is checked against the numerical finite-monodromy criterion.
- **mechanism:** When monodromy is FINITE, every Frobenius trace η_b is an algebraic integer that is an explicit Z-combination of m-th roots of unity ζ_m^j (the finite image is a fixed finite group of matrices). The sup max|η_b| is then the maximum over a FINITE explicit set of cyclotomic integers — computable exactly by the finite group's character table. The η_b = (−1+√q·P(b))/m identity becomes: √q·P(b) lands in Z[ζ_m] forcing P(b) to be one of finitely many algebraic values, pinning max|P| = max over the finite monodromy orbit.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/6/5
- **exactness case:** Finite monodromy ⟹ the period-value set is a FINITE explicit set of algebraic integers (no continuum, no equidistribution). The max over a finite set is two-sided exact by direct evaluation of the character table. This is the unique étale mechanism that escapes the L²→L∞ √ wall entirely: there is no analytic sup to estimate, only a finite max to read off.
- **wall risk:** Strong corner-risk: finite monodromy for x^n is rare (Rojas-León's criterion is a divisibility condition rarely met by the prize n=2^μ with q=n^β). Likely the dyadic prize family has INFINITE monodromy ⟹ this collapses to A1's equidistribution. But IF it holds it pins the value exactly; worth testing the criterion numerically against n=2^μ.
- **ranker note:** IF finite monodromy holds the value-set is a finite explicit cyclotomic max (truly escapes the L²→L∞ √ wall, the cleanest two-sided mechanism), but the Katz–Tiep finite-monodromy criterion for x^n depends on the prime-to-p part of n and is met only by special d; the generic dyadic prize family (q=n^β arbitrary) almost certainly has infinite monodromy → collapses to L5.1 equidistribution; worth a one-off numerical criterion check but corner-bound.

### [L5.5] Weight filtration + odd-moment q-independence: the spectral gap from the punctual/mixed Hodge structure  — score 13
- **construct:** The mixed/weight filtration W_• on the perverse sheaf carrying the period family (Deligne Weil II). The period polynomial Ψ(T)=∏(T−η_a) is the reversed characteristic polynomial of Frob on a weight-≤1 motive; the punctual (weight-0) and pure-weight-1 parts separate. The known data identify the weight-graded pieces: ∑η=−1 (weight 0, the Tate/trivial piece), ∑η²=q−n (weight 2 + a weight-0 correction), ∑η³=−n² (purely weight... q-independent).
- **mechanism:** Deligne's purity says weight-w Frobenius eigenvalues have modulus q^{w/2}. The verified q-independence of ODD power sums (∑η^{2k+1}=−n^{2k}) is the signature of a PUNCTUAL (weight-0, modulus-O(1)) sub-quotient that survives independent of q, while the q-growing even sums are the pure-weight-1 part (modulus √-scale). Separating these via the weight filtration isolates the sup: max|η_b|² = (weight-1 contribution, controlled by ∑η²=q−n averaged) + (punctual contribution, pinned by odd moments). The two-sided frame comes from purity bounding each graded piece exactly by its weight.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/8/5
- **exactness case:** Purity gives EXACT modulus q^{w/2} for pure pieces — a two-sided equality on each Frobenius eigenvalue, not a bound. The odd-moment q-independence forces a weight-0 punctual piece whose contribution is exactly O(1); the residual weight-1 piece has every eigenvalue of modulus exactly √q/m-scaled, and ∑η²=q−n pins the SUM of their squares. The sup is then two-sided framed: floor √(n(q−n)/(q−1)) (Parseval, already in-tree) and the purity ceiling where weight-1 eigenvalues cannot exceed their modulus.
- **wall risk:** Purity alone gives the √q-per-eigenvalue scale but the SUP over m eigenvalues still needs the count/distribution (this is the L²→L∞ √ wall reappearing as 'how many weight-1 eigenvalues align'). Weight filtration sharpens the structure but the alignment within weight-1 is the residual; medium wall. The new content is the punctual/odd decoupling, which IS exact.
- **ranker note:** Weight/purity is correct (pure pieces have exact modulus q^{w/2}) and the punctual/odd-decoupling is a real structural observation, but purity pins per-eigenvalue modulus and the SUM (Ση²=q−n), not how many weight-1 eigenvalues ALIGN — i.e. the L²→L∞ √ wall reappears verbatim as the alignment count; the new content (odd punctual decoupling) wraps just like L5.3.

### [L2.7] Eichler–Selberg / trace formula: express Σ_b f(η_b) over the b-horocycle as a spectral trace, isolating max via the trivial+CM eigenvalue contribution  — score 12
- **construct:** Eichler–Selberg trace formula / Kuznetsov-type spectral expansion: a sum over the m frequencies b (a Hecke orbit on the modular curve / a closed horocycle) of a test function f(η_b) equals a spectral side = Σ over automorphic eigenvalues (Eisenstein continuous spectrum + cusp-form discrete spectrum) plus a geometric/elliptic (CM-point) side. Use f = a high power or a sharp cutoff isolating the maximum.
- **mechanism:** The geometric side's elliptic/CM contributions correspond to the order-3 and order-2 fixed points (ρ=e^{2πi/3}, i) of the modular group; these are EXACTLY computable (CM values, class numbers h(−3),h(−4)). The exact moment data p_r=Σ η_i^r ARE the spectral traces for f=z^r, so the trace formula reorganizes the EXACT power-sum data (E_2,E_3,odd moments) into spectral contributions; the MAXIMUM B is captured as the worst the continuous-spectrum (Eisenstein) tail can be given the FIXED discrete+CM contributions. Subtracting the exactly-known CM/elliptic terms from the exact traces leaves a residual whose sup is pinned by positivity of the spectral measure.
- **quantity:** multiple | **novelty/relevance/feasibility:** 8/6/3
- **exactness case:** Two-sided via spectral POSITIVITY: the trace formula is an equality, and once the discrete/CM eigenvalues are pinned by the exact low moments (E_2,E_3 over-determine them), the continuous-spectrum contribution to high moments is squeezed between the total (exact power sum) and the nonnegative discrete part — giving max|η| both a floor (the dominant eigenvalue must reach the trace) and a ceiling (no eigenvalue can exceed what the trace plus positivity allow). Class-number values h(−3),h(−4) are exact integers, so the CM corner is exact.
- **wall risk:** HIGH. The Eichler–Selberg formula needs the b-sum to genuinely BE a Hecke/automorphic trace — the identification of the m-coset frequency sum with a closed horocycle on a modular curve is the same unproven transfer as A1, and is the deepest assumption. The continuous (Eisenstein) spectrum is precisely the part that is NOT pinned by finitely many moments, so the squeeze may leave an irreducible √log gap — the same near-Ramanujan-up-to-√log ceiling already in-tree (spectral_frame). Highest payoff if it works (unifies all four quantities through one spectral identity) but lowest feasibility; needs Mathlib automorphic machinery that does not exist.
- **ranker note:** Highest unifying payoff and the order-3/order-2 CM-corner (h(-3),h(-4)) is exactly computable, but the b-horocycle<->automorphic-trace transfer is the deepest unproven assumption (same as A1), the Eisenstein continuous spectrum is PRECISELY the part not pinned by finite moments (leaves the irreducible sqrt-log gap), and it needs Mathlib automorphic machinery that does not exist -- lowest feasibility.

### [L4.6] Frame quantum 2-design = unitary t-design; periods as a Gauss-sum spherical code on the flat torus  — score 12
- **construct:** The unimodular Gauss-phase sequence (γ_χ)_{χ∈Ẑ/m} as a point on the flat torus T^{m-1} (the polytorus of unit complex numbers), and P(b)=DFT(γ) as a spherical/frame code. The construct is the theory of spherical codes / the LP (Delsarte linear programming) bound for the torus, and the Bombieri-style Gauss-sum equidistribution making (γ_χ) a 'random unimodular' sequence whose DFT sup-norm is governed by the spherical-code packing on CP^{m-1}.
- **mechanism:** max|P| = sup-norm of the DFT of the unimodular Gauss-phase sequence; B≤√(2n ln(q/n)) ⟺ max|P|≤√(2m ln m) (verified identity in the prompt). Frame view: the m vectors w_b=(χ̄(b))_χ in C^{m-1} are an ORTHOGONAL (DFT) frame; P(b)=⟨γ, w_b⟩. The hard Parseval Σ|P|²=m(m−1) is EXACT (a frame tight-ness identity). The LP/Delsarte bound on the torus, fed the EXACT moments Σ|P|^{2r} = (m·E_r-correction) gives a two-sided polynomial-method bound: the EXTREME value of |⟨γ,w_b⟩| over a tight frame with prescribed moments is the optimum of a Delsarte LP whose dual feasible solution (an explicit positive-definite polynomial) certifies the EXACT max when the LP is tight — and tightness happens precisely at the 'design' points where the Gauss phases equidistribute (Katz/Deligne).
- **quantity:** max|P| | **novelty/relevance/feasibility:** 8/7/5
- **exactness case:** Two-sided: the Delsarte LP gives an upper bound from any dual-feasible polynomial AND the value is EXACT (LP duality, primal=dual) when a matching primal configuration exists. The EXACT moment data (Σ|P|²=m(m−1) hard, plus the higher Σ|P|^{2r} from E_r) are the LP constraints; the order-3 constraint (E_3) is the cubic LP refinement that, by the Levenshtein optimality, makes the bound EXACT for the relevant degree. The flat-torus equidistribution (Gauss phases are jointly equidistributed by Katz) certifies the primal matches the dual — the construct converts the open sup into a finite LP with an explicit exact optimum.
- **wall risk:** Moderate-high: Delsarte LP on the worst sup needs enough moments; cut off at order 3 it gives the Levenshtein bound ~√(3m) not the √(2m ln m) target — the gap to the logarithmic factor is the LP-degree wall (echoes moment no-go). BUT the Gauss-phase equidistribution (a Katz/Deligne INPUT, not the open BGK) certifying LP tightness is a genuinely different mechanism, and the EXACT hard-Parseval m(m−1) primal certificate is in-tree. Avoids the Fermat-corner vacuity because the torus LP is a global average over χ, not the explicit dyadic eval.
- **ranker note:** Delsarte/torus-LP with the in-tree Parseval m(m−1) as a tight-frame primal is a clean two-sided FRAMEWORK, but I verified the order-3 Levenshtein value √(3m) is loose where it bounds (6.0 vs target 7.72) and not even an upper bound in the worst Fermat corner (6.93 vs true 9.27); reaching the log factor needs LP degree ~log m → re-enters the moment ladder, and the higher LP constraints (Σ|P|^{2r}) are the same open even moments, not closed.

### [L3.1] Period polynomial as metaplectic-Frobenius characteristic polynomial: pin B via the real-rooted moment problem on p1..p6  — score 11
- **construct:** The Gauss-period polynomial Psi(X)=prod_{cosets}(X-eta_i), degree m=(q-1)/n. VERIFIED (q=233,n=8): Psi has rational-INTEGER coefficients and is REAL-ROOTED (all eta_i real, max|imag|=0), because the eta_i are the eigenvalues of the Frobenius/regular-rep operator on the rank-m fixed space of the Weil representation restricted to the cyclic group Z/m=F_q^*/mu_n. B=max|eta_b| is exactly the spectral radius (largest-magnitude root) of Psi.
- **mechanism:** Lens 3 supplies the EXACT Newton power sums of Psi: p1=-1, p3=-n^2, p5=-n^4 (the odd-moment law Sum eta^(2k+1)=-n^{2k}, proven via Lam-Leung antipodal pairing of 2^k-th roots), and the EVEN ones p_{2r}=(q*E_r - n^{2r})/n with E1=n, E2=3n^2-3n, E3=15n^3-45n^2+40n (the proven order-3 facts). VERIFIED exactly: p2=q-n=225, p4=4381, p6=116352. With 6 power sums of a real-rooted degree-m poly, the extreme root is squeezed by the classical Markov-Krein / Chebyshev moment-problem two-sided brackets: among ALL real measures with prescribed p1..p6, the support's max is pinned to a finite interval by the Hankel-matrix positivity boundary (the largest root where the order-3 Hankel determinant det[p_{i+j}]_{0<=i,j<=3} degenerates). The two-sided gap shrinks as more p_k are added; order-3 (6 moments) is the first nontrivial squeeze.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/9/6
- **exactness case:** Two-sided, not one-sided: the Markov-Krein theory gives BOTH a lower bound (the principal representation's max atom) AND an upper bound (the largest root of the orthogonal polynomial built from p0..p6, the canonical-representation endpoint). For a degree-m real-rooted poly whose first 2t power sums are FIXED, the max root lies in a closed interval [L_t,U_t] determined by Hankel positivity, and U_t-L_t->0 as t->m/2. At t=3 (our exact data) this is a genuine bracket, and IF the measure is determinate (Carleman) the bracket collapses; the metaplectic rigidity (roots are a single Galois orbit, equally weighted with multiplicity n) is exactly the extra determinacy hypothesis that can collapse it to the exact value.
- **wall risk:** The moment-problem bracket [L_3,U_3] from only 6 moments may still be O(sqrt(m))=O(sqrt(q/n)) wide unless the Galois-orbit/equal-weight rigidity is used; pushing to p_8,p_10 needs E_4,E_5 (not yet proven, beyond order-3). Does NOT hit the BGK or moment-no-go walls directly because it uses a FIXED finite set of exact moments plus real-rootedness rigidity, not the r->log q limit.
- **ranker note:** Real-rootedness verified (max imag 1e-14) and 6 exact power sums confirmed, but directly verified the Markov-Krein bracket from 6 moments does NOT collapse: Gauss nodes sit interior to the support and give no upper bound on the max root; closing requires t~m/2~thousands of moments (E_4..E_thousands unproven) = the moment no-go in disguise.

### [L4.2] Mutually-unbiased-bases flatness: B as coherence inflation over the equiangular optimum  — score 11
- **construct:** The Wootters-Fields / Ivanović MUB construction: in C^n the q characters partition into ~q/n bases that would be mutually unbiased iff all cross-overlaps |⟨u_a,u_b⟩|=1/√n exactly. The Gauss-period frame is precisely the 'character/Heisenberg-Weyl orbit' from which MUBs are built. The relevant space is the union of multiplicative-coset bases in CP^{n-1}; a perfect MUB ⟺ all nonzero periods equimodular = √n.
- **mechanism:** I verified EXACTLY (n=16): the Parseval floor √(n(q−n)/(q−1)) is the equimodular/MUB value (every |η_b| equal), and the prize ceiling √(2n ln(q/n)) = √(2ln(q/n))·(MUB value). So B = (MUB-flat value)×(coherence inflation factor κ), κ²=2ln(q/n) at the conjectured optimum. The MUB defect functional Δ = Σ_b(|η_b|²−avg)² (variance of the overlap distribution) is computable from the FOURTH moment: Δ = q·E_2 − (qn)²/q − ... = q(E_2 − n²(q−1)/q − ...). Since E_2=3n²−3n is EXACT, the overlap-distribution variance is EXACT, pinning the second moment of the deviation from MUB-flatness — and Chebyshev/Selberg two-sided on this exact variance brackets max|η_b|.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/9/7
- **exactness case:** Two-sided: the EXACT fourth moment E_2=3n²−3n gives the EXACT variance of the squared-overlap distribution about its mean (qn). A frame whose overlap distribution has known mean (Parseval) AND known variance (E_2) AND known third cumulant (E_3=15n³−45n²+40n) admits a Selberg/Chebyshev-Cantelli envelope that is two-sided: max ≥ mean+√(variance·(weight)) AND, using all three exact cumulants in a Markov-moment-problem (Hankel determinant) framing, an EXACT extreme-value of the support when the moment sequence is determinate. The MUB-defect is the obstruction to flatness and it is EXACTLY known to order 3.
- **wall risk:** Low-moderate: variance/Chebyshev alone gives only a weak lower bound (one-sided). The two-sidedness needs the Hankel/moment-problem determinacy, which from 3 cumulants determines only a 3-atom envelope — the gap to the true sup is the same L²→L∞ √_A wall. BUT exploiting the ORDER-3 cumulant E_3 (not just E_2) in a 3-point Gauss-quadrature node is genuinely new and gives a sharper-than-Chebyshev EXACT bracket on the discrete overlap distribution.
- **ranker note:** The MUB inflation framing B=(flat value)x sqrt(2 ln(q/n)) is a clean restatement and the exact variance from E2 is real, but Chebyshev/3-cumulant Gauss-quadrature gives only a weak one-sided envelope (the two-sidedness needs determinacy from 3 cumulants = a 3-atom approximation), so the gap to the true sup is exactly the L2->Linf sqrt_A wall.

### [L5.6] Symmetric-power L-function / moment zeta: the sup as the abscissa of a Frobenius-trace Dirichlet series  — score 11
- **construct:** The symmetric/tensor-power L-functions L(Sym^k M, T) = ∏_b det(1 − Frob_b·T^{deg} | Sym^k M)^{-1} of the period local system M, whose logarithmic derivatives generate the power-moment sequence ∑_b η_b^k. By the Grothendieck-Lefschetz trace formula each L(Sym^k,T) is a RATIONAL function with numerator/denominator of explicit degree (Euler-Poincaré), so the moment generating function ∑_k (∑_b η_b^k) T^k is a rational function whose pole structure is étale-computable.
- **mechanism:** The generating function F(T)=∑_b 1/(1−η_b T) = ∑_k (∑_b η_b^k) T^k is, by the trace formula, the logarithmic derivative of a product of L-functions of Sym^k M — each a rational function with poles only at inverse Frobenius eigenvalues. The SUP max|η_b| = 1/(radius of convergence of F) = reciprocal of the modulus of the pole of F nearest 0. The known moments (∑η=−1, ∑η²=q−n, ∑η³=−n², plus the q-independent odd ladder) are the first Taylor coefficients of F; matching them to a rational function of bounded degree (Euler-Poincaré gives the degree from the curve's genus/conductor) PINS the dominant pole and hence the sup exactly.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/7/4
- **exactness case:** By Grothendieck rationality, F(T) is a RATIO of polynomials of degree ≤ D (D = sum of Betti numbers, étale-computable from the conductor of M). A rational function of degree ≤ D is DETERMINED by its first 2D+1 Taylor coefficients. The power-moment sequence supplies exactly these coefficients — and the q-independent odd ones plus Parseval even ones may suffice. Once F is determined as an explicit rational function, its nearest pole (hence the sup) is an exact algebraic number, two-sided. This converts 'estimate a sup' into 'identify a finite-degree rational function from its moments' — a finite, exact problem.
- **wall risk:** The degree D (number of poles) grows with q in the prize regime (m=(q−1)/n distinct periods ⟹ up to m poles), so 'finitely many moments determine F' needs D bounded — true only if the Sym^k L-functions have small conductor (large monodromy makes them generic/large). Risk converges with A1's monodromy-size wall. But the rationality + Euler-Poincaré degree bound is a genuinely exact constraint not yet used; even a partial degree bound brackets the sup.
- **ranker note:** Rationality + Euler-Poincaré degree bound is a genuine exact constraint (F(T) determined by 2D+1 moments) but D ≈ number of distinct periods m grows with q in the prize regime, so 'finitely many moments determine F' is false here unless conductor is small — same large-monodromy wall as L5.1, plus the moments feeding it wrap; converges to A1's monodromy-size wall.

### [L11.4] Order-3 size-biased coupling on the additive-relation tower  — score 11
- **construct:** The size-biased version η_a^* of the period (Stein 1972 / Goldstein-Rinott size-bias coupling for sums). Since η_b=Σ_{y∈μ_n}ψ(by) is a sum of n unimodular characters, build the size-biased sum by re-weighting one summand according to its contribution. The construct lives on the THIRD additive-relation tensor N₀(μ_n,3)={(x,y,z)∈μ_n³:x+y+z=0}, the exact order-3 structure (E_3=15n³-45n²+40n).
- **mechanism:** The size-bias coupling bounds |E f(η)-E f(σZ)| ≤ (third-moment term)·||f''||. For a sum S=ΣY_i the size-biased S* differs from S by replacing one term, and the coupling distance is Σ Var-type. Here the EXACT order-3 data is the lever: the third cumulant κ_3 = P_3 - 3P_1P_2/m + ... is computed from N₀(μ_n,3) which is EXACTLY 15n³-45n²+40n minus diagonal corrections (in-tree). Goldstein-Rinott then gives a Berry-Esseen rate |F_η(t)-Φ(t)| ≤ C·κ_3/σ³ = C·O(n²)/n^{3/2}=O(√n)/σ — wait, this must be normalized: κ_3/σ³ with σ²=n gives the skewness, exactly the small value (-0.5,-0.35,-0.05) measured. The smallness of the skewness (order-3 cancellation) directly bounds the deviation of the period CDF from Gaussian, pinning the extreme value.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/9/4
- **exactness case:** Two-sided because the size-bias coupling produces an EXACT distributional identity E[η·f(η)]=σ²E[f'(η*)] (the Stein characterizing equation), and the order-3 moment E_3=15n³-45n²+40n being EXACTLY the Wick value 15n³ minus lower order means the third cumulant is EXACTLY pinned, not bounded. This forces the CDF into a two-sided band around Gaussian whose width is the explicit skewness, and the max is then the Gaussian edge ±skewness-correction — a genuine two-sided pin of the constant in σ√(2c log m).
- **wall risk:** Berry-Esseen controls the BULK CDF to O(skew/√m) but the EXTREME edge (max over m points) needs control at the 1/m quantile, where Berry-Esseen's additive O(1/√m) error swamps the e^{-t²/2} tail (the classic 'Berry-Esseen is useless for moderate deviations' wall). Must upgrade to a moderate-deviation / Cramér-series version using ALL the exact moments E_r, which re-enters the moment-no-go at high r. Order-3 alone pins the skewness but not the full edge.
- **ranker note:** Size-biased coupling correctly pins the order-3 skewness from N0(mu_n,3), but its own risk is the killer and is real: Berry-Esseen O(skew/sqrt m) bulk error swamps the e^{-t^2/2} tail at the 1/m extreme quantile, so order-3 pins the skewness but never the edge; upgrading to Cramer-series re-enters moment-no-go, and my probe shows E_3/Wick=0.667 (not the assumed exact Wick), so even the skewness anchor is char-p deflated.

### [L12.3] Vinogradov mean-value / decoupling on the unimodular root-number family with the EXACT off-diagonal Jacobi cocycle  — score 11
- **construct:** Vinogradov's Mean Value Theorem / ℓ²-decoupling viewpoint of P(b) = Σ_{χ≠1} χ̄(b)γ_χ as a 1-D exponential sum over the additive group Z/m (frequencies = the m−1 characters), with the 2r-th mean value Σ_b|P(b)|^{2r} = Σ over additive 2r-tuples (χ_1−χ_2+...) of products of γ-phases. The 2r-tuple solution set is the additive-energy variety E_r(Z/m); the phase products are products of Jacobi sums J(χ_i,χ_j)/q (the cocycle), whose magnitudes are EXACTLY √q-controlled (|J|=√q on the off-diagonal).
- **mechanism:** VERIFIED numerically: the dual 4th moment is NOT diagonal-dominated (ratio to diagonal grows like m), so the off-diagonal Jacobi cocycle CARRIES the moment — and it is exactly evaluable: each off-diagonal quadruple contributes a Jacobi sum of EXACT modulus √q with a computable phase. The Vinogradov mean-value of the Gauss-phase family thus has an EXACT closed form = (additive-energy count on Z/m) × (Jacobi-modulus factor). Plug this exact 2r-th mean value into the moment-to-max comparison: max|P|^{2r} ≤ (mean value)/(measure of near-max set), and the off-diagonal exactness turns the usual decoupling INEQUALITY into an EQUALITY because |J|=√q is sharp (no loss). The order-3 mean value (r=3) uses E_3 exactly.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/7/4
- **exactness case:** Two-sided because the Jacobi-sum cocycle modulus |J(χ,ψ)|=√q is an EXACT identity (Davenport–Hasse), not a bound — so the off-diagonal contribution to every mean value is exactly determined in modulus, and the only freedom is the phases, which are constrained by the cocycle relation γ_χγ_ψ = (J/√q)γ_{χψ}. This cocycle is a 1-cochain whose coboundary is exactly the Jacobi phase; solving the cohomology pins the phases, and the resulting P is determined up to the cocycle, giving the exact sup as the operator norm of the cocycle-twisted convolution.
- **wall risk:** The Jacobi cocycle |J|=√q is the SAME non-contracting cocycle flagged in the WALLS ('Jacobi cocycle |J|=√q non-contractive'). Decoupling normally needs the cocycle to LOSE √q per step; here it doesn't, so the standard decoupling iteration stalls. Counter-mechanism: don't iterate — use the cocycle's EXACTNESS (it's an equality) to solve the 1-cohomology of Z/m directly (H¹(Z/m, U(1)) is computable), turning non-contraction from a wall into the rigidity that pins the phase. Order-3 cocycle relations give the first nontrivial cohomology constraint.
- **ranker note:** The Vinogradov/decoupling framing with |J|=sqrt q exact is correctly identified as the non-contractive Jacobi cocycle (a named WALL); the proposed escape (solve H^1(Z/m,U(1)) directly instead of iterating) is creative but H^1 of a cyclic group into U(1) does not pin the actual Gauss-phase values — the phases are arithmetic (Gauss sums), not cohomologically free, so the cocycle rigidity is illusory and it reduces to the flagged non-contraction wall.

### [L16.3] Maslov dequantization of Cauchy–Schwarz: the √-free tropical limit that isolates B as a max-plus eigenvalue  — score 11
- **construct:** Maslov's dequantization: replace the field (R_{>0},+,×) by the max-plus semiring (R∪{−∞}, max, +) via the ħ→0 limit a⊕_ħ b = ħ log(e^{a/ħ}+e^{b/ħ}). The WALL is that B = one Cauchy–Schwarz L²→L∞ √. Dequantize the L²→L∞ inequality: |P|²_{L²}=Parseval=m(m−1) (known exact) and |P|_{L∞}=max|P| become, in the max-plus limit, a TROPICAL spectral problem with NO square root — the √ is the ħ=2 (L²) Maslov scale; the L∞ norm is the ħ→0 (tropical) limit where the Hölder exponent degenerates and the √ disappears. The construct is the family of L^{2r} norms (the moments E_r) as a one-parameter Maslov interpolation between Parseval (r=1) and the tropical sup (r→∞).
- **mechanism:** max|P| = lim_{r→∞} (Σ|P(b)|^{2r})^{1/2r} = lim_{r→∞} (q·E_r/known)^{1/2r}. The Maslov/Legendre-transform structure says: the limit is governed by the SLOPE of the Legendre transform of r ↦ (1/2r)log(qE_r), evaluated at the tropical boundary. The known exact moments E_2, E_3 (and the Wick/Bessel char-0 value (2r−1)!!n^r matching to depth r*~β) give the FIRST TWO derivatives of this Legendre transform at the relevant point. Mechanism: the tropical limit converts the moment sequence into a rate function I(t) (large-deviation) whose support endpoint = log B. The exact E_2, E_3 pin the curvature, and the tropical/Legendre exact endpoint = B if the rate function is determined by finitely many moments (which the order-3 structure might force via a tropical Hankel/moment-determinacy criterion).
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/7/4
- **exactness case:** Two-sided because the Maslov limit is an EQUALITY: max|P| IS the r→∞ limit of the normalized moments, and the Legendre transform endpoint of a moment sequence equals the sup of the support of the underlying measure EXACTLY (Varadhan/Gärtner-Ellis), provided the rate function is steep — which the tropical structure certifies. If the order-3 moment forces the rate function (a 2-parameter exponential family / tropical Hankel determinant condition), the endpoint is pinned exactly, both above and below.
- **wall risk:** Moderate-high: the moment-determinacy of the endpoint from finitely many moments is exactly the MOMENT NO-GO in disguise unless the order-3 (or a tropical Hankel-positivity) datum closes it — the rate function generically needs all moments. The novel escape is the Maslov dequantization viewpoint (the √ is literally the ħ=2 scale, vanishing tropically), which reframes WHY the √ is removable. Risk: re-derives (pE_r)^{1/2r}≥n.
- **ranker note:** The Maslov 'the sqrt is the hbar=2 scale, vanishing tropically' reframing is conceptually elegant and novel. But max|P| = lim (q E_r)^{1/2r} is LITERALLY the moment no-go, and large-deviation endpoint-from-finite-moments is moment-determinacy = the no-go in disguise; the order-3 Hankel-positivity escape is asserted, not shown, and only 2 of the needed moments are even exact.

### [L17.2] Ultraflat L4-defect pin: E_2 IS the fourth-power energy of P  — score 11
- **construct:** The space of unimodular polynomials P(z)=Σ_{χ≠1}γ_χ z^χ on the unit circle / Z/m (Erdos-Littlewood-Kahane ultraflat theory). The L4 norm ‖P‖_4^4 = Σ_c|autocorr(c)|² = aperiodic-correlation energy of γ.
- **mechanism:** For unimodular-coeff polynomials, ‖P‖_2²=m-1 (Parseval, exact) and the merit-factor / L4-L2 defect ‖P‖_4^4−‖P‖_2^4 controls flatness: by the inverse relation, max|P|² ≤ ‖P‖_2²+(‖P‖_4^4−‖P‖_2^4)^{1/2}·(spread). Crucially ‖P‖_4^4 over Z/m equals (1/m)Σ_b|ξ_b|^4 = the SECOND additive-energy moment, which is KNOWN EXACTLY: E_2(μ_n)=3n²−3n maps (via the affine η=(−1+√q P)/m) to a CLOSED-FORM value of ‖P‖_4^4. So the L4 norm of P is not unknown — it is pinned by E_2. Kahane's ultraflat theorem says: a sequence is ultraflat (max|P|=√m(1+o(1))) iff its L4-defect is o(m²); the exact E_2=3n²−3n value, transported, gives the EXACT L4-defect of the Gauss-phase polynomial, hence decides ultraflatness and pins max|P|.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 8/8/6
- **exactness case:** E_2=3n²−3n is a PROVEN EXACT value (not a bound), so ‖P‖_4 is exactly known. If the L4 value equals the Parseval-flat value m²(1+o(1)) (the ultraflat signature), Kahane/Saffari phase theory gives max|P|=√m·(1+o(1)) as a TWO-SIDED asymptotic equality. The defect being exactly computed converts the one-sided Erdos lower bound max|P|≥√m(1+c) into a pin via the matching ultraflat upper bound.
- **wall risk:** E_2=3n²−3n transported gives ‖P‖_4^4 = excess over flat 2n²−n (NOT o(m²) — it is Θ(n²)=Θ(m·?) order), so the Gauss-phase polynomial is NOT ultraflat in the strict Kahane sense; the L4-defect is positive of order n². This pins ‖P‖_4 but the L4→L∞ step (ultraflat needs L4-defect=o, here it is Θ) only gives max|P|≥√m+const, not the √(2 ln m) extreme. Risk: lands the floor not the ceiling. But the EXACT L4 value is genuinely new leverage on the Wiener spectrum (feeds A4).
- **ranker note:** Transporting the exact E_2=3n^2-3n to pin the L4 norm of the Gauss-phase polynomial P is a correct and genuinely new lever on the Wiener spectrum, but my probe confirms the attack's own stated risk decisively: the L4-defect grows as Theta(m^2) (def/m^2: 0.5->22->368) and maxP/sqrt(m) grows (0.7->2.5), so P is NOT ultraflat in the Kahane sense, the L4->Linf step is exactly wall W_A, it lands the floor not the ceiling, and the E_2-transport even breaks at the prize-relevant Fermat corners.

### [L2.1] Theta-sum limit law: pin B as the essential-sup of the Demirci Akarsu–Marklof automorphic functional Theta on the metaplectic horocycle  — score 10
- **construct:** The theta sum Θ(x,φ) = Σ_{n} e(½ n² x + n y) and its limiting distribution as an SL(2,ℤ)-automorphic function Θ_χ on the universal Jacobi group Γ_θ⋉ℝ² (Jurkat–van Horne, Marklof, Cellarosi–Marklof); incomplete Gauss sums embed into this via Demirci Akarsu–Marklof (arXiv:1207.1607), whose value distribution is the push-forward of Haar measure on a horocycle in SL(2,ℤ)\SL(2,ℝ)⋉ℝ² under Θ.
- **mechanism:** η_b/√n equidistributes (as b ranges over the m cosets, i.e. over the horocycle parametrized by b) according to the SAME universal automorphic law Θ that governs incomplete Gauss/Weyl sums. B/√n is then the supremum of |Θ| sampled along a specific closed horocycle of length m. Because Θ is a FIXED automorphic function (independent of n,q), its sup over a length-m piece of an equidistributing horocycle is pinned by the function's own growth toward the cusp: |Θ(z)| ≲ (Im z)^{-1/4} near the cusp, and the deepest cusp excursion reached by a length-m horocycle is Im z ≈ 1/m, giving sup|Θ| ≈ m^{1/4} — but the RIGID cusp-coefficient (the leading theta Fourier coefficient = 1) fixes the CONSTANT, turning the ~m^{1/4} into an exact asymptotic B = (1+o(1))·n^{1/2}·(c·log(q/n))^{1/4} with c computable from the cusp expansion.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/8/3
- **exactness case:** Two-sided because equidistribution gives BOTH a matching lower bound (the horocycle MUST visit every cusp depth down to Im z≈1/m infinitely often as m→∞, forcing sup ≥ the cusp growth) AND the upper bound (Θ is bounded by its cusp growth everywhere). The limit LAW is an equality of distributions, not an inequality, so the extreme-value scaling is two-sided up to the (computable, n-independent) automorphic constant. The probe's stable tail-constant C≈0.66–0.74 is exactly this universal cusp constant.
- **wall risk:** MEDIUM-HIGH. The Demirci Akarsu–Marklof law is for ONE quadratic Weyl sum length growing along a single x; transferring to the FINITE-FIELD subgroup family (m cosets, fixed q) requires that the b-horocycle equidistributes in the metaplectic homogeneous space — proven only in the archimedean limit, not for fixed q. Could collapse to the same √log ceiling as the Gaussian model (W_moment) if the horocycle fails to reach Im z≈1/m. Sidesteps the L²→L∞ √ wall by using the EXACT tail SHAPE rather than a single Cauchy–Schwarz step.
- **ranker note:** The Demirci Akarsu-Marklof theta-sum paper is already in-tree and triaged as 'supports the large-value/statistical lens, NOT a worst-case uniform bound'; transfer to the fixed-q finite-field cosets is proven only in the archimedean limit, and the m^{1/4} cusp-growth prediction contradicts the measured sqrt(log) scale — internally inconsistent on the very quantity it claims to pin.

### [L4.3] Equiangular tight frame / difference-set obstruction via the absolute bound N ≤ d²  — score 10
- **construct:** The q nonzero periods seen as a putative equiangular tight frame (ETF) in C^{n} (or in the m-dim character space C^{Ẑ/m}). The Gauss phases γ_χ=G(χ)/√q form a UNIMODULAR difference-set-like sequence on Z/m; P(b)=Σ_χ χ̄(b)γ_χ is the difference-set / Singer-cycle character sum. The relevant construct is the ETF-from-difference-set theory (Strohmer-Heath, Xia-Zhou-Giannakis), and Gerzon's ABSOLUTE BOUND.
- **mechanism:** An ETF in C^d has at most N≤d² vectors and ALL pairwise coherences equal to the Welch value √((N−d)/(d(N−1))). The periods CANNOT form an ETF (the q=N character-sum overlaps are not all equal — odd moments Σηᵢ^{2k+1}=−n^{2k}≠0 prove the overlap distribution is non-symmetric). The ABSOLUTE bound N≤d² is VIOLATED here (q≫n²), forcing the configuration into the 'mixed' regime where the spread of coherences is FORCED by the Gerzon defect. The exact non-ETF-ness measured by the third moment E_3 (which an ETF would force to a specific value) pins how far max coherence must exceed the Welch floor: max|η_b|² − Welch_value = exact function of (E_3 − ETF-predicted-E_3).
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 7/6/5
- **exactness case:** Two-sided: an ETF has EXACTLY one coherence value; the deviation of the actual third frame moment E_3=15n³−45n²+40n from the ETF-forced value Σ over an equiangular config is an EXACT measure of coherence-spread. Since the maximum coherence in a non-equiangular frame with prescribed first three moments is the largest root of an exact cubic (the 3-moment truncated moment problem / Stieltjes), and the odd-moment data Σηᵢ^{2k+1}=−n^{2k} fixes the asymmetry, the support's right endpoint is an algebraic number determined by the cubic — an exact value, not a bound.
- **wall risk:** Moderate-high: the truncated moment problem with 3 moments gives an envelope (Markov bound) whose right endpoint is exact ONLY for the extremal measure, which need not be the actual period measure — the gap is the determinacy wall. Risk it collapses to E_3-only info which is order-3 (good per the prompt's emphasis) but still finitely many moments versus the r≈ln q the prize needs. Mitigated by the odd-moment EXACT data forcing asymmetry that pure ETF theory ignores.
- **ranker note:** Correct that the periods cannot be an ETF (odd moments != 0 prove asymmetry) and the absolute bound is violated, but the 'max coherence = largest root of an exact cubic' is the same 3-moment truncated moment problem already shown to give interior nodes with no upper bound; the extremal measure of the 3-moment problem is not the actual period measure (determinacy wall).

### [L6.6] Davenport-Hasse lifting tower pins B(p) by exact recursion across the p^s-tower  — score 10
- **construct:** The Davenport-Hasse relation g_s(chi compose N) = (-1)^{s-1} g_1(chi)^s relating Gauss sums over F_{p^s} to those over F_p (lifting via the norm N: F_{p^s}^x -> F_p^x), giving an EXACT multiplicative recursion on the periods up the p-power tower, paired with the probe-confirmed odd-moment rigidity T_{2k+1}=-n^{2k+1}.
- **mechanism:** Davenport-Hasse says the Gauss sum over the degree-s extension is EXACTLY (-1)^{s-1} times the s-th power of the base Gauss sum. So the period polynomial over F_{p^s} has roots that are EXACT algebraic functions (s-th-power-related) of the F_p periods. This gives a self-similar TOWER: B(p^s) is determined by B(p) through an exact relation, and the zeta-function (rationality, Dwork) forces the eta_b to be the reciprocal roots of an EXACT rational function across s. Mechanism for exactness: the L-function L(T) = prod(1 - eta_b T) is RATIONAL (Dwork rationality) with the eta_b as its inverse roots; Davenport-Hasse fixes the numerator EXACTLY, and the functional equation of the L-function (Poincare duality / Weil) gives a TWO-SIDED constraint |eta_b|=sqrt-magnitudes pairing reciprocal roots, pinning the spectral radius B.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 7/6/4
- **exactness case:** Two-sided via the L-function FUNCTIONAL EQUATION (Weil/Dwork): rationality makes the eta_b the exact reciprocal roots of an explicit rational function, and the functional equation pairs roots eta_b <-> q/eta_b' forcing a reflection that pins the spectral radius (max|eta_b|) symmetrically from above (Weil bound on the variety) and below (the functional-equation reflection forces a root AT the extreme, no slack). Davenport-Hasse fixes the recursion exactly (an equality, not bound).
- **wall risk:** The Weil/functional-equation route gives |eta_b| <= sqrt(q)-type bounds that are exactly the sqrt-cancellation the prize is ABOUT - so naively it re-hits the Weil/sqrt(q) wall (the |J|=sqrt(q) non-contraction). The reflection forces a root near the Weil bound only for the FULL group, not the subgroup-restricted period. Davenport-Hasse and Dwork rationality are not in Mathlib. High wall risk; the genuinely new piece is the EXACT tower recursion which, with odd-moment rigidity, might close a self-similar fixed-point equation for B independent of the Weil bound.
- **ranker note:** Davenport-Hasse g_s = +-g_1^s is a genuine exact recursion and the L-function rationality framing is structurally serious, but DH relates Gauss sums across FIELD EXTENSIONS, not across the SUBGROUP mu_n at fixed q, so it gives no self-similar fixed-point equation for the prize B; the functional-equation reflection pins roots at the FULL-GROUP Weil scale sqrt(q) (I measured B/Paley = 0.24 -> 0.04, B nowhere near Weil), re-hitting W1. Davenport-Hasse and Dwork rationality are also absent from Mathlib. Low feasibility, near-wall.

### [L10.6] Polynomial-method partition-rank of the antipodal-excess tensor: the n^2-2n excess in E_2 is rank-2, and the full B is the partition-rank generating function whose dyadic 2-adic recursion is closed  — score 10
- **construct:** The antipodal-excess tensor: probe PROVES E_2 = (Sidon 2n^2-n) + (antipodal excess n^2-2n) EXACTLY (n=4,8,16 -> 36=28+8, 168=120+48, 720=496+224). Build the partition-rank (Naslund's higher-order generalization of slice-rank) of the excess part of T_{2r}, which is generated by the antipodal involution x->-x. Over dyadic mu_n this involution generates a 2-adic tower (mu_2 < mu_4 < ... < mu_n), matching the in-tree QuartetTowerLaw 4-adic recursion.
- **mechanism:** The trivial/Sidon part of every moment is the pure-Wick (2r-1)!!n^r baseline (the diagonal); the ANOMALY = partition-rank of the residual antipodal-generated tensor. Probe pins the order-2 anomaly EXACTLY: excess(E_2) = n^2-2n = n(n-2), a rank-2 partition tensor (the antipodal pairing is a single involution = partition into 2-blocks). The 2-adic self-similarity (E_2 excess n(n-2), and the e2=0 width-4 count (n^2-3n)/4 both being clean quadratics in n) suggests the anomaly partition-rank satisfies a CLOSED 2-adic recursion R(mu_{2n}) = f(R(mu_n)) via the squaring map h_{2j}(T)=h_j(T^2) (complete-homogeneous halving, already in-tree per memory). Solving the recursion pins the anomaly generating function exactly, hence B = sqrt(baseline + anomaly) two-sided.
- **quantity:** multiple | **novelty/relevance/feasibility:** 9/8/5
- **exactness case:** Two-sided at order 2 EXACTLY: E_2 anomaly = n(n-2), a verified integer polynomial, partition-rank 2. The 2-adic recursion (if it closes) gives every order's anomaly as an exact polynomial in n via the squaring-map functional equation — exact because partition-rank is an integer invariant and the recursion is a finite linear-algebra step per level (no estimate). The max B is then sqrt of an exactly-computed moment-generating function evaluated at its saddle, two-sided. Exploits E_2, E_3 (order-3) AND the dyadic 2-adic structure simultaneously — the only attack that uses the 2-power structure of n=2^mu as the SOURCE of exactness rather than an obstruction.
- **wall risk:** Partition-rank (Naslund) not in Mathlib; the 2-adic recursion may NOT close at higher orders (the order-3 even-part anomaly may break self-similarity, per the memory note that antipodal-quotient escape recurses down the 2-adic tower and likely fails). Moment no-go still requires summing the recursion to r~ln q. Medium-high wall risk, but it is the boldest exploitation of the 2-power structure and the order-2/order-3 exact data as recursion anchors.
- **ranker note:** Boldest use of the 2-power structure and partition-rank is a genuinely new construct, but the load-bearing claim that the 2-adic anomaly recursion CLOSES at higher orders is directly contradicted in-tree (CumulantDyadicDescent: cross-resonance fraction jumps 0 to 0.73-0.86 from r=1 to r=2, dominating, = BCHKS 1.12 wall); E_2 excess n(n-2) is real but partition-rank determinacy at r>=3 fails exactly where the descent stops being exact (only at order 2).

### [L14.3] Gauss-Milgram / determinant-of-S identity: exact Galois norm ∏η_i pins B via the period resultant  — score 10
- **construct:** Gauss-Milgram sum and det(S) in a modular tensor category: the product of all quantum dimensions / S-matrix determinant has an exact value (a power of the total dimension times a root of unity). Translate to the period polynomial Ψ_n(x) = ∏_i (x−η_i) whose constant term (−1)^m ∏η_i is the exact Galois norm of a Gauss period — an explicit cyclotomic resultant / integer (verified: +1 for n=8; 591872, 6561 for n=16). In-tree substrate: CyclotomicResultantBound, AdditiveEnergyResultantProduct.
- **mechanism:** The exact norm ∏|η_i| is the geometric mean to the m-th power, giving B ≥ (∏|η_i|)^{1/m} (AM-GM floor). More powerfully, pair it with the EXACT Parseval second moment Σ|η_i|² = p−n: by the log-convexity (Schur/Maclaurin) of power means, knowing both the product (degree-0 / geometric mean) and the sum-of-squares (degree-2) of m nonnegative reals pins the spread, hence sandwiches the maximum. The norm being an explicit resultant of cyclotomics (computable as a discriminant-like quantity in Z[ζ]) makes the floor exact, and TQFT's det(S) congruences predict its p-adic valuation.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/7/6
- **exactness case:** Both the geometric mean (∏η_i, exact integer norm) and the quadratic mean (√((p−n)/m), exact via Parseval) of the m moduli are EXACTLY known. A maximum of m nonnegative reals with fixed geometric mean G and quadratic mean Q is two-sided bounded: B ≤ Q·√(m) trivially but, more sharply, the constraint that the OTHER m−1 values cannot collapse below G/B^{1/(m-1)}-type floors yields an upper bound, while AM-GM gives the matching lower bound. This is genuinely two-sided because it uses degree-0 AND degree-2 exact data simultaneously.
- **wall risk:** AM-GM floor is weak (∏ can be ±1, giving only B≥1). The upper bound from product+sum-of-squares is loose unless many periods are forced bounded away from 0; in the prize regime ∏ can be small so the geometric mean is uninformative for the MAX. Strongest as a lower-half / floor tool, weaker as an upper bound.
- **ranker note:** Combining exact geometric mean (norm = cyclotomic resultant, in-tree) with exact quadratic mean (Parseval p-n) for a two-sided power-mean squeeze is methodologically sound and uses degree-0 AND degree-2 data, but as its own risk concedes the product can be +-1 (B>=1 trivial floor) and the upper half is loose unless periods are forced bounded-away-from-0 — which is the equidistribution wall; strongest only as a weak floor tool, not exactness.

### [L15.2] Field discriminant Delta = +-p^(m-1) as an exact Vandermonde of the periods  — score 10
- **construct:** The Gaussian-period subfield K of degree m=(q-1)/n over Q has field discriminant exactly Delta_K = +-p^(m-1) (Maxson-Roettger 'Field discriminants of cyclotomic period equations', arXiv:1912.06921; sign by (m-1) mod 4 and parity of n). disc(period polynomial) = prod_{i<j}(eta_i-eta_j)^2 = Vandermonde^2; for the monogenic/dyadic case it equals the field discriminant up to a known index-square.
- **mechanism:** disc = prod_{i<j}|eta_i - eta_j|^2 = +-p^(m-1) is an EXACT equation linking ALL pairwise differences of the periods. Combined with the exact Newton-power-sums (Sigma eta_b = -1, Sigma eta_b^2 from E_2=3n^2-3n, Sigma eta_b^3 from E_3, and the odd-moment identities Sigma eta_i^{2k+1} = -n^{2k}), the period polynomial Psi(X) is determined coefficient-by-coefficient. The max-modulus root of an EXPLICITLY-determined integer polynomial Psi of known degree m and known discriminant +-p^(m-1) is a computable algebraic number; the sup over b is exactly max_root|Psi|.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/7/5
- **exactness case:** Two-sided: disc = +-p^(m-1) is an exact equality (algebraic number theory), as is each power-sum from the known E_r data. m exact symmetric functions (power sums p_1..p_m via Newton from E_1..E_r and the discriminant as the final resolvent) determine Psi exactly, hence its root multiset exactly. The max root is then a two-sided pinned value -- not a Johnson-type one-sided estimate. The discriminant being a single prime power is an unusually rigid constraint (it forces the difference-product to have NO other prime factors).
- **wall risk:** Medium: the power-sums are only KNOWN exactly up to r=3 (E_2,E_3 + odd moments); beyond r=3 we hit the open char-p energy (moment no-go region). So disc + 3 power sums under-determines Psi for m>4. But for the SMALL-m provable regime (n near sqrt q) this CLOSES exactly, and the discriminant gives one extra high-degree constraint the moment method does not. Emphasize ORDER-3: E_3 exact gives p_3, the third elementary symmetric e_3, which with disc constrains the cubic resolvent.
- **ranker note:** I numerically REFUTED the load-bearing premise: disc(period polynomial) = p^(m-1) * index^2 (measured 4=2^2, 729=27^2, huge at p=97), so Maxson-Roettger's p^(m-1) is only the FIELD discriminant; the period order is non-maximal and the index is unknown/large in the prize regime, so disc gives no exact extra constraint. With only p1,p2 universally exact (p3 non-universal) this under-determines Psi for m>3.

### [L3.3] Schur orthogonality of the Weil representation: P(b) as a matrix coefficient, |P| bounded by Frobenius norm via the Weil rep's known dimension  — score 9
- **construct:** The Weil (oscillator) representation W of SL_2(F_q) (or its metaplectic double cover when q=1 mod 4 fails), acting on functions on F_q. The Gauss sum gamma_chi = G(chi)/sqrt q is the eigenvalue/matrix-coefficient of the Weil element w=[[0,-1],[1,0]] (the 'Fourier transform' / metaplectic S-operator) on the chi-isotypic line. P(b)=Sum_{chi!=1}chibar(b)gamma_chi is then a single matrix coefficient <pi(w) v_b, v_b> of W in the chi-decomposition.
- **mechanism:** Schur orthogonality / the Weil representation's explicit character formula (Howe, the dimensions of the +/- pieces are (q+1)/2 and (q-1)/2). The Frobenius-Schur indicator and the known character chi_W(w)=Weil index pin Sum_chi |gamma_chi|^2 = m exactly (each |gamma_chi|=1). The matrix coefficient <pi(w)v_b,v_b> is bounded by ||pi(w)v_b||*||v_b|| (Cauchy-Schwarz inside the unitary rep) — but UNITARITY of W means ||pi(w)v_b||=||v_b||, turning the L2->Linf step into an EQUALITY constraint via the rep's unitary structure, with the defect controlled by how far v_b is from a w-eigenvector.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/7/4
- **exactness case:** Two-sided via the unitarity of the Weil representation: |<pi(w)v,v>| <= ||v||^2 (upper) AND |<pi(w)v,v>| >= |Re Tr(pi(w)P_v)| where P_v is the rank-1 projector (lower, from the spectral decomposition of pi(w) which has KNOWN eigenvalues = the Weil indices, a finite explicit list of 4th/8th roots of unity times signs). The exact eigenvalue multiplicities of pi(w) (the metaplectic S-operator has eigenvalues among {1,i,-1,-i} with multiplicities determined by Gauss sums) pin <pi(w)v_b,v_b> to a SUM of unimodular eigenvalues weighted by |projection|^2, a convex combination whose extreme value (when v_b aligns with one eigenspace) is EXACTLY computable.
- **wall risk:** Mathlib has no Weil/oscillator representation; building W of SL_2(F_q) from scratch is heavy. The eigenvalue multiplicities of pi(w) are known but the alignment of v_b with eigenspaces is exactly the equidistribution (Deligne) content — risk of hitting the 'eval set = full character group, rigid orthogonal DFT' wall noted in prior R3 work. Best used to PIN the antipodal-symmetric piece exactly, feeding A2.
- **ranker note:** The matrix-coefficient/unitarity reframing is legitimate but |<pi(w)v,v>|<=||v||^2 is exactly Cauchy-Schwarz = the L2->Linf sqrt_A wall renamed; the lower companion needs v_b's alignment with pi(w)-eigenspaces which is the Deligne-equidistribution content (the 'rigid orthogonal DFT' O2 wall), and Mathlib has no Weil/oscillator representation.

### [L7.3] Real-rooted integer period polynomial: Newton-inequality log-concavity + integrality squeeze  — score 9
- **construct:** The period polynomial Psi(X) in Z[X], monic of degree m, REAL-ROOTED for dyadic mu_n (all periods real, VERIFIED in /tmp/period_poly.py), with e_1=1, e_2, e_3 the elementary symmetric functions determined exactly by p1=-1,p2=q-n,p3=-n^2 via Newton's identities. Newton's inequalities (real-rootedness => normalized esym log-concave) plus integrality of the coefficients.
- **mechanism:** For a real-rooted monic integer polynomial, Newton's inequalities e_{k-1}e_{k+1} <= e_k^2 constrain the root configuration far more tightly than power sums alone. With e_1=1, e_2=(p1^2-p2)/2=(1-(q-n))/2, e_3 = (Newton from p1,p2,p3) all EXACT integers in the prize regime, the log-concavity chain bounds the spread of roots. The max root r then satisfies Lagrange/Cauchy root bounds in terms of (e_1,e_2,e_3) that, combined with the INTEGRALITY of all e_k (roots are algebraic integers, sum and products integral), force r into a discrete set. The squeeze: r is an algebraic integer whose conjugates' power sums are pinned to order 3, and Newton-concavity forbids the 'one huge root' configuration the trivial bound allows.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 7/7/6
- **exactness case:** Two-sided because real-rootedness makes the moment problem DETERMINATE up to the unknown higher e_k, and Newton's inequalities are equalities exactly at the extremal (clustered) configuration. The integrality of e_2,e_3 (exact in prize regime) means the admissible root patterns are isolated points, not a continuum — so a sufficiently tight Newton-concavity squeeze pins B to a single value rather than a range. Mechanism isolates the exact value by ruling out all but one integer-coefficient real-rooted completion.
- **wall risk:** Risk: Newton's inequalities use only e_1,e_2,e_3 and may not isolate a unique configuration for large m (the higher e_k are free). Escapes W1/W2 by leveraging integrality+real-rootedness (structure the moment no-go ignores). Wall risk is that the unknown e_4..e_m re-open the range; mitigated by also fixing e_4 via the conditional p4 (E_2). Mathlib has Polynomial.esymm and Newton-identity infrastructure (ListInteriorT3 already proves the order-3 Newton identity), so formalizable.
- **ranker note:** Real-rootedness is confirmed (max|Im eta| ~ 1e-10) and Newton inequalities + integrality are legitimate extra structure the moment no-go ignores, but fixing only e1,e2,e3 (or e4) leaves 4093 free DOF in a degree-4096 polynomial; verified that 3-4 symmetric functions cannot isolate the max root of an m~2^126 spectrum. The higher e_k re-open the full range. Effectively the moment no-go (W2) wearing a symmetric-function coat; integrality does not discretize finely enough at prize m.

### [L7.6] Three-atom Gauss quadrature determinacy at the smallest-m dyadic prizes, then 2-adic lift  — score 9
- **construct:** Gaussian quadrature / the principal representation of a moment sequence: with EXACTLY the rigid moments p1,p2,p3 known, the unique 2-point (lower principal) and the (m,e_1,e_2,e_3)-constrained representations. The construct is the orthogonal-polynomial recursion (Jacobi matrix / tridiagonal Hankel operator) whose eigenvalues are the periods.
- **mechanism:** Build the truncated Jacobi (tridiagonal) matrix J_2 from the rigid moments p0=m, p1=-1, p2=q-n, p3=-n^2: its entries (recurrence coefficients alpha_0,beta_1,alpha_1) are exact rational functions of these, and its largest eigenvalue is the Gauss-quadrature node = the EXACT lower principal representation of B^2. Because the period operator is a genuine finite tridiagonal-after-orthogonalization, the largest period is bracketed between the largest Gauss node (from p1,p2,p3) and the largest Gauss-Radau node (fixing an endpoint). When m is small (e.g. m=9,11,12,16 dyadic prizes I tabulated), only a few quadrature nodes exist and the bracket is tight; the value then LIFTS up the 2-adic tower via the proven TowerMonotonicity (ε_mca monotone up the tower) to the full prize regime.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 6/7/6
- **exactness case:** Two-sided by the Gauss vs Gauss-Radau/Lobatto node interlacing: the largest Gauss node (interior principal rep, lower bound on the max atom) and the largest Radau node (boundary principal rep, upper bound) sandwich the true max period, and these two principal representations are EXACTLY computable from p1..p3 (interior) and p1..p4 (boundary). When the two nodes coincide (determinacy at m matching the moment count) the pin is exact. The interlacing is a classical two-sided theorem (Chihara), not a one-sided estimate.
- **wall risk:** Moderate: the Gauss/Radau bracket width grows with m, so for the deep prize regime (m=n^{beta-1} huge) the bracket loosens unless higher rigid moments are added (only p1,p2,p3 are unconditionally rigid; p4 needs E_2). The 2-adic lift via TowerMonotonicity is the key escape but transfers monotonicity of epsMCA, not directly of B — needs the proven LD<->MCA bridge. Escapes W1 by being a determinacy argument. Formalizable on existing Hankel/quadrature-free reformulation since it reduces to the same e_1,e_2,e_3 data as A3.
- **ranker note:** Gauss/Gauss-Radau node interlacing is a legitimate classical two-sided theorem and the Jacobi-matrix object is exact, but it shares L7.1's fatal flaw: from only p1..p3 (unconditionally rigid) the bracket width GROWS with m (verified the 2-pt node undershoots B by 60-75% at prize m), so the deep prize regime m=n^(beta-1) loosens hopelessly. The escape (2-adic lift via TowerMonotonicity) transfers epsMCA monotonicity, NOT B monotonicity, so the bridge is unproven. Same moment-determinacy wall (W2) as L7.1.

### [L13.1] Period polynomial as integer-symmetric reciprocal: Newton-closure of Ψ from E_2,E_3 + B = max-root-modulus  — score 9
- **construct:** The Gauss-period polynomial Ψ(T)=∏_{cosets}(T−η_j) ∈ ℤ[T] (degree m=(q−1)/n), whose power sums p_r=Σ_j η_j^r=E_r are the SAME moments E_r(μ_n) as the unified target, and whose elementary symmetric coeffs e_1,…,e_m are determined from p_r by Newton's identities (Mathlib MvPolynomial.Symmetric.NewtonIdentities). B = max_b|η_b| is the maximum modulus of a ROOT of Ψ.
- **mechanism:** Each η_j is an algebraic integer; Ψ∈ℤ[T] with e_1=−1 (always, =−Σμ_n shift), e_2=(p_1²−p_2)/2 = (1−(3n²−3n))/2, e_3=(p_1³−3p_1p_2+2p_3)/6 from the KNOWN E_2=3n²−3n, E_3=15n³−45n²+40n. The max root modulus B is then squeezed two-sidedly by the full Newton-symmetric data: lower by p_2/p_1-type reverse-Markov (Ση_j²/Ση_j gives a mean-square floor), upper by the Cauchy/Lagrange root bound max|root| ≤ 1+max_i|e_i/e_m| applied to the truncated-symmetric profile. Pinning ALL e_i (not just e_1,e_2,e_3) via a closed Newton recursion in the period-polynomial coefficients (the cyclotomic period polynomial of Gauss/Kummer) closes B exactly.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 7/9/5
- **exactness case:** Two-sided because the max-root-modulus of a fixed integer polynomial is pinned EXACTLY by its full coefficient vector (resultant/discriminant of Ψ with the unit circle), not bounded: B is literally an algebraic number with minimal polynomial dividing Ψ. The known low-order E_r give the leading symmetric coefficients exactly; the lower bound comes from p_2 = Σ|η_j|² = m·(avg) forcing some |η_j|² ≥ E_2/m (mean), and equality structure (all-but-one periods small) is detectable from e_3.
- **wall risk:** The full coefficient vector e_4,…,e_m needs E_4,…,E_m which are NOT all known (only E_2,E_3 + odd-moment Σηᵢ^{2k+1}=−n^{2k}); the moment no-go ((pE_r)^{1/2r}≥n) bounds the upper Newton coefficients but the max-root may sit in the unknown tail. Mitigated by: the odd-moment closed form pins HALF the Newton recursion (every odd p_r=−n^{r−1}) which is genuinely new leverage not yet exploited.
- **ranker note:** Same Newton-from-low-moments engine as L13.2 but routed through the period polynomial coefficients e_1,e_2,e_3 only; my probe shows these are near-invariant across primes where B genuinely varies, so 'pinning ALL e_i via a closed Newton recursion' is exactly the unavailable high-order tail = cumulant wall; the equivariance-group-order division to extract max-from-average just re-encodes moment-no-go. Weak.

### [L16.5] Newton–Okounkov body of the order-3 power-sum filtration: B as the exact volume/vertex of a graded valuation  — score 9
- **construct:** A Newton–Okounkov body built from the order-3 (p_1,p_2,p_3) moment filtration. The in-tree recoordinatization threeSymmetric_count_eq_moment_count makes (e_1,e_2,e_3)↔(p_1,p_2,p_3) a fixed unitriangular change of coordinates. Define a rank-3 valuation v: F[η_0..η_{f-1}]^{Σ_f} → Z³ on the ring of symmetric functions of the periods, sending a symmetric function to its leading (e_1,e_2,e_3)-degree. The Newton–Okounkov body Δ(v) is the closure of the normalized value semigroup. The EXACT data E_2 (p_2), E_3 (p_3) and the odd-moment law are the lattice points/generators of this value semigroup.
- **mechanism:** Newton–Okounkov theory (Kaveh–Khovanskii, Lazarsfeld–Mustață): the spectral radius B = max|η_j| of the period polynomial equals the support function of Δ(v) evaluated in the 'trace' direction (the linear functional reading off the dominant root), and the VOLUME of Δ(v) computes the asymptotic dimension = the period count f. Because the order-3 filtration is FINITELY generated (unitriangular, generators p_1,p_2,p_3 known exactly), Δ(v) is a rational polytope whose vertices are pinned by E_2,E_3 and the odd-moment identity. The support function in the trace direction at a vertex = B exactly. This exploits that the order-3 graded piece is the FIRST place the periods' modulus distribution is rigidly constrained (p_1=−1, p_2,p_3 fixed) before higher moments (the no-go regime) enter.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/6/4
- **exactness case:** Two-sided because the Newton–Okounkov body's support function is an EXACT equality (support function = sup of a linear functional over the body, attained at a vertex), and for a finitely-generated rank-3 valuation the body is a concrete polytope with vertices determined by the three known moments. B = support-function value is therefore an exact number, not a bound, IF the dominant-root direction is one of the body's actual vertices (which the order-3 rigidity + odd-moment antipodal structure can certify). The volume identity gives an independent cross-check (=f), pinning consistency.
- **wall risk:** Moderate-high: Newton–Okounkov bodies for non-toric/non-finitely-generated valuations need not be polytopes; truncating to order-3 may only give a sub-body (lower bound on B) unless the higher graded pieces are forced by the period recursion. The honest risk is that the order-3 truncation captures only the first-order shape and the exact vertex needs order ~β=log q (moment no-go). Genuinely novel construct; emphasizes the requested order-3 structure directly.
- **ranker note:** Bold and maximally novel (Newton-Okounkov bodies are completely untried here and the order-3 unitriangular filtration is real), but there is a category mismatch: N-O theory computes VOLUME = asymptotic dimension and counts numbers of roots/solutions, not the MAGNITUDE of a single dominant root, so no literature supports the support-function = B claim; the dominant-root vertex needs order ~beta = log q (the moment no-go), so order-3 truncation gives at best a sub-body lower bound.

### [L7.1] Markov-Krein 4-moment Hankel pin via the rigid order-3 power sum P3=-n^2 plus E_2  — score 8
- **construct:** The classical truncated Hamburger/Markov moment problem on the real line: the m real Gaussian periods {eta_k = eta_b for b a coset rep} form an m-atom measure mu on R whose power sums p_r = sum_k eta_k^r are EXACTLY known up to order 3 (and 4 conditionally) in closed form. Pin max|eta_k| as the maximal atom location compatible with the Markov-Krein principal representation, using the Hankel-PSD feasibility cone H_r = [[m,p1,p2],[p1,p2,p3],[p2,p3,p4]] >= 0.
- **mechanism:** Translate B^2 = max_b|eta_b|^2 to the maximal root r of the real-rooted period polynomial Psi(X)=prod_k(X-eta_k). For dyadic mu_n in the prize regime I VERIFIED (probe /tmp/exact_pin.py, /tmp/four_moment.py) the rigid exact data p1=-1, p2=q-n, p3=-n^2 (since the zero-sum-triple count T3=0 off the Fermat corners), and p4=q(3n-3)-n^3 whenever E_2=3n^2-3n holds (large m=(q-1)/n, true in prize regime q=n^beta). Placing one atom at r and demanding the remaining (m-1)-atom Hankel be PSD with these four prescribed moments gives an extremal r. Measured: B<= 5.2..10.9 vs actual B=4.8..9.8 (within 10-25%), DRAMATICALLY below the Samuelson/L^2->L^inf root-q wall (9-24).
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/9/6
- **exactness case:** The Markov-Krein bound is a SUP over all measures with the given moments, hence is attained by an extremal (principal) measure; it is a genuine two-sided pin when m equals the number of constrained moments (Gauss-quadrature determinacy). For larger m the upper is exact-extremal and the matching lower comes from the same cone evaluated at the lower principal representation; together they bracket B between the two principal representations of the SAME 4-moment data. This is two-sided (not a one-sided/Johnson bound) because the moment cone is symmetric: both the largest and smallest feasible atom are pinned, and B = max of the two by the side-law (L7-A2).
- **wall risk:** Partially hits W2 (moment no-go): with the 4th moment alone the upper is exact but not yet matching the lower without extra structure. Escapes W1 (root-q) decisively because it uses the rigid CLOSED FORMS as hard equality constraints, not asymptotic moment growth. Residual: closing the bracket needs either real-rootedness of Psi (integrality, A3) or p5/p6 (which I verified FAIL in the prize regime), so the pin is a narrow bracket, not yet a single value.
- **ranker note:** Markov-Krein with rigid p1..p4 is a principled two-sided idea, but I REFUTED the headline numbers: the 2-atom Gauss node from p0..p3 UNDERSHOOTS the true extreme by 60-75% (2.8 vs 7.3, 5.7 vs 23.0) with the gap WIDENING in n. A 2-atom (4-moment) fit cannot bracket an m=32769-atom spectrum's extreme; the claimed 'within 10-25%' was evidently measured at a tiny non-prize m. This is the moment no-go (W2): closing needs p5/p6 which the proposal itself admits fail in the prize regime.

### [L13.6] Verschiebung/Adams operation on the period λ-ring: B-scaling rigidity from ψ^d-compatibility with dilation  — score 8
- **construct:** The λ-ring (lambda-ring) structure on the representation ring / period algebra, with Adams operations ψ^d (ψ^d(p_r)=p_{dr} on power sums) and the Frobenius/Verschiebung pair. The μ_n-dilation action is precisely the Adams/Frobenius operation: dilating S by a generator g of μ_n acts as ψ on the period algebra, with ψ^n = id (since g^n=1).
- **mechanism:** The periods η_b live in a λ-ring where the dilation g• acts as a ring automorphism of order n (g acts on η_b by η_b ↦ η_{gb} index-shift). The Adams operation ψ^d satisfies ψ^d(η)^{power sums}=p_{dr}, and the KNOWN moments give ψ^2(p_1)=p_2=3n²−3n, ψ^3(p_1)=p_3=E_3. The λ-ring Newton relation (ψ^r = Newton poly in λ^i) combined with ψ^n=id (the n-torsion from μ_n) forces a SYSTEM of algebraic equations on the elementary symmetric λ^i(η) that, together with the exact p_2,p_3 and odd p_{2k+1}=−n^{2k}, OVER-determines the period spectrum. Solving the ψ^n=id rigidity with the known low Adams values pins the eigenvalue spectrum {η_j} (hence B=max|η_j|) by an exact finite linear-algebra system over ℤ.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 10/6/3
- **exactness case:** Two-sided because ψ^n=id is an EXACT operator identity (the dilation has order exactly n), turning the period spectrum into eigenvalues of a finite-order operator: each η_j satisfies an explicit cyclotomic-type relation. The constraint ψ^d(p)=p_{d·} with p_2,p_3 and all odd p known is an exact (over-determined) linear system; if consistent it has a unique solution = the spectrum, hence B exactly. Two-sided: the system gives both that each |η_j| is an exact algebraic integer AND the max among them.
- **wall risk:** λ-ring/Adams-operation machinery is heavy and almost entirely absent from Mathlib; ψ^n=id constrains the FULL spectrum but the prize regime has m=(q−1)/n eigenvalues (large), so ψ^n=id alone (a rank-n constraint) under-determines the m-dimensional spectrum — need the q-dependence which re-introduces the Gauss-sum/Weil content. High risk this is the MOST elegant restatement of the open core rather than a solution. Highest novelty (Adams operations never tried on this problem), lowest feasibility. Order-3 = ψ^3 first nontrivial Adams op, matches E_3 exact data.
- **ranker note:** Highest novelty (Adams operations / lambda-ring never tried here) and order-3 = first nontrivial psi^3 matching exact p_3, but it concedes its own death: psi^n=id is a rank-n constraint on an m=(q-1)/n dimensional spectrum (m>>n in prize regime), so it under-determines and re-introduces the q-dependence = Gauss-sum/Weil content; lambda-ring machinery near-absent in Mathlib; 'most elegant restatement of the open core', explicitly.

### [L14.4] T-matrix Galois action: the period polynomial is the characteristic poly of a fixed S-matrix, Galois-conjugate roots are equidistributed on a circle  — score 8
- **construct:** The Galois symmetry of modular data (de Boer-Goeree / Coste-Gannon / Ng et al 'Congruence property'): Gal(Q(ζ)/Q) acts on the S-matrix by signed permutation, and on a CFT all modular-matrix entries are polynomials in a single root of unity. Here Gal(Q(ζ_p)/Q) ≅ (Z/p)^* acts transitively on the m periods (they form a single Galois orbit), and the period polynomial Ψ_n is irreducible of degree m. The roots η_i are the full Galois orbit of one Gauss period.
- **mechanism:** Galois transitivity forces the m roots to be 'spread' — they cannot cluster, because any algebraic relation among a proper subset would be Galois-stable and contradict irreducibility of Ψ_n. Quantitatively: an irreducible monic integer polynomial of degree m with EXACTLY known p_1,p_2,p_3 and with all roots of comparable size (Galois-conjugate, hence the same 'house') has its max root house bounded by the Mahler measure M(Ψ) = ∏max(1,|η_i|). Since all conjugates are essentially the same size (transitive orbit, bounded by √q individually), B = the common house, and Mahler-measure/Lehmer-type two-sided bounds (logM = Σlog⁺|η_i|, related to the resultant) pin the house from the exact discriminant/resultant data.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/7/5
- **exactness case:** Because the η_i are a SINGLE Galois orbit, |η_i| are all algebraic conjugates of one number; B is exactly the 'house' (max conjugate modulus) of the algebraic integer m·η = −1+√q·P. The house of an algebraic integer is two-sidedly controlled by its minimal polynomial's resultant/Mahler measure — and the Mahler measure here equals an explicit cyclotomic resultant (TQFT-3's norm). This is exact-value algebra (house of a fixed algebraic number), not an analytic one-sided estimate.
- **wall risk:** Conjugates of a Gauss period are NOT all the same modulus (they vary, as the numerics show: 4.79 down to 0.05), so 'house = common size' is false — Galois transitivity permutes but does not equalize moduli. The house is exactly B but bounding it via Mahler measure runs into the same L∞ gap. Real content: prove Galois orbit cannot have one giant conjugate, i.e. equidistribution of |conjugate| — which is the Sato-Tate-flavored wall.
- **ranker note:** Galois-transitivity / Mahler-measure 'house' framing is elegant but I numerically confirmed its OWN fatal risk: the m periods are NOT equimodular (range 0.05..4.8 in its data; my probe: wide spread, B!=common size), so 'house = common size' is false and the real content (no giant conjugate = |conjugate| equidistribution) IS the Sato-Tate/Paley wall; reduces to the named open core.

### [L2.6] Weight-3/2 Shimura lift: B² as a half-integral-weight theta coefficient pinned by its Shimura-corresponding weight-2 Hecke eigenvalue  — score 7
- **construct:** Shimura correspondence: a half-integral weight-3/2 Hecke eigenform (Γ₀(4)) ↔ weight-2 eigenform with EQUAL Hecke eigenvalues. The fourth moment / quadratic structure Σ_b |η_b|⁴ relates to counting solutions x₁+x₂=x₃+x₄ in μ_n (additive energy E_2, EXACT 3n²−3n); represent this counting generating function as a weight-3/2 theta series Θ_{3/2}(τ)=Σ r(n) q^n whose coefficients r(n) ARE the energy/period data.
- **mechanism:** The additive-energy generating function (coefficients = N₀(G,r), the in-tree relation counts whose r=2 value gives E_2) is a half-integral-weight modular form by the theta/sum-of-squares correspondence (modular forms & sums of squares). Its Shimura lift is a weight-2 form whose Hecke eigenvalue a_p satisfies the Deligne/Ramanujan bound |a_p|≤2√p EXACTLY (this IS Ramanujan–Petersson for the weight-2 cusp form, a THEOREM). Transporting back through Shimura pins the half-integral coefficient — hence the energy growth rate — exactly, and B²=max|η|² is read off as the dominant such coefficient.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/5/3
- **exactness case:** Two-sided because Ramanujan–Petersson |a_p|=2√p·cos θ_p with θ_p the Sato–Tate angle is an EXACT statement (the eigenvalue lies ON the circle of radius 2√p, with a determined angle), not just |a_p|≤2√p. If the energy theta series is a genuine eigenform, its Shimura lift's eigenvalue is exactly determined (e.g. by CM, the angle is 0 or π/2 — a special value), pinning B² to an exact algebraic number. The exact E_2,E_3 data fix the first Hecke eigenvalues, identifying WHICH eigenform, hence ALL its coefficients.
- **wall risk:** HIGH. The energy generating function for μ_n is a finite sum, not obviously a true modular form (it would need to be the q-expansion of a form on Γ₀(4N) — the sum-of-squares analogy is for r(n)=#representations as sums of squares, a different quadratic form). If it is only QUASI-modular or Eisenstein-dominated, Ramanujan–Petersson (a cusp-form theorem) does not apply and the bound degrades to the Eisenstein O(σ-divisor) growth — back to the trivial side. The CM special-angle claim needs the form to have CM, true only for special n (powers of 2 may give CM at ρ — worth checking, order-3 link).
- **ranker note:** Load-bearing modularity is almost certainly false: the mu_n energy generating function is a finite sum, not a true modular form (the sum-of-squares analogy counts representations, a different quadratic form); if quasi-modular/Eisenstein-dominated, Ramanujan-Petersson (a cusp-form theorem) is inapplicable and the bound degrades to Eisenstein sigma-divisor growth = the trivial side; CM-at-rho order-3 hook is speculative.

### [L8.3] Interlacing of the real Cayley graph against a known Ramanujan host (eigenvalue interlacing / quotient)  — score 7
- **construct:** Because -1 in mu_n for the dyadic regime (verified spec_probe.py: graph undirected/real spectrum, matching arXiv:2604.06513 'real iff undirected'), Cay(F_q,mu_n) is a real symmetric n-regular graph. It is an induced/equitable sub-structure of the FULL Paley graph Cay(F_q, QR) = Cay(F_q,mu_2) (since mu_n <= mu_2, n=2^mu): mu_n is a union of n/2 cosets... rather mu_2 = mu_n * (mu_2/mu_n). Use the Cayley-subgroup tower mu_n < mu_{n/2}... < mu_2 of nested generalized Paley graphs.
- **mechanism:** Cauchy eigenvalue INTERLACING along the subgroup tower: A(Cay(F_q,mu_2)) = Sum over the 2/n... the adjacency matrices add: A(mu_2)=Sum_{cosets} A(coset-translate of mu_n). Since the Paley graph Cay(F_q,QR) is EXACTLY Ramanujan-type with eigenvalues (-1+-sqrt q)/2 KNOWN exactly, and our eta_b are partial sums refining these, interlacing/quotient-matrix bounds pin max|eta_b| between consecutive Paley eigenvalues. The equitable partition into mu_n-cosets gives a QUOTIENT matrix whose spectrum interlaces and whose entries are cyclotomic numbers.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 7/7/5
- **exactness case:** Interlacing gives genuine TWO-SIDED enclosure: the k-th eigenvalue of the refined graph lies between the k-th and (k+m-...)-th of the host whose spectrum {(-1+-sqrt q)/2, with multiplicities} is EXACTLY known. The quotient/equitable-partition matrix has its spectrum as a SUBSET of the full spectrum, and for equitable partitions the bounds are sharp (achieved), not merely one-sided. The host being exactly solvable (Paley = self-complementary SRG) makes the enclosing values exact algebraic numbers.
- **wall risk:** Interlacing of A(mu_2) into n/2... cosets only constrains SUMS of eta over the refinement, not the single max; the interlacing gap is O(sqrt q) wide (host eigenvalue ~ sqrt q >> M ~ sqrt(n log)), so naively the enclosure is loose at prize scale. Needs the equitable-partition quotient (cyclotomic) to tighten — converges back to A2's cyclotomic-number wall.
- **ranker note:** Equitable-partition/interlacing against the exactly-solvable Paley host is mathematically clean, but I measured the host gap ~ sqrt(q) is 9x-45x wider than B and WORSENS with n (B/Paley = 0.24 -> 0.04), so the enclosure is useless at prize scale; the proposal's own risk note concedes it converges back to the cyclotomic-number wall (L8.2). The tightening quotient matrix IS the cyclotomic-number object with no closed form at m>24. Loose by an order of magnitude.

### [L7.2] The skewness side-law: P3=-n^2<0 forces the extreme period onto the NEGATIVE side, halving the search  — score 6
- **construct:** Third standardized cumulant (skewness) kappa_3 = p3 - 3 p1 p2/m + 2 p1^3/m^2 of the real period measure, as a sign invariant of the period polynomial. The negative-skew tail of a real-rooted integer polynomial.
- **mechanism:** B = max|eta_k| is achieved at EITHER the max root or the min root of Psi. I VERIFIED (probe /tmp/skew_law.py, 132/142 = 93% consistent, exceptions only at tiny m) that the sign of P3 = sum eta_k^3 = q*T3/n - n^2 determines which: in the prize regime P3 = -n^2 < 0 (left-skewed), and the negative extreme dominates (|min root| >= max root). This collapses the two-sided extreme problem to a ONE-SIDED problem on the heavy-tail side, where the order-3 moment additionally SHARPENS the Laguerre-Samuelson bound (the skew correction term). The exact value q*T3/n - n^2 is computable: T3 = n * #{a,b in mu_n : a+b=-1} is the order-3 additive-triple invariant.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/8/7
- **exactness case:** The side-law is an exact dichotomy (not a bound): it pins WHICH algebraic conjugate realizes B, reducing the problem to the minimal root, whose value is then a single algebraic number (the most-negative root of the degree-m integer period polynomial). Combined with L7-A1's one-sided Markov bound on exactly that side, the upper becomes a genuine two-sided pin because the opposite (positive) side is provably non-extremal. The exactness is in the reduction: B = |minroot(Psi)| EXACTLY, not B <= something.
- **wall risk:** Does not hit W1/W2 (it is a sign/parity statement, not a magnitude moment bound). Residual risk: the 132/142 figure shows ~7% exceptions at small m; must prove the dichotomy holds in the LARGE-m prize regime (where T3=0 forces P3=-n^2 strictly negative, so the law should be unconditional there). Formalizable: P3=qT3/n - n^2 is a clean Lean identity built on the existing ListInteriorT3 / AdditiveEnergyThreeDvd machinery.
- **ranker note:** REFUTED at the prize regime: I tested 18 primes and got 6 POS-side / 12 NEG-side extremes (essentially a coin flip), not the claimed 93% — the period measure is nearly symmetric at large m (max approx |min| within a few percent) so the weak P3=-n^2 skew does NOT force the extreme onto the negative side. The 'halving the search' / one-sided reduction is illusory beyond the n=8 toy case. The exact dichotomy it claims to pin is false in the regime that matters.

## Tier: WALL (36)

### [L1.2] Tracy-Widom-to-Gumbel crossover: B as a Gaussian-ensemble maximum with explicit Gumbel constant  — score 9
- **construct:** The m=(q-1)/n distinct period values {η_c} viewed as the eigenvalue point process of a structured ensemble. Probe data: standardized spectral moments m_4=2.81, m_6=12.3, m_8=70.9 converge to GAUSSIAN (3,15,105) NOT semicircle (2,5,14). So the bulk is Gaussian variance-n, and the edge fluctuation is in the GUMBEL universality class (max of ~m near-Gaussian variables), NOT Tracy-Widom (no hard semicircle edge — confirmed by the litsweep rejecting Sato-Tate).
- **mechanism:** For the maximum of m correlated mean-zero variables with variance n and Gaussian-matching moments, extreme-value theory gives max ≈ √(2n ln m)·(1 + (ln ln m + ln 4π)/(4 ln m) + ...) — the GUMBEL location with the EXACT second-order Gumbel correction. The known data E_2=3n^2-3n, E_3=15n^3-45n^2+40n FIX the variance-n and the correlation-decay (the -3n, -45n^2 terms are the negative correlation between distinct periods, computable from Tr A^{2r}). The exactness comes from: B = Gumbel location + Gumbel-scale·γ (Euler-Mascheroni) where scale=√(n/(2 ln m)). This gives B EXACTLY √(2n ln m) + γ√(n/(2 ln m)) + lower order — a two-sided asymptotic with named constant γ.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 7/8/4
- **exactness case:** Gumbel convergence is a two-sided LIMIT THEOREM (max-(loc)/scale ⇒ Gumbel in distribution), giving both the leading √(2n ln m) AND the exact subleading γ·scale correction — matching the §R.3 measurement max|η|^2≈n(ln p + G) where G is identified as the Euler-Mascheroni-type Gumbel constant. Not a one-sided bound: it predicts the precise value B/√(2n ln m)→1 with a computable rate.
- **wall risk:** The Gumbel limit theorem REQUIRES asymptotic independence (or controlled correlation decay) of the m periods — exactly the joint Gauss-sum independence (Katz monodromy) open core. The moments matching Gaussian is necessary but the EXTREME-VALUE behavior needs tail-independence, which is the BGK wall. But the contribution is genuinely new: it PINS the constant (γ) and identifies the universality class as Gumbel-not-TW, ruling out the TW edge attempts.
- **ranker note:** This is literally the in-tree i.i.d.-Gumbel verdict (correct universality class, subleading -1/2 here dressed as Euler-Mascheroni gamma); it re-derives the existing sub-Gaussian floor sqrt(2n log m) and the Gumbel limit needs tail-independence of the m periods = the BGK/Katz wall — correctly identifies the class but adds no new leverage.

### [L3.4] Heisenberg group / theta-Poisson summation: the subgroup period as a finite theta value, pinned by the dyadic 2-adic Poisson self-duality  — score 9
- **construct:** The finite Heisenberg group H(F_q) and its unique Stone-von-Neumann rep; the subgroup mu_n (dyadic, n=2^mu) is a SELF-similar lattice under the 2-power Frobenius (squaring x->x^2 is a 2-to-1 endomorphism of mu_n with kernel {+/-1}). The period eta_b=Sum_{x in mu_n} e_p(bx) is a finite theta function; the 2-adic descent mu_n -> mu_{n/2} (squaring) is the finite analogue of the modular/theta transformation tau->tau/2.
- **mechanism:** Finite Poisson summation on the tower mu_n > mu_{n/2} > ... : eta_b over mu_n relates to eta over mu_{n/2} via the 2-adic descent (in-tree CumulantDyadicDescent, GaussPeriodTower exact recursion ||eta||^2+||eta^chi||^2=2(||A||^2+||B||^2)). The Heisenberg/theta angle says this recursion is a metaplectic action of the 2-adic SL_2(Z_2) on the tower of periods; the FIXED POINT of the descent (where eta^chi aligns with eta, cos=1) is the metaplectic invariant pinning B. The theta transformation's automorphy factor IS a Gauss sum (sqrt of the index), so the recursion multiplies B by sqrt2 per level UNLESS the 2-adic phases cancel.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/8/6
- **exactness case:** Two-sided because the descent recursion ||eta||^2+||eta^chi||^2=2(||A||^2+||B||^2) is an EXACT identity (already landed, #407 tower), so each level gives an exact linear relation between consecutive-level period norms; iterating mu times down to mu_1={+/-1} (where eta_b=2cos(2pi b/q) is EXACTLY known, |eta|<=2) gives a telescoping product. The exactness of the base case (mu_1: eta=2cos, a CLOSED FORM) plus mu exact recursion steps pins B if the per-level alignment cos-factor is determined; the dyadic odd-moment vanishing (proven) forces the cross-terms, removing the freedom.
- **wall risk:** The per-level alignment (cos between eta and eta^chi) is exactly the open content; prior #407 work found cos=1.0000 alignment->sub-maximality but could not close it. The sqrt2-per-level automorphy factor is the metaplectic-multiplier=Gauss-sum=sqrt(index) restatement of the BGK wall. Genuinely exploits the proven exact recursion + exact mu_1 base case, so the bracket is real even if not collapsing.
- **ranker note:** Honestly uses the proven exact tower recursion + exact mu_1 base, but self-identifies that the per-level cos-alignment is the open content (prior #407 found cos=1.0000 -> sub-maximality but could not close) and the sqrt2-per-level automorphy factor is literally the metaplectic-multiplier=Gauss-sum=sqrt(index) restatement of the BGK wall.

### [L9.2] Delsarte LP dual on the Z/m group code of unimodular Gauss phases  — score 9
- **construct:** The cyclic group code structure on Z/m (m=(q-1)/n) carrying P(b)=Sum_{chi != 1} chibar(b) gamma_chi with gamma_chi=G(chi)/sqrt(q) UNIMODULAR. The Bose-Mesner algebra of the cyclic association scheme on Z/m, with Krawtchouk/character eigenvalues being the m-th roots of unity. Delsarte's LP: feasible f = Sum_k f_k chi_k with f_k>=0, f(j)<=0 off the diagonal.
- **mechanism:** max|P| = sup_b |Sum_{a=1}^{m-1} gamma_a omega^{ab}| is the sup-norm of a DFT with unimodular coefficients on Z/m. Parseval pins Sum_b |P(b)|^2 = m(m-1) EXACTLY (known data). The Delsarte LP DUAL for the sup-norm becomes: maximize the largest |P| subject to fixed L2 mass m(m-1) and the extra hard constraints from |gamma_a|=1 (each |coefficient|=1, not just bounded). The dual LP optimum is achieved by an f supported on a Sidon/perfect-difference structure; if Z/m admits a perfect difference set (m = prime-power planar) the LP is TIGHT and pins max|P| to the perfect-DFT value sqrt(m).
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/8/5
- **exactness case:** Delsarte LP is two-sided when the configuration meeting the dual polynomial is a tight design (equality in both primal and dual). The unimodularity |gamma_a|=1 turns the problem into a flat-DFT / Sidon-set extremal problem where the EXACT extremizers are known (Gauss/Fekete polynomials, Rudin-Shapiro). The L2 constraint Sum|P|^2 = m(m-1) is exact data, so the LP feasible region is a fixed sphere intersected with the unimodular torus; its support function (= max|P|) is the exact LP value.
- **wall risk:** The gamma_a are not free unimodulars — they are the SPECIFIC Gauss phases tau(chi_a)/sqrt q, satisfying the Jacobi cocycle relation gamma_a gamma_b = (J/sqrt q) gamma_{a+b} (|J|<=sqrt q, NON-contractive). The LP relaxation that forgets this cocycle gives only sqrt(2m ln m)-type bounds (the Salem-Zygmund / random-unimodular regime); pinning the EXACT value needs the cocycle, which is the W4 Gauss-sum wall. The LP exactness only fires for special m (planar difference set) — a corner, not the generic prize m.
- **ranker note:** Delsarte LP on the unimodular Gauss phases reduces to the same flat-DFT/Sidon extremal that yields sqrt(2m ln m); the attack itself concedes the gamma_a obey the Jacobi cocycle (W4 wall) and that exactness fires only for planar-difference-set m (a corner), so generic prize m gives only the one-sided random envelope.

### [L9.6] Bombieri-Selberg / Beurling extremal LP majorant for the Gauss-phase sup-norm  — score 9
- **construct:** The Beurling-Selberg extremal functions and the Bombieri-Davenport LP large-sieve duality, transplanted to the finite group Z/m. The extremal majorant/minorant pair F^+ >= 1_E >= F^- with bandlimited Fourier support, optimized as a (continuous) linear program, is the analytic analogue of Delsarte's LP and gives EXACT large-sieve constants.
- **mechanism:** max|P|^2 = max_b |Sum_{a} gamma_a omega^{ab}|^2 over the unimodular Gauss phases. The large-sieve inequality Sum_b |P(b)|^2 <= Delta * Sum_a |gamma_a|^2 with the EXACT large-sieve constant Delta (Selberg's extremal-function value, = m for the full group) becomes equality (Parseval gives m(m-1) exactly), so the slack in the large sieve localizes the sup. The Beurling-Selberg LP majorant of the indicator of the peak-set produces the EXACT extremal constant; because |gamma_a|=1 are unimodular, Selberg's construction (which is sharp for the Bombieri-Davenport setting) transfers and pins max|P| via the optimal LP majorant's value at 0.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/6/4
- **exactness case:** The Beurling-Selberg extremal problem has a KNOWN exact optimum (the extremal functions are explicit, value computed by Vaaler/Selberg) and the large-sieve constant is achieved with equality precisely for well-distributed point sets — the m-th roots of unity (the support of the cyclic DFT) are EXACTLY equispaced, the equality case. So the LP majorant value is the exact two-sided constant, not a one-sided estimate.
- **wall risk:** The large-sieve/Selberg LP is sharp for the WORST-CASE point configuration (random/spread phases) and yields the sqrt(2m ln m) random-model upper bound — exactly the Wick scale — but the gamma_a are deterministic Gauss phases with the Jacobi cocycle correlation, so equality in the large sieve need NOT hold for them; the gap is the W4 incomplete-Gauss-sum problem. Risk: pins the upper envelope (conjectured target) but the matching lower needs the cocycle's actual sup, which is the open wall.
- **ranker note:** Beurling-Selberg/large-sieve LP pins the sqrt(2m ln m) RANDOM-phase envelope = the conjectured target's upper side, but the matching lower needs the actual Gauss-phase sup which is governed by the non-contractive Jacobi cocycle |J|=sqrt q — exactly the W4 incomplete-Gauss-sum wall the prompt forbids re-proposing.

### [L1.4] Fourth-moment theorem (Nualart-Peccati) on the period chaos: total disorder forces Gaussian edge  — score 8
- **construct:** Each period η_b = Σ_{x∈μ_n} e_p(bx) sits in a fixed WIENER-CHAOS-like / hypercontractive algebra generated by the m Gauss-sum coefficients {τ(χ_j)} (unimodular up to √q). The period sequence is the inverse-DFT of (τ(χ_j)) — a degree-1 'chaos' in the Gauss-sum variables. Apply the Nualart-Peccati Fourth Moment Theorem: a sequence in a fixed chaos converges to N(0,σ^2) IFF its fourth cumulant → 0, with the de Jong / Peccati-Tudor multidimensional CLT giving JOINT Gaussianity.
- **mechanism:** The fourth cumulant of η_b is κ_4 = E[|η_b|^4]-2(E|η_b|^2)^2 = (per-frequency 4th moment) - 2n^2. The proven GLT Theorem 3 / Hasse-Weil bound |27V_4 - (6p^2+12p+1)| ≤ 8p^{3/2} gives this 4th cumulant EXACTLY up to O(p^{3/2}) = the random-Gaussian value 3n^2 minus 2n^2 = n^2, with relative error → 0. By the 4th-moment theorem, vanishing normalized 4th cumulant ⇒ the periods are ASYMPTOTICALLY a Gaussian field, and Peccati-Tudor upgrades single-variable to JOINT Gaussianity of (η_{b_1},...,η_{b_k}) from the SAME 4th-cumulant input. Joint Gaussianity ⇒ max over m of a Gaussian field = √(2n ln m) exactly (Gumbel). The miracle: the 4th-moment theorem says you only need to control ONE cumulant (the 4th, which we KNOW via Hasse-Weil), not all moments — bypassing the deep-moment wall.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/6/4
- **exactness case:** The 4th Moment Theorem is an IFF (two-sided): convergence to Gaussian ⟺ 4th cumulant vanishes, and the Peccati rate (total-variation distance ≤ C·√κ_4) is quantitative. So a measured/proven 4th-cumulant size gives a two-sided control on the Gaussian approximation, hence on max|P| = √(2m ln m) with explicit error, not just an upper bound.
- **wall risk:** The 4th-moment theorem requires the variables to live in a FIXED chaos of fixed order; the periods are degree-1 in Gauss sums but the Gauss sums are NOT independent (Hasse-Davenport/Stickelberger relations) so the Wiener-chaos structure is only approximate. Joint Gaussianity for the MAX (uniform over m frequencies) needs uniform 4th-cumulant control = the joint-equidistribution open part. BUT genuinely new: it is the ONE attack that needs only a single cumulant (the proven 4th) rather than the full hierarchy — strongest candidate to dodge the moment no-go.
- **ranker note:** Explicitly ground out in-tree as theory #48: Nualart-Peccati is a char-0 Wiener-chaos theorem with no char-p content, 'collapse to r=2' only re-derives Lam-Leung sub-Gaussian, its large-deviation guise is the already-proven JohnsonFourthMomentNoGo, and the MAX needs uniform joint 4th-cumulant control = the joint-equidistribution open part.

### [L1.5] Free-probability subordination: B as the edge of a free convolution of m Gauss-phase semicircles / arcsines  — score 8
- **construct:** Model η_b = (1/m)Σ_j τ(χ_j) e(-jb/m): a sum of m rotated rank-1 contributions, each τ(χ_j) UNIMODULAR (×√q). The empirical distribution of a single phased term e(θ)·(unimodular) is the ARCSINE law on [-1,1] (real part of uniform phase). Under joint Gauss-sum equidistribution the periods' law is the FREE (or classical, given the DFT independence) CONVOLUTION of m arcsine laws, normalized. Use Voiculescu's free convolution ⊞ and the subordination functions ω_1,ω_2 to compute the edge of the m-fold free convolution.
- **mechanism:** Free convolution of m identical arcsine (Bernoulli-symmetric) laws scaled by √q/m has an edge given EXACTLY by the R-transform additivity: R_{μ^{⊞m}}(w)=m·R_arcsine(w), and the edge = sup support = the critical value of z=1/w + m·R_arcsine(w). For arcsine on {±a}, R(w)=(√(1+4a^2w^2)-1)/(2w), giving a closed-form edge that scales as √(m)·a in the free CLT regime → semicircle, but for FINITE m with the DFT (classical-independence) structure instead converges to GAUSSIAN with edge √(2 ln m)·(per-coordinate scale). The exact edge of the m-fold convolution, evaluated at the per-coordinate scale √q/m, gives B = edge = √(2(q/m) ln m)·(1+o(1)) = √(2n ln m) — and free-vs-classical convolution is decided by whether the Gauss phases are FREE (asymptotically free ⇒ Katz monodromy) or classically independent.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 8/6/3
- **exactness case:** Voiculescu subordination gives the edge of a free convolution as an EXACT algebraic equation (solve ω'(z)=0), not an inequality — the supremum of the support is a definite root. Two-sided because the free-convolution density is supported up to and including the edge with a square-root vanishing (the generic free edge), so the spectral radius = the edge exactly.
- **wall risk:** Deciding free vs classical independence of the Gauss-sum family is the Katz-monodromy open core; and the arcsine-convolution model assumes phases uniform/independent. The free-CLT edge (semicircle) is REJECTED by the data (moments are Gaussian not semicircle), so one must use the FINITE-m classical convolution — which loops back to joint independence. New construct (arcsine ⊞-convolution, subordination edge) genuinely untried, gives the right √(2n ln m) shape with an exact algebraic edge equation.
- **ranker note:** Explicitly in-tree as theory #37 (free additive convolution edge), refuted as a bulk-spectral object blind to the Fermat-prime spike; the doc further notes the free-CLT edge is semicircle which the Gaussian moment data rejects, forcing the classical finite-m convolution = loops straight back to joint Gauss-sum independence (the open core).

### [L9.4] Levenshtein Gauss-Jacobi quadrature with Krawtchouk nodes for the list size  — score 8
- **construct:** The Hamming association scheme H(n,q); the dual code C^perp of RS; the q-ary Krawtchouk polynomials K_k (already in tree, krawtchouk_eq_coeff). Levenshtein's optimal LP polynomials built from adjacent Krawtchouk pairs, with nodes at the roots of K_k (Gauss-Jacobi quadrature for the Krawtchouk weight).
- **mechanism:** L_{RS[k+1]}(delta) = number of codewords within Hamming distance delta*n; the close-codeword set is a code with minimum distance >= d_RS = n-k (MDS), so Delsarte/Levenshtein LP applies. Levenshtein's bound L <= f(0)/f_0 is OPTIMAL among LP bounds and its value is given EXACTLY by Gauss-Jacobi quadrature: the optimal polynomial f vanishes (doubly) at the Krawtchouk roots, and L = a closed expression in the largest Krawtchouk root in the decoding interval. The bridge ScriptFreeRSBridge gives eps_mca <= (n/w) L, so pinning L pins eps_mca and hence delta*.
- **quantity:** L_RS | **novelty/relevance/feasibility:** 7/8/6
- **exactness case:** Levenshtein's bound is the PROVABLY-OPTIMAL LP bound (no LP polynomial does better), and it is TWO-SIDED for codes meeting it: the MRRW/Levenshtein bound is attained with equality by tight designs (e.g. the dual is a tight design), and RS codes have explicit weight enumerators (RSWeightEnumerator in tree, A_d = q^{deg-|S|}) which let one check the design-tightness condition exactly. When the close-codeword set is a tight t-design the LP equals the actual list size.
- **wall risk:** Beyond Johnson (the prize regime delta in (1-sqrt rho, 1-rho-Theta(1/log n))) the LP / Levenshtein bound is conjectured NOT to be tight for general codes (Gil Kalai's recent work on LP-bound limits) — RS may not be a tight design there. Risk: LP gives a clean closed value but it is an upper bound on L that is NOT attained, so it is one-sided. Mitigation: use the explicit RS weight enumerator to TEST tightness; if RS is a completed/tight design at the specific dyadic delta, the bound is exact.
- **ranker note:** Levenshtein/Gauss-Jacobi LP gives a clean closed value but it is a ONE-SIDED upper bound on the list that is provably not attained beyond Johnson (Kalai LP-limit results; RS is not a tight design in the prize window (1-sqrt rho, ...)), so it fails the no-Johnson/no-one-sided prize criterion.

### [L10.3] Sunflower-free / Erdos-Ko-Rado on the owned k-subset families: the slice-rank list bound becomes EXACT when the agreement set is a multiplicative-subgroup arithmetic progression  — score 8
- **construct:** The owned-k-subset hypergraph from ListIncidencePolyMethod (each list codeword owns powersetCard k of its matchSet, pairwise disjoint by rank_collapse_on_kset). Impose the smooth-domain structure: matchSets are far-coset agreement sets, which over mu_n are translates/dilates of multiplicative cosets. Apply the sunflower lemma (Erdos-Rado / Alweiss-Lovett-Wu-Zhang improved bound) and the sunflower-free slice-rank bound to the owned families.
- **mechanism:** ListIncidencePolyMethod proves |L|*C(a,k) <= C(n,k) (field-blind). The honest collapse there is that it's field-blind. The sunflower upgrade: over the SMOOTH domain, the owned k-subsets of distinct codewords cannot form a large sunflower because a sunflower of owned sets would force a common 'core' of agreement positions shared by many codewords, which by rank_collapse on the core forces a degree-<k polynomial pencil constraint = e2=0-style algebraic locus. Sunflower-free bound (Tao slice-rank: a family with no s-sunflower has size <= (Cs)^n... but the SHARP form for the agreement geometry) caps L_RS exactly when the core forced is the mu_n coset. L_RS = exact list size, and the line-ball bridge gives B from L via SqrtFreeRSBridge (eps_mca <= (n/w)L).
- **quantity:** L_RS | **novelty/relevance/feasibility:** 8/7/4
- **exactness case:** Two-sided IF the sunflower-free bound saturates: the lower bound on L is the explicit set of bad codewords (the e2=0 dilation orbits, exactly n*K), and the sunflower-free UPPER bound matches when the agreement sets are forced into a single mu_n-coset sunflower core. The matching happens because the smooth domain makes the owned families EXTREMAL for EKR (intersecting families on a cyclic group are pinned by Frankl-type theorems with equality on AP-structured ground sets). At equality L_RS = (exact EKR extremal count) = the in-tree n*K, giving a two-sided pin of the list size rather than the field-blind super-poly C(n,k)/C(a,k).
- **wall risk:** Field-blindness wall (the documented collapse in ListIncidencePolyMethod): the generic sunflower bound is also field-blind. The escape requires the smooth-domain sunflower-CORE to be forced algebraic, which is exactly the beyond-Johnson list-decoding open core. Also the modern sunflower bounds (ALWZ) are not in Mathlib. High wall risk, but the EKR-equality angle is genuinely untried in this cone.
- **ranker note:** EKR/sunflower angle is fresh in this cone but lands on the documented field-blind collapse of ListIncidencePolyMethod (the smooth-domain escape IS the beyond-Johnson open core), and modern ALWZ sunflower + the equality-design machinery are not in Mathlib; high wall + low feasibility.

### [L12.1] Hausdorff moment-determinacy support-edge collapse (exact E_r ladder pins the support edge)  — score 8
- **construct:** The empirical spectral measure μ_q := (1/(q-1)) Σ_{b≠0} δ_{|η_b|²/q} on the compact interval [0, n/?] ⊂ ℝ≥0; its moments are the NORMALIZED energies E_r/q^{r-1}·(1/(q-1)). Hausdorff (bounded-support) moment problem on a compact interval, where a measure is uniquely determined by its moments and the support edge equals limsup_r (M_{2r})^{1/2r}.
- **mechanism:** VERIFIED exactly: the 2r-th moment of |η_b| over b≠0 equals q·E_r(μ_n) − |G|^{2r} (the cumulant identity already in tree: cumulant_eq). So the ENTIRE moment ladder of the spectral measure is exactly known wherever E_r is known: E_2=3n²−3n, E_3=15n³−45n²+40n (order-3 exact), and the Wick recursion E_r=(2r−1)!!nʳ in char 0. The support edge of the measure — which is EXACTLY max_b|η_b|²/q (the prize quantity squared, normalized) — equals lim_r (E_r/q^{r-1})^{1/r} ... but more sharply, the truncated-moment determinacy theorem (Tchakaloff / core-variety) says: a measure on [0,c] supported on ≤N atoms is pinned by its first 2N moments. The Gauss-period measure is supported on at most (number of distinct |η_b|² values) = the orbit count under the dual group action, a FINITE explicit set. Pin that atom-set from E_1..E_{2N} via the truncated moment problem, then max = largest atom = EXACT.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/8/5
- **exactness case:** Two-sided because the truncated Hamburger/Hausdorff moment problem with KNOWN finite atom-count N gives a DETERMINED measure (not a bound): the first 2N moments uniquely reconstruct all N atom locations via the Hankel-matrix kernel (the atoms are the roots of the orthogonal polynomial of degree N w.r.t. the moment functional). The largest root IS max|η_b|², exactly — no inequality. The number of distinct values N is the dual-orbit count K(n) (already in tree as the e₂=0 count machinery / RatioValueMultiplicity), which is small and explicit for dyadic n.
- **wall risk:** Atom-count N grows with q in the inflation regime; if N ~ q the moment system is no longer finitely truncated and one needs E_r for r up to N — but the ladder E_r is only exactly known (char-0 Wick) for r ≤ β before the char-p wrap (Q4 wall). Mitigant: in the prize regime distinct-value count is q-INDEPENDENT (RatioValueMultiplicity / per-direction packing), so N stays bounded and only E_1..E_{2N} for FIXED small N are needed — order-3 (E_3) already covers N up to 1-2 atoms beyond the trivial.
- **ranker note:** Truncated Hausdorff moment determinacy is exact ONLY if the atom count N stays q-independent and small — my probe REFUTES this: distinct |eta_b|^2 values = 504/509 (n=8) and 4010/4096 (n=16), i.e. N ~ m grows with q; the 'q-independent orbit count' it cites is the e2=0 BAD-SCALAR locus K(n), a different sparse set, not the period-value spectrum, so the moment system needs E_r to r~N~m = the char-p wall.

### [L12.5] Selberg/Beurling extremal majorant pinning via the two exact moments (band-limited duality)  — score 8
- **construct:** Beurling–Selberg extremal functions: the extremal band-limited majorant/minorant of the indicator of [max−ε, ∞). Pair the EXACT Parseval Σ|P|²=m(m−1) (a band-limited L² constraint on Z/m, frequencies = characters) with the exact 4th moment as a SECOND linear functional, and run the Selberg LP duality: the extremal majorant of the point-mass at the spectral edge, expanded in the m characters, gives matching upper AND lower bounds on max|P| that COINCIDE when the number of active moment-constraints equals the number of distinct spectral values.
- **mechanism:** Selberg's LP gives: max|P|² = max over distributions consistent with the exact moments (Parseval, 4th, 6th=E_3) of the largest support point, and the DUAL LP gives the matching majorant. By LP strong duality these coincide. The constraints are the EXACT moments (2 from Parseval+M4, a 3rd from E_3 order-3). The number of distinct |P(b)|² values in the dyadic prize regime is q-independent and small (orbit count), so 2-3 exact moments saturate the LP and primal=dual EXACTLY. The band-limitation is automatic: P lives in the span of m characters, a finite-dim space, so the Selberg LP is finite-dimensional and EXACTLY solvable.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 8/9/6
- **exactness case:** Two-sided by LP strong duality: the primal (largest spectral value consistent with given exact moments) and dual (smallest majorant value) are EQUAL when the moment problem is determinate, i.e. when #constraints ≥ #atoms. The exact moments (Parseval=m(m−1), 4th=q·E₂-derived, 6th=E_3) provide exactly the constraints; the finite atom-count (q-independent in prize regime) makes the LP determinate, so primal=dual=max|P| exactly. Beurling–Selberg gives the sharp extremal function realizing equality.
- **wall risk:** If atom-count exceeds available exact moments (need E_r for r > 3 but order-4+ deviates from Wick / char-p anomaly), the LP is under-determined and primal>dual (a true gap, = the prize wall). Risk shared with A1/A2: the determinacy threshold is exactly 'does the q-independent orbit count ≤ (number of unconditionally-exact moments = 3 via E_3)?' This is a CONCRETE checkable inequality for each dyadic n, not open math — for small dyadic n (n=8,16) the orbit count is 1-3, so 3 moments suffice and the LP closes; for n=32 it may need E_4 (the boundary).
- **ranker note:** Selberg LP duality is exact only when #constraints >= #atoms; my probe shows #atoms ~ m (hundreds-to-thousands), not <=3, so the LP is massively under-determined and primal>dual is a genuine gap = the prize wall — its own determinacy criterion ('orbit count <= 3 exact moments') is false on the actual period-value distribution, same conflation of K(n) with the value spectrum as L12.1.

### [L14.6] Frobenius-Perron quantum dimension n minus B: the spectral-gap of the Cayley/fusion graph as the categorical dimension defect  — score 8
- **construct:** In a fusion category the Frobenius-Perron dimension is the top eigenvalue of the fusion matrix; non-FP eigenvalues are the other quantum dimensions / S-matrix ratios. The Gauss-period fusion matrix N (cyclotomic numbers, TQFT-1) is the adjacency operator of the Cayley graph Cay(F_q, μ_n) folded onto cosets; its top eigenvalue is EXACTLY the quantum dimension n (FP, verified), and B = the second eigenvalue. The 'FP dimension defect' n − B is the spectral gap = expansion of the μ_n-Cayley graph (in-tree GaussPeriodSpectralFrame: M(n)=λ₂).
- **mechanism:** Exploit that the fusion matrix N satisfies an EXACT polynomial identity inherited from the category: tr(N)=p_1=−1, tr(N²)=p_2=p−n, tr(N³)=p_3=−n² are exact, AND N has the exact top eigenvalue n with the explicit Perron eigenvector (the trivial coset / all-periods-equal vector). Deflate: N = n·Π_0 + N' where Π_0 is the rank-1 FP projector; then tr(N'^k) = p_k − n^k are EXACT for k=1,2,3, and B = ‖N'‖_spectral. Bound ‖N'‖ two-sided from its exact low moments using that N' is a real symmetric (or normal, via the abelian Galois action) integer-combination matrix: λ_max(N') ≤ √(tr(N'²)) but also ≥ tr(N'³)/tr(N'²)-type Rayleigh floors.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/9/7
- **exactness case:** After exact FP-deflation, tr(N'²)=p_2−n²=(p−n)−n² and tr(N'³)=p_3−n³=−n²−n³ are EXACT. For a NORMAL matrix N' the second-eigenvalue B obeys both λ_max² ≤ tr(N'²) (Frobenius-norm ceiling, exact) and the Rayleigh-quotient floor λ_max ≥ |tr(N'³)|/tr(N'²) (exact). These bracket B with EXACT numerators/denominators — a genuine two-sided pin whose width is the gap between L²-mass and the cubic Rayleigh estimate, both computed from known data.
- **wall risk:** tr(N'²)=(p−n)−n² ≈ p in the prize regime, so the Frobenius-norm ceiling λ_max ≤ √p is the TRIVIAL √q bound — not the √(n log) target (this is the moment no-go re-appearing: low moments of N only see √q). The cubic Rayleigh floor is O(n) (since tr(N'³)≈−n³, tr(N'²)≈p, ratio ≈ n³/p ≪ n). Gap between √q ceiling and O(1) floor is enormous; needs the LOG-many higher moments the no-go forbids. Honest: pins the floor sharply, ceiling stays trivial.
- **ranker note:** I verified its own admitted risk: after FP deflation the Frobenius-norm ceiling sqrt(tr N'^2)=sqrt(p-n)~sqrt(q) (ratio to target grows 1.06->1.65), which is exactly the trivial L2->Linf bound = moment no-go, while the cubic Rayleigh floor is O(n). Pins the floor sharply but the ceiling is the wall; also the in-tree 'fusion' files are subset-sum combinatorics, not this categorical matrix, and GaussPeriodSpectralFrame already provides a wall-conditioned two-sided frame.

### [L17.3] Newton-identity support reconstruction from the odd-moment data  — score 8
- **construct:** Symmetric-function / Newton-identity machinery on the multiset {η_b}_{b≠0} viewed as roots; the power sums p_r=Σ_{b≠0}η_b^r are ALL known exactly via PeriodMomentLaw (=q·N₀(G,r)−n^r), including the EXACT odd moments Ση^{2k+1}=−n^{2k} and E_2,E_3.
- **mechanism:** max|P| (equivalently max|η_b|) is the largest root-modulus of the period polynomial Ψ(x)=∏_{b≠0}(x−η_b). Newton's identities recover the elementary symmetric functions e_1,e_2,... (hence Ψ's coefficients) from the power sums p_r. The KNOWN low-order data — p_1,p_2 (E_2),p_3 (E_3=15n³−45n²+40n), and the entire odd-moment family p_{2k+1}=−n^{2k} — over-determine the FIRST several coefficients of Ψ. The odd moments being exactly −n^{2k} is a rigid algebraic signature (a geometric series Σ_k −n^{2k}x^{2k+1} = generating function with a DOUBLE pole pattern) that, fed through the Newton recursion, forces a specific factorization structure of Ψ. The extreme root max|η| is then pinned by the dominant-root asymptotics of the recursion (Perron/largest-modulus from the power-sum growth rate p_r^{1/r}→max|η|). The ORDER-3 datum E_3 fixes the third symmetric function, breaking the degeneracy that the second moment alone leaves.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/8/5
- **exactness case:** p_r^{1/r}→max|η_b| is a TWO-SIDED limit (Cauchy-Hadamard for the dominant root): the exact power sums give both lim sup and lim inf of p_r^{1/r}, pinning the spectral radius exactly IF the multiplicity of the extreme root is controlled. The odd-moment exactness (−n^{2k}, all k) gives the limit along odd r directly; E_2,E_3 give even r. Matching odd and even limits = two-sided pin.
- **wall risk:** p_r^{1/r}→max|η| converges to max|η| only as r→∞, but the prize r≈ln q is FINITE; the moment no-go ((qE_r)^{1/2r}≥n ∀r) says the finite-r power-sum root is stuck at ≥n. The odd moments −n^{2k} are dominated by η_0 cancellation structure, not the extreme nonzero root, so they constrain the SHAPE of Ψ but the dominant-root extraction needs ALL r (the same r→∞ wall as the moment method). Genuinely new (exploits odd-moment rigidity + E_3) but risks the Q4 moment-wrap wall.
- **ranker note:** Newton-identity root reconstruction is sound machinery, but it rests on a FALSE premise: the prompt's odd-moment law p_{2k+1}=-n^{2k} is NOT what the nonzero spectrum satisfies (verified: actual law is q*N0-n^r with N0 the hard additive-relation count, p_1=-n not -1, p_3=-n^3 not -n^2), so there is no infinite closed-form moment family; and p_r^{1/r}->B is an r->infinity limit while the prize r~log q is finite and stuck >=n (moment no-go, Q4 wrap) — pure re-proposal of the moment wall.

### [L3.2] Hasse-Davenport reflection: pairing gamma_a with gamma_{-a} forces a quadratic-Gauss-sum factorization of |P|^2 on the antipodal involution  — score 7
- **construct:** The metaplectic phase cocycle, VERIFIED exactly: gamma_a*gamma_b=(J(a,b)/sqrt q)*gamma_{a+b} with |J|=sqrt q, AND the reflection relation gamma_a*gamma_{-a}=chi_a(-1) (unimodular, =1 here). The group Z/m of characters carries the involution a->-a; P(b)=Sum_{a!=0}chibar_a(b)gamma_a splits into the +/- antipodal eigenspaces of this involution (the Weil representation's even/odd theta decomposition).
- **mechanism:** |P(b)|^2 = Sum_{a,a'} chi_a(b)chibar_{a'}(b) gammabar_a gamma_{a'}. Group terms by the involution: the diagonal-antidiagonal pairs (a,-a') with a'=−a give gammabar_a*gamma_{-a}=conj(chi_a(-1)) = a PURE PHASE (Hasse-Davenport), independent of the field. Summing the antipodal-paired contribution over a uses the EXACT relation gamma_a*gamma_{-a}=chi_a(-1) to collapse half the double sum to a SINGLE quadratic-character sum Sum_a chi_a(-1)chi_{2a}(b), whose magnitude is a quadratic Gauss sum (=sqrt q, in-tree gaussSum_normSq) when m is even. The off-diagonal residue obeys the Jacobi cocycle.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/8/5
- **exactness case:** Two-sided because the antipodal pairing is an EXACT algebraic identity (gamma_a gamma_{-a}=chi_a(-1), verified=1), so it pins the antipodal-symmetric part of |P|^2 EXACTLY to a quadratic Gauss sum (known magnitude sqrt q both sides), not a bound. The exactness propagates: |P|^2 = (exact antipodal piece) + (Jacobi-cocycle off-diagonal), and the antipodal piece is a CLOSED FORM. The residual off-diagonal is itself constrained by Parseval Sum|P|^2=m(m-1) (verified=812), giving a second exact equation. Two exact equations on the antipodal-even and antipodal-odd halves bracket max|P| two-sidedly.
- **wall risk:** In the prize regime m=(q-1)/n is typically ODD (verified: q=233->m=29, q=41->m=5), so Z/m has NO order-2 element and the quadratic character is absent from the gamma_a; the antipodal involution a->-a is then FIXED-POINT-FREE on the nonzero a, which is good (clean pairing) but means the 'quadratic Gauss sum factor' must be replaced by the Hasse-Davenport relation for the 2-fold cover, living in the metaplectic group not F_q itself. Risk: the off-diagonal Jacobi residue is exactly the non-contractive |J|=sqrt q wall.
- **ranker note:** REFUTED its own premise: verified m=(q-1)/n is EVEN in the actual prize regime (q=4129/12289/65537 all give even m), inverting the 'm typically odd' rescue; and it explicitly concedes the off-diagonal Jacobi residue IS the non-contractive |J|=sqrt q wall, so the antipodal-exact piece leaves the dominant term at the wall.

### [L5.4] Deligne-Lusztig / Gauss-Heilbronn covering: periods as Frobenius traces on a DL-type curve with known zeta  — score 7
- **construct:** The Gauss-Heilbronn covering curve of Deligne-Lusztig type (arXiv:2512.11288): the μ_n-cover of 𝔸¹ whose Artin-Schreier–Kummer structure realizes the dyadic Gaussian periods as point-count contributions. Its zeta function ζ(C,T)=∏(1−α_iT)/((1−T)(1−qT)) has Frobenius eigenvalues α_i = the period-related algebraic numbers of modulus √q.
- **mechanism:** The DL-type curve C = {y^q − y = x^n} (Artin-Schreier-Kummer) has a μ_n × (additive) automorphism group; its H¹ decomposes under the character group into eigenspaces indexed by (additive char ψ_b, multiplicative char), and each eigenvalue is EXACTLY a Gauss-sum / period combination. The period η_b appears as the partial Frobenius trace on the ψ_b-isotypic piece. Because C is a quotient of a Deligne-Lusztig variety, its Frobenius eigenvalues are KNOWN explicitly (DL theory gives them as products of Gauss sums / roots of unity times √q), pinning the periods to that explicit algebraic list.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/6/3
- **exactness case:** Deligne-Lusztig varieties have EXPLICITLY KNOWN Frobenius eigenvalues (Lusztig's character-sheaf theory: eigenvalues are roots of unity times a power of √q). If the dyadic period family is a quotient of such a curve, the η_b are pinned to an explicit finite eigenvalue list — two-sided exact, since DL eigenvalues are computed, not estimated. The sup is the max-modulus DL eigenvalue in the ψ_b-isotypic block.
- **wall risk:** High risk: the Artin-Schreier-Kummer curve y^q−y=x^n has genus growing with q, and only a SPECIAL fiber (q = power related to n) is genuinely Deligne-Lusztig with clean eigenvalues — the Fermat-prime / DL corner, which the prize regime (q=n^β generic) avoids. Likely the DL structure is only present at special q (the same vacuous corner as the Jacobi cocycle wall).
- **ranker note:** Misidentifies the source: arXiv:2512.11288 (Ito–Takeuchi–Tsushima) is Gauss–HEILBRONN sums on length-2 Witt vectors (wild/mod-p² additive), 3-typical — NOT the tame multiplicative subgroup Gauss sums over μ_n; the y^q−y=x^n curve is genuinely Deligne–Lusztig only at special q (the Fermat/DL corner the prize regime q=n^β avoids), so clean DL eigenvalues are vacuous — same Jacobi-cocycle |J|=√q corner wall.

### [L6.2] Gross-Koblitz p-adic Gamma factorization of the even power sums  — score 7
- **construct:** The p-adic Gamma function Gamma_p and the Gross-Koblitz formula g(chi) = (-pi)^{wt_p(j)} prod_{i} Gamma_p(<p^i j/(q-1)>), applied to expand each eta_b = (-1 + sqrt(q) P(b))/m with P(b) = sum over nontrivial chi of chibar(b) tau(chi)/sqrt(q), and tau(chi) replaced by its Gross-Koblitz Gamma-product.
- **mechanism:** Each even power sum T_{2k} = sum_b eta_b^{2k} expands into a sum of products of 2k Gauss sums tau(chi_1)...tau(chi_{2k}) with sum of characters = trivial (the only surviving archimedean-large terms). Gross-Koblitz replaces each tau by a p-adic Gamma product; the Hasse-Davenport PRODUCT relation collapses character-sum constraints. The surviving T_{2k} is then an EXACT sum of Gamma_p-products evaluated at the dyadic fractions <p^i j/(q-1)>. For n=2^mu the relevant j are multiples of n, so the fractions <p^i j n/(q-1)> = <p^i j /m> live on Z/m — exactly the quotient dual group. This gives a CLOSED p-adic-Gamma expression for each even T_{2k}, feeding A1's Newton reconstruction with explicit (not merely numeric) integers.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/7/4
- **exactness case:** Two-sided: Gross-Koblitz is an EQUALITY (g(chi) equals the Gamma product exactly, no error term), unlike Stickelberger's congruence which is only mod p. The full p-adic Gamma evaluation pins the integer T_{2k} exactly (its archimedean value is recovered by the product formula |T_{2k}| determined since T_{2k} is a rational integer and its other-prime valuations are forced by Gamma_p reflection Gamma_p(x)Gamma_p(1-x)=+-1). So it gives the exact value of each Newton coefficient, not a bound.
- **wall risk:** Gamma_p and Gross-Koblitz are NOT in Mathlib (only gaussSum, JacobiSum, gaussSum_frob exist) - full Lean formalization is a large project. The dyadic-fraction <p^i j/m> evaluation is the Fermat-prime corner unless m has large odd part (prize regime m has huge odd part = generic Gamma_p values, not closed). High novelty but feasibility low; best as a closed-form GENERATOR for the even T_{2k} that A1 then consumes, stated as a named Prop.
- **ranker note:** Gross–Koblitz is a genuine equality (vs Stickelberger's congruence) but the dyadic fractions ⟨p^i j/m⟩ give closed Γ_p values only in the Fermat-prime corner; prize-regime m has huge odd part → generic non-closed Γ_p, so it produces the SAME even T_{2k} that wrap, just re-expressed; Γ_p/Gross-Koblitz absent from Mathlib makes feasibility very low for a route that doesn't close anyway.

### [L6.3] Field-discriminant closed form pinning the root diameter (B via 2B = max gap)  — score 7
- **construct:** The discriminant disc(Psi) = prod_{i<j}(eta_i-eta_j)^2 of the cyclotomic period equation, which has a KNOWN closed analytical form (field discriminant of cyclotomic period equations, arXiv:1912.06921) factoring as a product of prime powers determined by Stickelberger valuations; paired with the probe-observed identity max pairwise gap = diam(root set) ~ 2B (the polynomial has a near-extremal pair {eta near +B, eta near -B}).
- **mechanism:** disc(Psi) = (prod of pairwise gaps)^2 is an EXACT integer with a Stickelberger-closed factorization. Combined with the EXACT integer power sums T_k (which fix sum of gaps^2 = m*T_2 - T_1^2 etc.) we get a system: the m(m-1)/2 pairwise gaps have a known product (disc) AND known low moments (from T_2,T_4). The probe shows the SPREAD is dominated by one antipodal pair realizing diam ~ 2B (because negation x->-x on mu_n maps eta_b -> eta_{-b}=conj, and for the worst b the real part saturates). So pinning the extreme gap = pinning B. The discriminant's exact value plus the antipodal symmetry (negation-closure for n even, MEMORY) gives a two-point extremal equation.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 7/7/5
- **exactness case:** Two-sided: disc is an EXACT integer (an equality with the Stickelberger product formula), and 2B = max-gap is a geometric equality once the extremal antipodal pair is identified. Pairing an exact product (disc) with exact low moments (T_2,T_4) over the gap multiset is a closed determinant/resultant system; for n even the antipodal pairing eta_b, -eta_b? (verify) reduces the extremal gap to a single algebraic-integer modulus, pinning B exactly rather than bounding it.
- **wall risk:** The full discriminant constrains the PRODUCT of all gaps, not the single max gap, so extracting B from disc alone is underdetermined (a one-sided AM-GM here re-hits flatness). The mitigation - antipodal extremal pair - needs the negation-closure structure (n=2^mu even, so -1 in mu_n only if 2|n, true) to actually FORCE the extreme pair, which is an additional lemma. Medium risk; exploits genuinely new exact data (disc closed form) the project has not used.
- **ranker note:** The cyclotomic-period discriminant (Gallas 1912.06921) closed form is just ±p^{e−1}, a pure prime power carrying ZERO spread information — it constrains the PRODUCT of all gaps, not the single max gap (the attack concedes this), so extracting B re-hits flatness/AM-GM; the antipodal-pair mitigation needs an extra forcing lemma and is itself a one-sided diameter heuristic.

### [L12.2] Perfect large-sieve equality + Hankel positivity gap on the dual Z/m  — score 7
- **construct:** Davenport–Halberstam / Montgomery large sieve specialized to the COMPLETE character group of Z/m with unimodular coefficients γ_χ = G(χ)/√q (root numbers). The b run over a full set of m coset-reps, so the 'well-spaced points' are the m points of Z/m at perfect spacing 1/m — the boundary case where the large-sieve inequality Σ_b|P(b)|² ≤ (m + Q)Σ|a_χ|² becomes the EXACT Parseval equality Σ|P|² = m(m−1) (VERIFIED: ratio = m exactly).
- **mechanism:** Because the large sieve is an EQUALITY (not inequality) here, ALL the slack between Σ|P|²=m(m−1) and max|P|² lives in the higher-moment Hankel structure of the value-distribution of {|P(b)|²}. The dual fourth moment Σ_b|P(b)|⁴ = q·E_2(μ_n)·(closed form) is EXACT (it is the bridge transform of M4_full=q·E₂). With two exact moments (Parseval Σ|P|²=m(m−1) and the exact 4th moment), the sharpest two-sided pin of max|P|² is the Hankel/Cantelli extremal: max ≤ μ + σ·√((m−1)·(1−...)) with equality iff the distribution is two-point. Test whether the Gauss-phase distribution IS two-point (an explicit binary-spectrum check), which would make Cantelli SHARP and pin max|P| exactly.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 8/9/6
- **exactness case:** Cantelli's inequality (one-sided Chebyshev) is ATTAINED with equality precisely by a two-point distribution with the given mean and variance. If the empirical distribution of |P(b)|² is provably two-valued (which the e₂=0 orbit structure and the exact equality of M4_full=q·E₂ both point toward in the dyadic case — the value-multiplicity is binary), then max|P|² = mean + σ·√(weight-ratio) is EXACT, both sides. The two exact moments (Parseval + exact 4th) then DETERMINE both the values and the largest, with no residual inequality.
- **wall risk:** If the value distribution has ≥3 distinct values, Cantelli is only one-sided; one then needs the exact 6th moment (E_3, available, order-3 exact) to add a third Hankel constraint. Risk: the distribution may have an unbounded number of values in the inflation regime, where the char-p anomaly (the cases where M4_full/qE₂ > 1) breaks the exact 4th moment. But those are exactly the small-q corner; in q ≫ n³ prize regime M4_full = q·E₂ holds exactly (VERIFIED), so the 4th moment lever is live.
- **ranker note:** Cantelli is sharp ONLY for a two-point distribution; the periods' |P(b)|^2 distribution is NOT two-point (my probe: ~m distinct values), and the in-tree M4=q*E2 exactness gives only one extra moment beyond Parseval — two moments cannot pin a ~m-valued distribution, so the 'binary spectrum' premise is empirically false and the attack collapses to the same finite-determinacy wall as L12.1/L12.5.

### [L15.5] Davenport-Hasse base-change tower: dyadic Gauss-phase self-similarity fixes the order-3 anomaly  — score 7
- **construct:** The Davenport-Hasse relation G_{F_{q^s}}(chi o N) = (-1)^{s-1} G_{F_q}(chi)^s relates Gauss sums up the field tower F_q subset F_{q^2} subset ... For the dyadic subgroup mu_{2^mu}, lift through the 2-power tower; the period eta_b in F_q is the trace of a period in an extension, giving an EXACT recursion eta^{(mu)}_b in terms of eta^{(mu-1)}. The third-power-sum E_3(mu_{2^m})=15n^3-45n^2+40n (the ORDER-3 exact datum) is the base case of this recursion.
- **mechanism:** Use Davenport-Hasse to write the level-mu Gauss phase as the s-th power of a base phase. The dyadic tower mu_2 subset mu_4 subset ... subset mu_{2^mu} gives a Davenport-Hasse cocycle relating eta at consecutive levels. The EXACT E_3 = 15n^3-45n^2+40n (order-3) is the first place the dyadic period DEVIATES from the generic Sidon energy 3n^2-3n at second order; this order-3 excess is precisely the antipodal/negation-closure term (-1 in mu_n iff 2|n). Davenport-Hasse turns this into an EXACT functional equation eta^{(mu)} = f(eta^{(mu-1)}) whose fixed-point/iteration determines the sup at the top level from the (small, exact) base level.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 7/7/4
- **exactness case:** Two-sided: Davenport-Hasse is an EXACT identity (equality, not bound), and E_2,E_3 are exact at every level. The recursion preserves exactness: if the sup is pinned at level mu-1, the DH relation gives it exactly at level mu (the s-th-power lifting multiplies the phase, the trace-down is a fixed averaging). Pinning the base case (mu small, where the provable-regime sqrt-free bound already gives the exact delta*) and iterating the EXACT recursion yields the exact value at prize depth -- a two-sided induction, not a one-sided estimate.
- **wall risk:** Medium: Davenport-Hasse lifts within ONE prime's tower (F_q to F_{q^s}), but the prize regime fixes q and varies the subgroup -- the relevant tower is mu_2 subset mu_4 in a FIXED F_q, where DH does not directly apply (it needs field extension). The honest version is the in-tree exact recursion ||eta||^2+||eta^chi||^2=2(||A||^2+||B||^2) (memory: parallelogram tower) -- DH is the multiplicative analogue. Risk = the per-level descent shows cos=1 alignment -> sub-maximality but does NOT close (open BGK). EXPLOITS order-3 E_3 directly as required.
- **ranker note:** Davenport-Hasse lifts within ONE prime's field tower (F_q -> F_{q^s}), but the prize regime fixes q and varies the subgroup mu_2 < mu_4 in a FIXED field where DH does not apply; the honest analogue is the already-landed parallelogram tower that shows cos=1 alignment -> sub-maximality but does NOT close (open BGK). It also leans on the non-universal order-3 p3=-n^2 as its base case.

### [L17.6] Carathéodory-Toeplitz positive-definite extremal-measure determinacy  — score 7
- **construct:** The Toeplitz moment problem on Z/m: |P(b)|² is the DFT of the positive-definite autocorrelation sequence A=(A(0),...,A(m-1)); A is a positive-definite (Toeplitz-PSD) sequence with PRESCRIBED exact moments from the period-moment law. The Carathéodory-Fejér extremal measure attains the max of the spectral density.
- **mechanism:** Frame max|P|² as the maximum of a positive measure μ on the circle (the discrete spectrum of A) subject to its trigonometric moments being the EXACT autocorrelation lags A(0)=m-1, A(1),A(2),A(3) (fixed by Parseval, E_2, E_3, and the odd moment p_3). The Carathéodory-Toeplitz theorem says: a PSD Toeplitz section determines a UNIQUE extremal (Carathéodory) measure that is finitely-atomic, and the spectral peak of ANY admissible μ is sandwiched between the Carathéodory atomic mass (upper) and the maximum-entropy density (lower) — a CLOSED INTERVAL. The width of this interval shrinks to zero (determinacy) exactly when the Hamburger/trigonometric moment problem becomes DETERMINATE, which the Riesz criterion ties to the growth of the known moments. The odd-moment family p_{2k+1}=−n^{2k} provides an INFINITE explicit moment subsequence (all odd lags via the period law), and its EXACT geometric form −n^{2k} is a determinacy certificate (the moments grow like n^{2k}=(n²)^k, sub-factorial, so by Carleman's condition Σ p_{2k}^{-1/2k}=Σ 1/n diverges ⟹ DETERMINATE moment problem ⟹ unique measure ⟹ EXACT peak).
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/8/5
- **exactness case:** Carleman's condition Σ m_{2k}^{-1/(2k)}=∞ is a TWO-SIDED determinacy theorem: when satisfied, the moment problem has a UNIQUE solving measure, so its spectral peak max|P| is EXACTLY determined by the moments (no upper/lower gap). The odd-moment data −n^{2k} (exact, all k, from the period law) is precisely an explicit moment sequence whose growth can be checked against Carleman — turning 'incomputable sup' into 'unique-measure-determined value'.
- **wall risk:** The η-spectrum has m-1 atoms (finite measure on a finite group), so the trigonometric moment problem is intrinsically DETERMINATE already — the issue is COMPUTING the unique measure's peak from finitely-many CLOSED-FORM moments (only lags 0-3 + odd lags are closed-form; even lags ≥4 = open char-p E_r). Carleman gives determinacy but not the peak VALUE without all moments. The odd-moment subsequence −n^{2k} alone determines only the SYMMETRIC (even) part of A. Risk: determinacy is free (finite group) so the real content is again 'need ~ln m exact even moments', same wall — but the Carathéodory atomic extremal measure is a sharp, untried two-sided bracket worth a probe.
- **ranker note:** Carleman-determinacy is FREE and vacuous here (the eta spectrum is a finite atomic measure on a finite group, hence already determinate), so the framing supplies no content; the Carathéodory atomic extremal measure still needs the value of the unique measure's peak, which requires ~log m exact even moments (E_4 onward = char-p open), and the claimed determinacy certificate rests on the same false -n^{2k} odd-moment premise as L17.3 — reduces to the moment wall with a determinacy theorem that gives nothing.

### [L2.5] Jacobi theta transformation / Landsberg–Schaar reciprocity: fold the incomplete subgroup sum into a complementary SHORT sum whose length-1 term is exact  — score 6
- **construct:** Landsberg–Schaar reciprocity Σ_{n mod c} e(n²a/c) = (factor)·Σ_{n mod a} e(−n²c/4a), the finite-field shadow of the Jacobi theta transformation θ(−1/τ)=√(−iτ)θ(τ) (modular S-transform on Γ_θ⊂SL(2,ℤ)). For the subgroup sum, use the multiplicative analogue: η_b = Σ_{x∈μ_n} e_p(bx) folds under x↦x^{-1} (μ_n is inversion-closed) and under the Frobenius/dilation x↦x^d, giving a functional equation η_{b} ↔ η_{b'} on a complementary subgroup.
- **mechanism:** The modular S-transform exchanges Im τ → 1/Im τ, i.e. a sum of length L ↔ a sum of length q/L. For μ_n (length n) this exchanges with a sum of effective length m=(q−1)/n over the dual cosets — but the SELF-DUAL / fixed point of this involution (where n²≈q, or the antipodal/inversion fixed structure) is where the max is achieved, and AT the fixed point the transformation becomes an exact identity B = √(factor)·B, forcing B = (factor)^{1/(2(1−1))}... i.e. pinning the ratio B²/(self-dual scale). The inversion-closure of μ_n (in-tree: SubgroupGaussSumCosetInv) is the multiplicative S-transform fixed structure.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 10/6/4
- **exactness case:** Two-sided at the FIXED POINT: a genuine functional equation B = T(B) where T is the modular transform pins B exactly (it is an eigenvalue equation, the fixed point is unique), analogous to how θ's S-transform pins the Gauss-sum value √q EXACTLY (the classical c=1 Landsberg–Schaar IS the evaluation of the quadratic Gauss sum). The order-3 structure enters via the order-3 element of the modular group (the ST fixed point ρ=e^{2πi/3}) where the theta-value is a known special value (CM point at the cube root of unity).
- **wall risk:** HIGH. Landsberg–Schaar is for QUADRATIC (additive-quadratic) Gauss sums; the subgroup sum η_b is a MULTIPLICATIVE-subgroup sum, so the 'S-transform' is the multiplicative-Fourier (γ_χ) side, which is the documented Jacobi-cocycle |J|=√q NON-contractive wall. The fixed-point argument needs the transform to be a genuine CONTRACTION/eigen-map, which the workbench says it is not in the Fermat corner. The order-3 CM point ρ angle is the freshest piece and may evade this if the cubic Davenport–Hasse relation (E_3 exact) provides the missing contraction.
- **ranker note:** Landsberg-Schaar is for additive-quadratic Gauss sums; the multiplicative-subgroup analogue IS the gamma_chi side = the documented |J|=sqrt(q) non-contractive Jacobi-cocycle Fermat-corner wall (author concedes), and the fixed-point eigenvalue argument needs a contraction the workbench says does not exist — re-proposes the dead non-contraction with a thin, unsupported order-3 CM-point escape.

### [L6.4] Newton polygon of Psi at p forces the L^2-L^infinity ratio (anti-flatness)  — score 6
- **construct:** The p-adic Newton polygon of the period polynomial Psi(X) over Q_p, whose slopes are the Gross-Koblitz valuations nu_p(eta_b) = (contributions of wt_p(j)/(p-1) digit-sums), governing how the m periods distribute p-adically; combined with the archimedean Parseval ceiling sum|P|^2 = m(m-1).
- **mechanism:** Gross-Koblitz gives nu_p(g(j)) = wt_p(j)/(p-1) EXACTLY, so the p-adic sizes of the eta_b (equivalently the Newton slopes of Psi) are EXPLICITLY the base-p digit-sums of the coset indices j. The Newton polygon thus has EXPLICIT vertices. The mechanism: a polynomial whose p-adic root-size distribution (Newton polygon) is HIGHLY UNEVEN (many distinct slopes from varied digit-sums) cannot have an archimedean-FLAT root set - there is a product-formula tension. Specifically, the local-global product formula prod_v |eta_b|_v = 1 ties the archimedean |eta_b| to the p-adic |eta_b|_p = p^{-slope}; integrating over the m periods against the EXACT Parseval ceiling sum|P|^2=m(m-1) yields a two-sided pin: the archimedean variance is forced by the p-adic slope spread.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/6/4
- **exactness case:** Two-sided via the PRODUCT FORMULA (an equality prod_v |x|_v = 1 for algebraic numbers): the p-adic side is EXACTLY computed by Gross-Koblitz digit-sums (no slack), and the archimedean L^2 is EXACTLY m(m-1) (Parseval). Equating the two through the product formula at all places turns a one-sided moment into a CONSERVED quantity, so the sup is squeezed between the L^2 floor and the slope-forced ceiling - both exact.
- **wall risk:** Product-formula tension typically yields HEIGHT lower bounds (Mahler measure), which are L^2-ish averages - re-hits the L^2->L^infinity sqrt wall (W_A) since p-adic data controls the product/average not the max. The dyadic digit-sums wt_p of multiples of n=2^mu may degenerate (Fermat corner). Genuinely novel construct but high risk of collapsing to an average; value is the EXACT slope data which no prior attack used.
- **ranker note:** Product-formula tension (∏_v|η|_v=1) with exact Gross-Koblitz digit-sum slopes is novel data, but the product formula constrains a PRODUCT/height — an L²-type average — so it yields Mahler-measure lower bounds, the L²→L∞ √ wall (W_A) restated; dyadic wt_p of multiples of 2^μ also degenerate to the Fermat corner; the slope data is exact but controls the wrong (average) functional.

### [L8.4] Failure-of-Ramanujan defect = exact non-Ramanujan excess via Alon-Boppana-Serre and the trace of A^{2k}  — score 6
- **construct:** The Cayley graph Cay(F_q,mu_n) is provably NOT Ramanujan in the constant-n direction (verified: n=4,m=15 gives M=3.685 > 2sqrt(n-1)=3.464). Quantify the defect using the Alon-Boppana / Serre asymptotic-eigenvalue theory in REVERSE: tr(A^{2k}) = Sum_b eta_b^{2k} = q * (number of closed 2k-walks per vertex) = q * (Sum_{x_i in mu_n} 1_{Sum +- x_i = 0}) = q * (additive 2k-th moment / structure).
- **mechanism:** Serre's quantitative Alon-Boppana: a fixed positive proportion of eigenvalues exceed 2sqrt(n-1)-epsilon. The closed-walk count tr(A^{2k}) is EXACTLY the additive energy moment E_k(mu_n) times q (the in-tree pairing-census data, (2r-1)!! Wick term). So the deviation of M from the Ramanujan value 2sqrt(n-1) is governed by the EXCESS of E_k over the Kesten-McKay (tree/random-walk) baseline binom(2k,k)*n^k/(k+1)... The non-Ramanujan defect M^2-4(n-1) equals the leading excess in E_k - Kesten-McKay_k, which the in-tree exact E_2,E_3 (=3n^2-3n vs Sidon-generic 2n^2-n) PIN at order 3.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/7/5
- **exactness case:** Two-sided: tr(A^{2k}) >= M^{2k} (one eigenvalue) AND tr(A^{2k}) <= q*M^{2k} (all q), giving (tr/q)^{1/2k} <= M <= tr^{1/2k}, with tr(A^{2k}) = q*E_k EXACTLY (closed walks = additive moment). The Kesten-McKay measure is the EXACT limiting spectral distribution of n-regular trees; the DEFECT (E_k minus its Kesten-McKay value) is computable from the known excess E_2=3n^2-3n, E_3=15n^3-45n^2+40n (antipodal/negation-closure excess), pinning the deviation order-by-order rather than bounding it.
- **wall risk:** This is the moment NO-GO and the Bessel-wraps-at-r* wall in graph-theoretic clothing: tr(A^{2k})=q*E_k is the same moment data, and the Kesten-McKay baseline is the char-0 Wick term that wraps. Novelty is the Alon-Boppana-Serre DEFECT framing (excess as non-Ramanujan obstruction), but the analytic core is the known excess; risk it just re-expresses the wall.
- **ranker note:** Alon-Boppana-Serre 'non-Ramanujan defect' is the moment no-go (W2) and the Bessel-wraps-at-r* wall (W3) in graph-theoretic clothing: tr(A^{2k}) = q*E_k is literally the same additive-moment data, and the Kesten-McKay baseline is the char-0 Wick term that wraps. Confirmed Cay(F_q,mu_n) is non-Ramanujan (B/Ramanujan = 1.4 -> 2.1) but the defect is governed by the known E_2/E_3 excess; the framing is a relabel, not new analytic content. Re-proposes W2/W3.

### [L8.5] Abelian-lift / signed-graph (Bilu-Linial) realization: M as the worst signing eigenvalue with exact 2-cover data  — score 6
- **construct:** Cay(F_q,mu_n) for n=2^mu is an iterated ABELIAN 2-LIFT (Bilu-Linial / Friedman): mu_n <- mu_{2n} doubles via the index-2 subgroup, and the lift is signed by the quadratic character of the coset. The whole tower is a sequence of signed double covers of the base graph Cay(F_q, F_q*) (complete graph K_q), with signings = additive characters restricted to mu_n. eta_b = the signed-adjacency eigenvalue of the b-twisted signing.
- **mechanism:** Bilu-Linial theory: a graph has a 2-lift whose new eigenvalues are the eigenvalues of a SIGNED adjacency matrix A_s; the new spectral radius = max over signings of rho(A_s). Here the signing is NOT free — it is FORCED by the additive character e_p(b * .), so eta_b is one specific signed-adjacency eigenvalue, and M = max over the m FORCED signings (one per coset b). Friedman/Marcus-Spielman-Srivastava interlacing-families theory bounds the worst signing eigenvalue by the matching polynomial / expected characteristic polynomial of the signing ensemble; the expected char poly over the m signings has roots = exact period averages, pinning M via the largest root of the expected (integer) polynomial = Psi again, but now with the MSS real-rootedness giving a one-line max-root bound.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/6/4
- **exactness case:** Two-sided in the interlacing-families sense: MSS proves the worst eigenvalue over a signing ensemble is <= largest root of the EXPECTED characteristic polynomial (upper) AND >= for at least one signing (lower, existence), and the expected char poly over the FULL forced ensemble of m signings is exactly the period polynomial Psi (its r-th coefficient = the r-th symmetric function = exact Newton data). For the forced (non-random) ensemble the max is ACHIEVED, so the largest root of Psi IS M exactly, not a bound.
- **wall risk:** MSS interlacing-families bounds the worst over RANDOM/free signings; our signings are deterministic and correlated (forced by characters), so the expected-char-poly argument gives the average period not the max. Bridging average-to-max is the same L^2->L^infinity sup-norm sqrt (Wall A). High novelty (no one has framed eta_b as Bilu-Linial forced signings) but the closure needs the same Cauchy-Schwarz gap.
- **ranker note:** High novelty (no one has framed eta_b as Bilu-Linial FORCED character signings) but MSS interlacing-families bounds the worst over RANDOM/free signings via the expected char poly = average behavior; our signings are deterministic and correlated, so the expected poly's max root is the AVERAGE period, not the max. I measured the max/RMS gap = 2.7 -> 4.1 GROWING with n — exactly the L2->Linf sup-norm sqrt (Wall A) that MSS leaves wide open. Reduces to the central wall by the proposal's own admission.

### [L9.3] Three-point Schrijver SDP exploiting the negative odd moments  — score 6
- **construct:** The Terwilliger algebra / three-point semidefinite hierarchy on the multiset of eta_b values (NOT just |eta_b|^2). The odd power sums Sum_b eta_b^{2k+1} = -n^{2k} (exact, known) are genuine THREE-POINT correlations: they measure the signed/asymmetric structure that a 2-point LP cannot see.
- **mechanism:** The 2-point Delsarte LP only sees even moments (|eta|^2-type) and is symmetric under eta -> -eta, so it can never beat the symmetric envelope. The exact NEGATIVE odd moments break this symmetry: Sum eta^3 = -n^2 < 0 forces the distribution of Re(eta_b) to be left-skewed by a precise amount. The Schrijver 3-point SDP encodes triple correlations Sum_b eta_{a+b} eta_b eta_c as PSD constraints; feeding in the exact value -n^{2k} as linear constraints on the SDP variables tightens the upper bound on max|eta| below the symmetric LP value. The SDP dual produces a sum-of-squares certificate whose value is the pinned max.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/7/4
- **exactness case:** SDP duality (strong duality, Slater interior point) gives a matching primal-dual pair: the SOS certificate (dual) upper bounds max|eta|, and a feasible measure (primal) attaining it lower bounds it; when the exact odd-moment constraints make the SDP a single-point feasible set (overdetermined by the order-3 dyadic identities), the two-sided values coincide. The order-3 emphasis is exact here: E_3 and Sum eta^3 together give 4 of the needed SDP moment inputs in closed form.
- **wall risk:** 3-point SDPs are notoriously hard to solve in closed form and the Terwilliger algebra of the cyclic scheme on Z/m is large; the odd moments are O(n^{2k}) which is tiny relative to the n^{2k+1}-scale total, so the asymmetry signal may be too weak to move the upper bound past sqrt(n) — risk of giving the same Cauchy-Schwarz L2->Linf sqrt as the 2-point bound (wall W_A). Computational closed-form extraction is the main feasibility risk.
- **ranker note:** Premise broken: I computed Sum_b eta_b^3 = -n^3 (not -n^2 as claimed), so the odd moments are the SAME ORDER as the mean-cube, not a delicate skew that tightens the symmetric envelope; mechanism mis-founded and 3-point Terwilliger SDP closed-form extraction is intractable (collapses to the wall-W_A sqrt).

### [L12.4] Subconvexity-as-equality via the exact second/fourth-moment functional equation of the period polynomial  — score 6
- **construct:** The period polynomial Ψ(T) = Π_b (T − η_b) whose power-sum coefficients are EXACTLY the odd/even moments (Σηᵢ^{2k+1}=−n^{2k}, Σ|η|²=qn−n², M4_full=q·E₂). Treat the generating Dirichlet series L_Ψ(s)=Σ_b |η_b|^{-2s} or the spectral zeta ζ_μ(s)=Σ_b (|η_b|²)^{s} as an L-function; max|η_b|² is its rightmost 'spectral pole'/edge, recoverable from the residue/growth via a Tauberian / subconvexity argument applied to the EXACTLY-known moment Dirichlet coefficients.
- **mechanism:** The spectral zeta ζ_μ(−r) = Σ_b|η_b|^{2r} = q·E_r − n^{2r} is known exactly at ALL negative integers r where E_r is pinned (r=2,3 unconditional). The edge max|η_b|² = exp(lim_{r→∞} (1/r)·log ζ_μ(−r))? No — better: use the EXACT meromorphic continuation. Since the moments grow like (2r−1)!!nʳ (Wick) with a precise order-3 correction, the moment GENERATING function Σ_r ζ_μ(−r) z^r/r! is an explicit Bessel-type entire function whose order/type pins the support edge by the Phragmén–Lindelöf / Pólya theorem (the type of the entire function = the support edge of the spectral measure). Order-3 exactness fixes the first correction to the Bessel type, separating the char-0 Wick edge (√(2n ln(q/n))) from the true edge.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 10/6/3
- **exactness case:** Two-sided because the Pólya–Bernstein theorem says the exponential TYPE of the moment generating (Laplace-transform) entire function EQUALS the support edge of the underlying measure — an identity, not a bound. The moment generating function is exactly Σ_r (q·E_r−n^{2r})z^r/(2r)! whose growth type is computable from the exact E_2, E_3 plus the Wick tail, and the type IS max|η_b|². The functional-equation/symmetry of Ψ (odd-moment = −n^{2k} exact, an exact symmetry) constrains the continuation on both sides.
- **wall risk:** Pólya type needs the moment ASYMPTOTICS (all r), but E_r is only exactly known for r ≤ β before the char-p wrap (Q4: Wick matches to depth r*~β then deviates). So the entire-function type uses the WRONG (char-0 Wick) tail past r*, giving the char-0 edge √(2n ln(q/n)) — which is the conjecture, not a proof. Mitigant: the order-3 EXACT correction (E_3 deviation from Wick = the 40n − 45n² term) quantifies the FIRST char-p deviation; if the deviations form a geometric/computable series (testable: measure E_r/Wick ratio vs r), the type can be summed exactly. This is the genuine open core but order-3 gives the leading correction sign.
- **ranker note:** Polya-type=support-edge is a true theorem but needs the moment ASYMPTOTICS (all r), and E_r is exactly known only to r<=beta (char-0 Wick) before the documented Q4 wrap; my probe confirms E_r/Wick falls steeply and non-geometrically (char-p deflation), so the entire-function type computed from the Wick tail returns the CONJECTURE sqrt(2n ln(q/n)), not a proof — its own risk note concedes this is the open core.

### [L14.5] Reshetikhin-Turaev surgery on the Z/m abelian U(1) TQFT: B as the modular kernel of a lens-space partition function  — score 6
- **construct:** Abelian (U(1)_k / Z/m) Reshetikhin-Turaev TQFT: the S and T matrices are S_{ab}=e^{2πi ab/m}/√m, T_{ab}=δ_{ab}e^{πi a²/m}; partition functions of lens spaces L(p,1) are Gauss sums Σ e^{πi a²·.../m}. Reciprocity (the RT/Witten-Reshetikhin-Turaev exact evaluation of abelian Chern-Simons on lens spaces) gives EXACT closed forms for these as products of classical quadratic Gauss sums (Jeffrey, Garoufalidis). Map the period DFT P(b)=Σ_{χ≠1} χ̄(b)γ_χ onto the Z/m modular kernel with the Gauss phases γ_χ as the T-matrix twists.
- **mechanism:** P(b) is literally a finite Fourier transform on Z/m of the unimodular Gauss-phase vector (γ_χ); this IS the action of the abelian-TQFT S-matrix on the twist vector. RT surgery reciprocity (Deloup, Turaev) evaluates such S·(twists) sums EXACTLY when the twists are themselves quadratic Gaussians — and the prize phases γ_χ = G(χ)/√q ARE Gauss-sum phases. Use the Gauss-sum reciprocity / Landsberg-Schaar identity (the abelian-TQFT modular reciprocity) to fold the m-point DFT of γ_χ into a SHORTER Gauss sum (length ≈ gcd or √m), collapsing max|P| to a computable extreme of a quadratic Gaussian whose value is ±√(something) exactly.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 10/6/4
- **exactness case:** Landsberg-Schaar / quadratic-Gauss reciprocity gives EXACT values (modulus √m or 2√m with explicit phase) for quadratic-Gaussian DFTs, not bounds. If the prize phases γ_χ can be shown to carry an exact quadratic (degree-2) phase structure in χ (i.e. γ_χ behaves like e^{2πi c·ind(χ)²/m} up to a controlled error), then max|P| equals an exact quadratic-Gauss-sum modulus — a closed form like √(2m) — giving B = √q·√(2m)/m = √(2n)·(exact), matching the Paley B≤2√n side TWO-sidedly.
- **wall risk:** The Gauss phases γ_χ = G(χ)/√q are NOT pure quadratic Gaussians in the index of χ (only the QUADRATIC character gives a clean ±1; higher-order characters give honest Gauss sums whose phases are NOT a quadratic form). The Jacobi-cocycle non-contraction wall: the phase combination law γ_a γ_b = (J(a,b)/√q) γ_{a+b} is multiplicatively twisted by a Jacobi sum of modulus √q, so the DFT is NOT a clean abelian-TQFT modular kernel. Likely yields exact answer only at the Fermat corner — must escape the cocycle.
- **ranker note:** Directly re-proposes a prompt-listed wall: the Jacobi-cocycle gamma_a gamma_b = (J/sqrt q) gamma_{a+b} with |J|=sqrt q is non-contractive, so the gamma_chi are NOT pure quadratic Gaussians (only the n=2 quadratic char is clean) and the DFT is not an abelian-TQFT modular kernel; Landsberg-Schaar gives exact values only at the Fermat corner (vacuous per the dyadic-Gauss explicit-eval wall) — the attack's own risk says 'must escape the cocycle' and offers no escape.

### [L14.7] Anderson-Moore-Vafa rationality + Galois bound on central charge: B forced into a finite root-of-unity-indexed value set  — score 6
- **construct:** Vafa's theorem / Anderson-Moore rationality: in any RCFT the central charge and conformal weights are RATIONAL and the Gauss-sum ratio τ_+/τ_- is a root of unity (a 24th root for the central charge). Categorically (Ng et al, higher central charges): the n-th central charge ξ_n = τ_n/|τ_n| is a root of unity for n coprime to the T-matrix order, and the higher Gauss sums τ_n are 'd-numbers' (algebraic integers whose ratios to conjugates have bounded archimedean size). Apply to the period 'pre-modular' datum: the period power sums p_k are the higher Gauss sums τ_k, and their phases are forced into a finite root-of-unity set.
- **mechanism:** The exact data p_1=−1, p_2=p−n, p_3=−n² are REAL (no phase) — confirming a 'central-charge = 0 mod something' rationality. The Ng-Schopieray d-number theorem says higher Gauss sums τ_n for n coprime to ord(T) equal (first Gauss sum)×(global dim)×(twist), Galois conjugate; bounding B is bounding the archimedean size of a d-number. Because d-numbers have the property that ALL their Galois conjugates have the SAME absolute value (this is the defining 'd-number' = denominator-controlled algebraic integer with conjugate-size symmetry), one extracts that the relevant Gauss-period combination has |conjugate| = exact √(global dim)-type value, pinning B to a value in the finite set {√(divisors of q-related dimension)}.
- **quantity:** multiple | **novelty/relevance/feasibility:** 10/5/3
- **exactness case:** d-numbers (in the Ng-Schopieray sense) are algebraic integers a such that a/ā and all Galois conjugates have controlled, often EQUAL, absolute value — this is an exact algebraic constraint, not analytic. If the period combination giving B is shown to be (a Galois conjugate of) a d-number with global-dimension q, then |B| is exactly determined by the global-dimension factorization: B = √(n)×(exact root-of-unity-free factor), a two-sided exact value rather than a bound. The rationality of p_1,p_2,p_3 (all real integers) is the verified entry point.
- **wall risk:** The d-number conjugate-equal-modulus property is exactly what FAILS for individual Gauss periods (conjugates have wildly different moduli 4.79..0.05, TQFT-4), because a single period is NOT a d-number — only the global Gauss sum G(χ) (modulus √q) is. So the d-number machinery pins √q-scale quantities exactly but says nothing about the SUBDOMINANT B. High risk it only re-derives |G(χ)|=√q. Speculative bridge: needs a NEW 'pre-modular' structure where B itself is a d-number invariant, which would be a real discovery if it exists.
- **ranker note:** The d-number conjugate-equal-modulus property is EXACTLY what fails for individual Gauss periods (conjugate moduli range 4.79..0.05, TQFT-4) — only the global G(chi) of modulus sqrt q is a d-number, so the machinery pins the sqrt q scale and is silent on the subdominant B. The 'new pre-modular structure where B is a d-number invariant' is pure speculation with no entry point beyond the already-known |G(chi)|=sqrt q.

### [L17.1] Tao-Chebotarev prime-index sharp support law  — score 6
- **construct:** DFT on Z/m with m PRIME (choose q=mn+1 so the index m is prime); the m×m prime-order DFT matrix and the Chebotarev theorem that ALL its minors are nonzero. P(b)=Σ_{χ≠1}χ̄(b)γ_χ is the DFT of the unimodular vector γ on Z/m.
- **mechanism:** Tao's sharp uncertainty for cyclic groups of PRIME order gives |supp(γ)|+|supp(P)|≥m+1 with EXACT saturation classified (γ supported on an AP forces P maximally spread, and conversely). Here supp(γ)=m-1 (all χ≠1, γ_χ unimodular hence nonzero), so |supp(P)|≥2, but the contrapositive of Chebotarev pins the spread: P CANNOT concentrate on a small set of b. Combine with the exact Parseval Σ_b|P(b)|²=m(m-1): if P were supported on only s residues, max|P|²≥m(m-1)/s; Chebotarev's nonvanishing-minor property forces s near-maximal (s≥m+1-|supp γ|=2 is weak, but the SHARP form: any near-zero pattern of P of size m-s requires γ to vanish on s-1 places, impossible for unimodular γ unless s=1). This pins max|P| to the flat value √(m-1)·(1+o(1)) two-sidedly when the saturating configuration is excluded by Chebotarev.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/7/4
- **exactness case:** Tao's bound is an EQUALITY-characterized theorem (sharp, with saturators = coset/AP indicators). Two-sided because Chebotarev nonvanishing EXCLUDES the concentration that would push max|P| up, while Parseval (Σ|P|²=m(m-1), proven exact) pushes it down — squeezing max|P| to the flat scale exactly, not bounding one side.
- **wall risk:** Prime-index regime m=2^128+something is a SPECIAL prime choice — the prize wants index m≈2^128 (composite-friendly). Chebotarev controls support COUNT not magnitude profile, so the squeeze to the exact flat constant √(2 ln m) (vs merely √m) is not delivered by support-counting alone; gives the √m floor not the √(2 ln m) target. Hits W_A (the L²→L∞ √) unless the minor-nonvanishing is quantified into magnitude gaps.
- **ranker note:** Tao/Chebotarev prime-order uncertainty + Parseval is real, but it controls SUPPORT COUNT not magnitude profile; the squeeze yields only the flat sqrt(m) floor (square-root cancellation already known), never the sqrt(2 ln m) target — the attack admits it hits W_A — and the prime-index regime m~2^128 prime is a special, non-prize-generic choice that the composite-friendly prize does not supply.

### [L17.5] Hirschman-Beckner entropic uncertainty two-sided squeeze  — score 6
- **construct:** Entropic uncertainty principle on Z/m: H(|γ|²/‖γ‖²)+H(|P|²/‖P‖²)≥log m (Hirschman, sharpened by Beckner via Hausdorff-Young), where H is Shannon entropy of the normalized power distributions. γ is UNIMODULAR so |γ_χ|²≡1: its distribution is exactly UNIFORM, H(γ)=log(m-1) MAXIMAL.
- **mechanism:** Because γ is unimodular (every Gauss phase has modulus 1 exactly), the input entropy H(|γ|²) is EXACTLY log(m-1) — the maximum possible. The entropic uncertainty H(γ)+H(P)≥log m then gives essentially no lower constraint on H(P) (it is already satisfied with slack). BUT run it in REVERSE: the Hausdorff-Young / Beckner inequality is an EQUALITY exactly for Gaussian-type (here, character-translate) functions. Since γ is the maximally-flat input, the sharp Beckner constant forces the OUTPUT P to be maximally SPREAD in entropy too: H(|P|²)≥log m − H(γ) is vacuous, but the DUAL Rényi-2 entropy (collision entropy) H_2(|P|²)=−log(Σ|P|^4/(Σ|P|²)²) is controlled by E_2 (the L4 norm, exactly known from A2). So H_2(P)=2log m − log(‖P‖_4^4/...) is EXACTLY computed. Then max|P|²/‖P‖² ≤ exp(−H_∞(P)) and the Rényi interpolation H_∞≤H_2 with the EXACT H_2 from E_2 brackets max|P| from above, while uniform-input flatness brackets from below.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 10/6/3
- **exactness case:** Beckner's inequality is SHARP (best constant, with explicit extremizers). The unimodular input sits at the entropy-maximizing extreme, so the inequality is tested at its equality boundary — converting it to a near-equality for the output. Combined with the EXACTLY-known collision entropy H_2(P) (=function of E_2, proven exact), the Rényi chain H_∞≤H_2≤H_1 gives a two-sided window on max|P| whose width is fixed by the exact E_2.
- **wall risk:** H_∞≤H_2 gap can be large (collision entropy controls L4 not L∞); the step from exact H_2 (from E_2) to H_∞ (=max|P|) loses exactly the L4→L∞ √-factor = the central wall W_A. Entropic uncertainty caps the PRODUCT of spreads, not the peak magnitude, so it constrains how flat P is on average, not its single tallest spike. Bold/novel (entropy is genuinely untried here) but the L∞ peak is precisely what entropy methods smear over; high risk it only re-derives the L4 data of A2.
- **ranker note:** Hirschman-Beckner entropic uncertainty is novel-here but structurally caps the PRODUCT of spreads, not the single tallest L-infinity spike; the unimodular input gives maximal H(gamma)=log(m-1) which makes the lower bound vacuous, and the only quantitative output is the collision entropy H_2 = the L4 data (= A2 / L17.2 re-derived), with H_infty<=H_2 losing exactly the L4->Linf sqrt-factor = wall W_A — entropy methods smear over the very peak the prize asks for.

### [L2.3] Eisenstein-series special value: realize Σ|P|² and max|P| as Fourier coefficients of a weight-(m−1) Eisenstein/theta object with Hecke-controlled coefficients  — score 5
- **construct:** The phase vector (γ_χ)_{χ≠1} of unimodular Gauss phases as the Fourier-coefficient vector of a (twisted) Eisenstein series E attached to the Dirichlet characters χ mod q of order dividing m; P(b)=Σ_χ χ̄(b)γ_χ is then the value of E (or its Eichler integral / period polynomial in the Zagier sense) at a point indexed by b. Use the explicit Eisenstein period-polynomial formula (Bernoulli numbers, ζ-values) and the rationality of Eisenstein periods (arXiv:2410.20385).
- **mechanism:** max|P| becomes sup over b of an Eisenstein value. The Hecke eigenvalue structure of Eisenstein series gives multiplicativity γ_{χ₁}γ_{χ₂}=J·γ_{χ₁χ₂}/√q (Jacobi-sum cocycle, in-tree as MixedGaussSum), turning {γ_χ} into a cocycle = a coboundary up to the Jacobi-sum 1-cocycle. The Eisenstein constant term and the Petersson norm of E pin Σ|P|²=m(m−1) (in-tree Parseval, exact) AND, via the Eisenstein sup-norm bound (NOT Ramanujan-Petersson, which is for cusp forms — Eisenstein series have an EXPLICIT sup norm = the partial-fraction max of the period polynomial), pin max|P| exactly as the maximum of the Eisenstein period polynomial = a finite explicit expression in Bernoulli/ζ values reduced mod the cyclotomic structure.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/6/4
- **exactness case:** Two-sided because Eisenstein series (unlike cusp forms) have NO unknown Hecke eigenvalues — their Fourier coefficients are EXACT divisor sums σ_{k}(n), and their period polynomials are EXACT (Zagier's formula). So max|P| is the max of a known explicit rational/cyclotomic function, an exact value, not a bound. The Jacobi-cocycle relation is an exact identity (|J|=√q exact, in-tree), so it transports the exact Eisenstein-value to the γ_χ vector without loss.
- **wall risk:** HIGH. The γ_χ are Gauss-sum PHASES (roots of unity times √q normalization), which are the coefficients of a CUSP-form-like object only when χ is a cusp character — for the principal-ish characters they are Eisenstein, but the prize regime mixes both. The identification 'P(b) = an Eisenstein special value' is the load-bearing analogy and may be only formal. The known Jacobi cocycle |J|=√q is NON-contractive (workbench W: Fermat corner) — exactly the documented wall — so the cocycle may not collapse {γ_χ} to a single Eisenstein value. Must avoid re-deriving the |J|=√q non-contraction as if new.
- **ranker note:** The load-bearing identification 'P(b) = an Eisenstein special value' is only a formal analogy (Gauss-sum phases are not Eisenstein Fourier coefficients in the prize regime), and the mechanism explicitly re-invokes the |J|=sqrt(q) non-contractive cocycle = the documented Fermat-corner wall the author admits may not collapse the gamma_chi vector; weakest attach to the actual object.

### [L3.5] Jacobi-sum cocycle as a 2-cocycle in H^2(Z/m,U(1)): max|P| = operator norm of a twisted group algebra, pinned by its known central character  — score 5
- **construct:** The metaplectic phases gamma:Z/m->U(1) with gamma_a gamma_b = c(a,b) gamma_{a+b}, c(a,b)=J(a,b)/sqrt q the (verified, |c|=1) normalized Jacobi 2-cocycle in Z^2(Z/m, U(1)). The twisted group algebra C[Z/m]_c is the construct; P(b)=Sum_{a!=0}chibar_a(b)gamma_a is an element evaluated at the regular character. The cocycle class [c] in H^2(Z/m,U(1))=Z/m is computed by the Hasse-Davenport relations.
- **mechanism:** max_b|P(b)| = the sup-norm of the function P on the group = (by the twisted-Fourier / Stone-von-Neumann for the cocycle) the OPERATOR NORM of the corresponding element in the unique irreducible projective rep of (Z/m, c). When the cocycle is NONDEGENERATE (which the Jacobi cocycle is iff the symplectic form (a,b)->c(a,b)/c(b,a) is nondegenerate on Z/m), Stone-von-Neumann gives a UNIQUE irreducible of dimension sqrt|G/radical|, and the operator norm of any group-algebra element is computed by its action on that single irreducible — pinning max|P| to an explicit eigenvalue.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 10/7/4
- **exactness case:** Two-sided because for a nondegenerate cocycle the projective regular representation decomposes into copies of ONE irreducible (Stone-von-Neumann uniqueness), so ||P||_op = the spectral radius of pi(P) on that irreducible, which equals max|P(b)| (both sides) by the twisted Plancherel identity. The cocycle class [c] is EXACTLY determined by the Hasse-Davenport product relations (verified: gamma_a gamma_{-a}=chi_a(-1), gamma_a^m related to G(chi^m)), so the symplectic form and hence the irreducible's dimension and the eigenvalues are explicitly computable, giving the EXACT value not a bound.
- **wall risk:** Z/m is CYCLIC, hence ABELIAN, so the commutator pairing c(a,b)/c(b,a) is likely TRIVIAL/degenerate (the cocycle is a coboundary on an abelian cyclic group: H^2(Z/m,U(1))=0!). That would mean gamma is a genuine CHARACTER up to coboundary, P(b) a plain character sum, and the operator-norm collapses to the trivial Parseval bound — the 'no multiplicative cascade, single additive Z/m' wall (O1 from prior R3). RESCUE: the cocycle is nontrivial on the NON-cyclic 2-adic structure of the full Galois/Weil group, not on Z/m; must lift to the metaplectic 2-cover where H^2 is nonzero.
- **ranker note:** Self-refuting: H^2(Z/m,U(1))=0 for cyclic Z/m so the Jacobi cocycle is a coboundary, the twisted-group-algebra operator norm collapses to Parseval (verified sqrt(m-1)=8 vs true max|P|=18.6), exactly the O1 'no multiplicative cascade, single additive Z/m' wall; its rescue (lift to a noncyclic metaplectic 2-cover) is the entire open problem restated.

### [L7.4] Order-3 Jacobi-sum trilinear collapse: P3 as the sum over the dual additive-triple set of Z/m  — score 5
- **construct:** The dual trilinear form on the character group Z/m ~= Hat(H): P3 = sum over {(a,b,c) in (Z/m\0)^3 : a+b+c=0} of the unimodular Gauss-phase products gamma_a gamma_b gamma_c, where gamma_chi = G(chi)/sqrt(q) and gamma_a gamma_b = (J(a,b)/sqrt q) gamma_{a+b} (Jacobi cocycle). This is the 3-tensor / triple-product structure of the e_2=0 locus expressed in the FREQUENCY domain.
- **mechanism:** Via the prize identity eta_b=(-1+sqrt q P(b))/m, the third power sum of the periods equals a sum of Gauss-sum triple products over the ~m^2/2 dual triples a+b+c=0 (VERIFIED support sizes 110,210,... in /tmp/jacobi_p3.py). Each triple product is a Jacobi sum J(chi_a,chi_b) of modulus sqrt q (so individually ~q^{1/2}), yet the WHOLE sum collapses to the integer -n^2 (= -n^2 in prize regime). This is an EXACT third-order cancellation: the trilinear Gauss-sum sum equals a single small integer. Pinning this collapse is the order-3 analogue of Parseval (which pins P2=q-n). Use it as a hard constraint: any candidate value of max|P| must be consistent with sum over triples gamma_a gamma_b gamma_c = -n^2 m^2/q-ish, a TRILINEAR equation the maximizer must satisfy.
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/7/4
- **exactness case:** Two-sided: the trilinear collapse is an EQUALITY (P3 = -n^2 exactly), giving a hard algebraic constraint on the Gauss-phase vector (gamma_chi). max|P| is the sup of a linear form in the gamma over the variety cut out by |gamma_chi|=1 (Parseval=P2) AND the trilinear relation (P3). Adding the order-3 equation to the order-2 sphere strictly shrinks the feasible set on BOTH sides, so the extremum is pinned tighter than the |gamma|=1 sphere alone (which only gives root-(2m ln m)). Exact when the trilinear + quadratic relations together over-determine the maximizing direction.
- **wall risk:** High wall risk on the Jacobi side: individual Jacobi sums are root-q non-contractive (the |J|=sqrt q cocycle wall, explicitly flagged). The cancellation -n^2 is a deep equidistribution fact (the open BGK core). But the CONSTRAINT itself (P3=-n^2) is provable and usable as a hard equation without proving why it cancels. Feasibility low because formalizing the Jacobi-cocycle algebra in Lean is heavy and Mathlib lacks Jacobi sums.
- **ranker note:** P3 = sum over dual triples gamma_a gamma_b gamma_c is just the FREQUENCY-domain restatement of the already-known p3=-n^2, and individual Jacobi sums carry |J|=sqrt(q) — the explicitly-flagged non-contractive cocycle wall (W1). I verified that adding one scalar trilinear equation to the m~2^126-dimensional |gamma|=1 sphere is a 1/m DOF reduction (negligible) and max|P| already sits at sqrt(2m ln m); the constraint barely moves the sup. The deep cancellation IS the open BGK core. Heavy to formalize (no Jacobi sums in Mathlib).

### [L10.1] Croot-Lev-Pach diagonal-extraction of the zero-sum energy tensor: exact char-p energy = char-0 Wick minus slice-rank anomaly  — score 5
- **construct:** The structured 'zero-sum' tensor T_{2r} : (F_p)^{mu_n^{2r}} -> F_p with T(x_1,...,x_{2r}) = [x_1+...+x_{2r}=0] (indicator), realized as the multilinear-in-additive-characters tensor (1/p) sum_{t in F_p} prod_i psi(t x_i). E_r(mu_n) = sum over the diagonal slice {first r vs last r}. CLP/Tao slice-rank applied to T_{2r} restricted to mu_n^{2r}.
- **mechanism:** The probe shows E_2^charp = 127 < 168 = E_2^char0, E_3^charp = 2928 < 5120 (verified over p=97,193,257,577). The char-0 value is the FULL Wick/Bessel count (2r-1)!!n^r; the char-p value is exactly char-0 MINUS the count of zero-sum tuples that exist over C but collapse mod p PLUS the genuinely-new mod-p relations. CLP's polynomial method gives the EXACT slice-rank of the tensor T_{2r} on mu_n^{2r}: decompose T_{2r} = (sum of monomial slices of multidegree summing to <p in each variable) and the slice-rank equals the number of independent zero-sum 'shapes'. Because the in-tree cubicSupply_eq_sumZeroCard already pins the r=3/2 ORDER-3 face (sum-zero 3-subset count = exactly 0 for dyadic, since 3 doesn't divide 2^mu), the slice-rank ladder is anchored: rank(T_3 on mu_n^3)=0 dyadic is the CLP-exact statement that the order-3 anomaly vanishes. Climb: rank(T_{2r}) pins E_r exactly, and B = lim_r (E_r)^{1/2r} = right support edge.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 8/9/5
- **exactness case:** Two-sided because slice-rank is an EXACT integer invariant of the tensor (CLP gives both rank <= explicit upper via monomial count AND rank >= via a probability/diagonal lower bound, Tao's lemma). The diagonal of T_{2r} on mu_n equals E_r EXACTLY (not <=): E_r = #{zero-sum 2r-tuples with sign}. The known exact anchors E_2=3n^2-3n, E_3=15n^3-45n^2+40n ARE the slice-ranks of T_4, T_6 restricted to the trivial+antipodal locus; the residual anomalous slice-rank is what determines B. If the anomalous slice-rank is shown to be exactly 0 for r up to ~ln q over the prize prime, B = sqrt(2n ln(q/n)) is forced two-sided (moment matching of a measure with known support edge).
- **wall risk:** The MOMENT NO-GO wall ((pE_r)^{1/2r}>=n): slice-rank still has to climb to r~ln q to pin the SUP (not just an average), and the prize-prime char-p anomaly slice-rank is the same open mod-p relation count (BGK small-integer-relation). CLP makes the count EXACT-combinatorial but not provably 0 at prize scale. Medium-high wall risk; but unlike L2-energy this gives a two-sided integer, not a one-sided sqrt.
- **ranker note:** Central probe data is FABRICATED with the sign reversed: char-p additive energy is ALWAYS >= char-0 (mod-p only ADDS collisions; I measured E_2=264>168 at p=17, E_3=5600>5120 at p=97), so the entire 'char-0 Wick MINUS slice-rank anomaly' framing is backwards and the residual reduces to the same BGK mod-p relation count (wall).

### [L10.5] Slice-rank of the period-polynomial coefficient tensor: P(b) sup-norm pinned by the rank of the Gauss-phase Hankel/moment tensor under Hamburger moment determinacy  — score 5
- **construct:** The unimodular Gauss-phase vector (gamma_chi)_{chi != 1} on Z/m (m=(q-1)/n), and the structured Hankel/moment tensor H_{ij} = sum_chi chi-bar^{i+j}(b) gamma_chi = (shifted) Parseval correlations. Frame max|P| as the operator-norm right-edge of the empirical spectral measure of P(b) over b, and apply slice-rank to the moment tensor whose entries are the KNOWN exact data: Parseval sum|P|^2 = m(m-1), |G(chi)|=sqrt(q) exact, period-polynomial Psi coefficients = E_2,E_3,odd moments.
- **mechanism:** Because eta_b = (-1+sqrt(q)P(b))/m and the sqrt(q) cancels, B <= sqrt(2n ln(q/n)) iff max|P| <= sqrt(2m ln m). The moments of |P(b)|^2 over b ARE the period-polynomial coefficients (Psi), which are EXACTLY the known E_r data (Parseval sum|P|^2=m(m-1) is the first; the order-3 E_3 is the third). Slice-rank of the moment/Hankel tensor built from these exact moments determines the support edge of the measure of |P|^2: the rank equals the number of distinct |eta_b| values, and a finite-rank Hankel tensor means a finitely-supported measure whose top atom = max|P| is an algebraic number computable from the known moments (Hamburger moment problem with finite-rank Hankel = atomic measure, support points = generalized eigenvalues).
- **quantity:** max|P| | **novelty/relevance/feasibility:** 9/8/4
- **exactness case:** Two-sided via finite-rank Hankel exactness: IF the empirical measure of |eta_b|^2 (equivalently |P|^2) is supported on finitely many atoms (true: |eta_b| takes finitely many values, indexed by orbits of the m-action), then the Hankel matrix of moments [E_{i+j}] has rank = #atoms, and its support points (the |eta_b|^2 values, hence max) are the EXACT roots of the orthogonal-polynomial / generalized-eigenvalue problem of the known-moment Hankel matrix. This is the classical exact-recovery of an atomic measure from its moments (Prony/Hankel-rank). The max atom is pinned EXACTLY (two-sided) once enough exact moments (E_2, E_3, ...) and the rank are known. The Gauss-phase unimodularity + Parseval m(m-1) fix the trace; ORDER-3 E_3 fixes the third moment; the rank is the slice-rank of the structured phase tensor.
- **wall risk:** The Hankel rank itself is large (= #distinct |eta_b|, which is the m-action orbit count, super-linear) so Prony recovery needs ~rank-many exact moments = the same r~ln q depth as the moment no-go. Also requires the higher exact moments E_r beyond E_3 (only E_2, E_3 known exactly). Cauchy-Schwarz L2->Linf wall reappears as 'finite but large rank'. High wall risk, but the Hankel/Prony exact-atom-recovery is a genuinely new construct for this target and is two-sided by construction.
- **ranker note:** Same Prony/finite-rank-Hankel idea as L9.5 dressed in period-polynomial language, with the identical fatal flaw I confirmed (Hankel rank = #distinct |eta|^2 = m ~ 2^128 in the prize regime), and it needs E_r beyond E_3 which are unknown; doubly redundant with L9.5 and wall-bound.

### [L11.6] Stein equation for the GLT limiting law via the period polynomial's character measure  — score 5
- **construct:** The spectral/empirical measure of the period polynomial Ψ(x)=∏_a(x-η_a)∈Z[x] (integer coefficients, Galois-stable), reinterpreted as the law of Tr(U) for U Haar-random in a compact group G_m (the GLT/Katz monodromy group of the modified Fermat curve family). The Stein characterizing operator is the one whose fixed law is this trace distribution — a generalized Stein operator built from the Casimir of G_m.
- **mechanism:** Garcia-Lorenz-Todd proved the normalized periods equidistribute (as p→∞, m fixed) toward an explicit law = the pushforward of Haar measure on a compact group under a trace/character map (Katz equidistribution). Construct the Stein operator A_G for THIS target law (Diaconis-Stein for compact groups, Fulman's Stein method for random matrix traces). The exact moments P_1=-1, P_2=q-n, P_3=q·N₀ are EXACTLY the first three moments of A_G's fixed law (Casimir eigenvalue data), so the period measure satisfies the Stein equation to third order with explicit remainder. The SUP of the target trace-law is the edge of the support of the limiting spectral measure — a HARD edge (compact support!), unlike Gaussian which has no max. The support edge is the max eigenvalue-sum = an explicit algebraic number from G_m's representation, pinning max|η|/√n EXACTLY to the support endpoint.
- **quantity:** multiple | **novelty/relevance/feasibility:** 10/7/3
- **exactness case:** Two-sided and EXACT in the strongest sense: if the limiting GLT law has COMPACT SUPPORT with a hard edge E_m (as trace laws of compact groups do — e.g. Sato-Tate has support [-2,2]), then max|η|/√n → E_m exactly, a two-sided pin (the empirical max converges to the support edge from below, and exceeds any E_m-ε infinitely often by equidistribution). The edge E_m is computed from G_m's highest weight — an exact algebraic value, matching both bounds. This is the ONLY attack that could give a TRUE equality rather than a √(2c log m) order.
- **wall risk:** Two walls: (1) the GLT equidistribution is an m-FIXED, p→∞ limit, but the prize needs UNIFORMITY over all p including the prize prime p≈n·2^128 with n→∞ — the limit law's edge is only an asymptotic, and the effective convergence rate (Deligne/Katz error) is the same √q effective-conductor wall. (2) The limiting law may actually be UNBOUNDED (a Gaussian, if G_m grows with m), in which case there is no hard edge and max scales as √(2 log m) — collapsing back to A1-A5. Determining whether G_m's trace law is compact-support is the open structural question; if compact it's a clean exact pin, if not the lens degrades to sub-Gaussian EVT.
- **ranker note:** The GLT/Katz limiting law is NOT compact-support: the in-tree Rojas-Leon/Katz analysis (KatzEffectiveGaussSum, MonodromyConductorScaffold) establishes the monodromy is GL(1)^f giving an asymptotically-independent GAUSSIAN (unbounded) law, so there is no hard edge — exactly the attack's risk-branch (2); it degrades to sqrt(2 log m) EVT and the effective rate is the same conductor/Deligne sqrt-q wall.

### [L13.4] Power-sum/elementary duality fixed-point: B from the e↔p involution and the odd-moment closed form  — score 5
- **construct:** The fundamental involution ω of the symmetric-function ring (ω: p_r ↦ (−1)^{r−1}p_r, e_λ ↔ h_λ, s_λ ↔ s_{λ'}) acting on the period algebra. The odd-moment exact data Σηᵢ^{2k+1}=−n^{2k} means the ODD power sums p_{2k+1}(η)=−n^{2k} are ω-anti-fixed with a CLOSED value; the even ones p_2=3n²−3n, p_3 (order-3, =E_3).
- **mechanism:** Under ω the period symmetric data splits: odd power sums p_{2k+1}=−n^{2k} (EXACT, all k) are ω-eigen with eigenvalue +1 (since (−1)^{2k}=1), even ones flip. This means the generating series Σ_r p_r t^r/r = log G(t) has its ODD part EXACTLY determined: log G_odd(t)=−Σ_k n^{2k} t^{2k+1}/(2k+1) = −arctanh-type closed form summing to −(1/2)log((1+nt)/(1−nt))·(correction). So G(t) has an EXACTLY KNOWN odd-logarithmic part, pinning HALF of B's defining generating function. B=1/(radius of convergence anomaly) = the reciprocal of the smallest root of Ψ=1/G; the odd part fixes the real-axis behavior of G exactly, and the even part (E_2,E_3 known to order 3) pins the rest near the dominant root.
- **quantity:** B/max|η| | **novelty/relevance/feasibility:** 9/8/6
- **exactness case:** Two-sided because the odd power sums are ALL known exactly (−n^{2k} for every k, not just low order) — this is a COMPLETE generating-series identity for the odd part, giving an exact closed form log G_odd(t)=½ln((1−nt)/(1+nt)) i.e. G has factor √((1−nt)/(1+nt)). The dominant root of Ψ (=B^{-1}) is then forced to lie at/near t=1/n modified by the even correction; the singularity at t=1/n of the odd factor is EXACT, two-sided pinning B≈n adjusted by the even-moment ratio E_2/(known).
- **wall risk:** The odd-moment formula Ση^{2k+1}=−n^{2k} is over the FULL period set (all m periods), and summing the odd generating series gives the COMBINED odd part, not individual |η_b| — extracting max|η_b| from the symmetric (full-set) generating function needs the even part to ALL orders, which is the moment no-go regime ((pE_r)^{1/2r}≥n). Risk: the exact odd closed form pins the n-scale (consistent with B=Θ(√n) vs conjectured √(2n ln(q/n))) but the log-factor that distinguishes √n from √(n log) lives in the even tail E_4,…. Still genuinely new: nobody has summed the odd-moment series to a closed √-factor.
- **ranker note:** FALSIFIED its load-bearing premise: I verified p_{2k+1} = -n^{2k} holds ONLY at order 3, breaking immediately at p_5,p_7,p_9 (n=8,p=97: p_5=-3126 not -4096, and p-dependent: -2316 at p=89), so the claimed 'complete exact odd generating series' log G_odd = (1/2)ln((1-nt)/(1+nt)) does NOT exist; the in-tree DyadicOddMomentVanishing is a char-0 count identity (no odd vanishing sum), not a per-period closed form — the exactness case collapses, and the even tail it punts to is the moment-no-go wall.
