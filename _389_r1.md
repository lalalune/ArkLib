## FIBRE LAW ROUND 1: SURVIVES — ladder words attain exactly the subset-sum fibre maximum, nothing beats it

`probe_subjohnson_fibre_law.py` (landed), first falsification round at `(n,k) = (16,3)`, tower `s=8, m=2, r=3`, agreement `a = 6`, both `p ∈ {12289, 65537}`:

| word class | L(a=6) | max agreement |
|---|---|---|
| ladder `x^6 + λx^4`, λ at the 5 fibre-maximal sums | **3 = N_fib(8,3)** | 6 |
| monomials `x^5, x^6, x^7` | 0 | 3–4 |
| far-generic ×3 | 0 | **3 = k** |
| hill-climb (incl. ladder starts) | 3 | — |

- `N_fib(8,3) = 3` (max subset-sum fibre of `μ₈` over triples; 40 distinct sums / 56 — the same 40 as the deep census's pinned scalars, as the law predicts: the two objects are dual fibres of one map).
- **The step function again**: generic words have max agreement exactly `k` — no codeword reaches even `k+1` agreement. The entire sub-Johnson list mass at this scale is arithmetic.
- Exhaustive-by-class + hill-climb finds nothing above the fibre value.

Next rounds: `(16,4)` (m=1 towers + mixed), `(32,3)` (two admissible towers — the law's max-over-towers clause gets its first real test), and the char-0 anchor. If it survives `(32,3)`, the supply side formalizes immediately (`badline_pointwise_agreement` + subset-sum fibre = the list family; all bricks in-tree), and the upper bound becomes the named target: *no word beats the fibre packing* — which is the census-domination core yet again, now in its third equivalent form (deep MCA census ≡ alignment dependency ≡ sub-Johnson list fibres). One wall, three faces, all machine-checked at the interfaces.
