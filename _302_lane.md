**Cross-post from #347 (lane claim on shared infra)**: building the **rbr→soundness union-bound probability lift** — item (4) of the remaining-surface list from the session-wrap comment ("the union-bound probability lift (prefix-marginalization of the full run against the per-round games + budget sum) is now the ONLY missing piece of the chain rule").

Design (file in flight: `ArkLib/OracleReduction/Security/RbrToSoundnessLift.lean`):
- **Support-pointwise event transport** through one `simulateQ`'d bind (the reusable "event is decided by the prefix" drop — replaces per-shape trailing-drop lemmas);
- **`continueFromTo` prefix preservation on the support** (generalizes `processRound_support_restrict` to the full continuation, by `Fin.induction`);
- **the state-threading honesty point**: `StateFunction.toFun_full` rejects from a *fresh* `← init` state while the soundness game *threads* the prover's state into the verifier — for arbitrary stateful `impl` the bridge is not provable from the `StateFunction` fields alone. Named hypothesis `RejectStateOblivious`, discharged outright for subsingleton state (`σ = Unit` + `init = pure ()` — the regime of every in-tree consumer, incl. the `RbrToSoundBridge.MarginalBridge` slots and this issue's fences);
- per-round marginal: full-run flip probability at challenge round `i` ≤ the rbr game's flip probability (peel at stage `i.succ` via `runToRound_eq_bind_continueFromTo`, transport the event through the continuation, split the challenge draw, drop `receiveChallenge`);
- public: `rbrSoundness_implies_soundness` (error `∑ᵢ εᵢ`) + the knowledge-form accept bound (fused through `KnowledgeStateFunction.toStateFunction` to avoid the witness-type quantifier mismatch between the two rbr definitions — the naive `rbrKnowledgeSoundness → rbrSoundness` implication is NOT directly available because `rbrSoundness` quantifies provers over ALL witness types while the knowledge game fixes the relation's).

Downstream once landed: #347's shell-residual refutation (∑ε ≥ 1 forced), this issue's obliviousness-fence budget corollaries, the LogUp `MarginalBridge` discharge, and the #340 rbr composition surfaces.
