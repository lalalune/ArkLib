export const meta = {
  name: 'arklib-debt-wave1',
  description: 'Build wave 1: prove S/M provable residuals + repair bit-rot + delete dead surfaces',
  phases: [{ title: 'Wave1', detail: '10 parallel build agents on classified worklists' }, { title: 'Wave2', detail: 'L-effort clusters + open-research conditionality audit' }],
}

const PRE = `You are a Lean 4 prover in /Users/shawwalters/ethereumroadmap/upstream/lean-research/ArkLib (Lean4+Mathlib+VCVio). MISSION: discharge your assigned residual-named Props for real. Rules:

OPS: A concurrent swarm edits this tree. Never branch-switch or reset. Stage ONLY your own files. Commit+push EVERY verified item immediately: git add <files> && git commit -m "feat(audit-wave1): <what>" -m "<body>" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"; push to remote 'fork' (NOT origin): git push fork main; on reject: git pull --rebase --autostash fork main (union-resolve ArkLib.lean by deleting conflict markers keeping both sides; for foreign-file conflicts take --ours i.e. upstream; git rebase --skip stale foreign commits), then push (git push fork HEAD:main if detached). Verify with: lake env lean <file> (authoritative; 600000 timeouts; this can take minutes on heavy cones). If an import's .olean is missing: lake build <Module.Dotted.Name> first. Retry transient 'object file does not exist' errors up to 3x. Probe experiments in /tmp/*.lean with LEAN_PATH via lake env.

HONESTY (absolute): no sorry/admit/native_decide/new axioms/fake instances/vacuous restatements. If a subgoal is genuinely stuck after serious effort, leave it as ONE named hypothesis-taking lemma and say so in your report. End files with #print axioms <decls>; required output: only [propext, Classical.choice, Quot.sound]. Do not delete or weaken existing theorems to make goals fit. Do not trust docstrings — grep for actual theorems.

BUILD STATE (orchestrator, 05:55): the full library build is ~99.5% done (hot cache for you); /tmp/red_modules.txt lists the ~68 modules still lacking oleans — mix of in-flight builds and genuinely red files. If a module in your scope is in that list, `lake build <Module>` it first; if it errors, repairing it is in-scope for you (red-on-main repairs are part of the debt). PrizeMath arity rot is FIXED (8c0f1d42a); Spartan FirstChallengeComplete chain is CONFIRMED GREEN (phantom errors were stale-olean cascade).

WORKLIST SOURCE: read audit/triage-2026-06-10.json (repo root) — an array of 8 reports, each {scope, items:[{name, loc, status, route, papers, effort}], passThroughs, launderingFindings}. Your items' "route" fields contain the concrete proof plans, discharger names, and file:line refs gathered by the triage pass — FOLLOW THEM (verify cited lemmas exist before relying on them). AUDIT_LEDGER.md is the human-readable version. PAPERS_NEEDED.md lists paper ids; if you genuinely need a paper PDF that you cannot find content for via in-tree docstrings/blueprint, APPEND a row to the '## Per-residual additions' section of PAPERS_NEEDED.md (commit it) and work around with the in-tree information meanwhile.

Proof tricks proven in this codebase: rw [show <t> = <defeq> from rfl] pins; OptionT.ext + simp only [OptionT.run_bind, OptionT.run_mk, OptionT.run_pure, Option.elimM, pure_bind]; cases x <;> simp <;> rfl; Fin.elim0 for empty challenge indices; beware 'first | simp [h] | tac' partial-progress trap; erw for instance-heterogeneous rewrites; universe pins OracleSpec.{0,0}/OracleInterface.{0,0} when 'Eq.{1} vs Eq.{u+1}' appears; haveI inferInstanceAs for composed-family instances; pass (init :=)(impl :=) explicitly to keystones; structure-field instances may need 'local instance := X.field' pinning.`

const SCHEMA = {
  type: 'object',
  properties: {
    done: { type: 'array', items: { type: 'string' }, description: 'items fully discharged: name + discharging decl + commit hash' },
    partial: { type: 'array', items: { type: 'string' }, description: 'items advanced but not closed: what landed, what remains, honest blocker' },
    blocked: { type: 'array', items: { type: 'string' }, description: 'items not attempted / hard-blocked, with reason' },
    papersAdded: { type: 'string' },
    notes: { type: 'string', description: 'tricks/gotchas for the next wave' },
  },
  required: ['done', 'partial', 'blocked', 'papersAdded', 'notes'],
}

phase('Wave1')

const jobs = [
  ['binius', `YOUR SCOPE: the 13 Binius BinaryBasefold 'class *Residual : Prop' assumption classes (triage report scope 'Binius'). Priority order: (1) the two S items — PreviousSuffixFiberAlignmentResidual (QueryPhasePrelims.lean:568; the key lemma already EXISTS per the route — find its current name) and FoldEqMultilinearPreTensorStep1Residual (Incremental.lean:1077, Fin-coercion fragility only); (2) the M items with proven twins/bricks: FinalSumcheckStepLogicCompleteResidual (FULL twin proof at FRIBinius/CoreInteractionPhase.lean:1075 — port the skeleton), FoldMatrixDetNeZeroResidual (base case PROVEN at BaseFoldDetBrick.lean:75; do the block-factorization induction), FoldPreTensorCombineAffineSplitResidual (even/odd tensor identities live just above the class), PreTensorCombineJointProximityResidual (key ingredient proven at Soundness/Lift.lean:188), ExtractMLPCorrectnessResidual (BW decoder correctness exists — transport across enumeration). For each: provide an honest 'instance : <Class> := ⟨proof⟩' (or an unconditional theorem + instance). Commit each separately. The L items (Prop4212Case1/2 etc.) — attempt only if time permits after all S/M land.`],
  ['spartan-repair', `YOUR SCOPE: Spartan remaining rbr-KS leaves (the completeness side is DONE — do NOT redo it). STATUS as of 2026-06-10 05:30: FirstChallengeComplete.lean, ComposedCompletenessFinal.lean, ComposedCompletenessWithClaimFinal.lean, ComposedCompletenessTwoLeaves.lean ALL COMPILE CLEAN and the apexes composedCompletenessResidual_proven + composedPIOP_Rc_rbrKnowledgeSoundness_of_leaves + the challenge keystone (AppendRbrKnowledgeSeamZero.lean, hSeamZero discharged) are AXIOM-CLEAN VERIFIED. The earlier 'bit-rot' was phantom (stale oleans). YOUR ACTUAL WORK: (1) the 6 remaining short-phase rbr-KS LEAVES in SumcheckKnowledgeLeaves.lean (honest-lens pullback relations — read that file's TODO structure and the of_leaves consumer signature to see exactly which leaf statements are needed); (2) then the of_leaves INSTANTIATION (feed the 6 leaves into composedPIOP_Rc_rbrKnowledgeSoundness_of_leaves to get the unconditional composed rbr-KS apex); (3) dedup: if both ComposedCompletenessFinal and ComposedCompletenessProven(_114c) compile and duplicate the same apex, keep ONE and delete the other (grep importers first); (4) SpartanBricks D1 pair composedCompletenessWithClaimValueRelResidual/SecondSum (SpartanBricks.lean:1117,1128 — thread the real carried target through prependClaim per the triage route). Commit each item separately.`],
  ['oraclereduction-core', `YOUR SCOPE: OracleReduction core seam debt (triage scopes 'OracleReduction' + structural-audit items). Priority: (1) S: Verifier.liftContext_soundness (LiftContext/Reduction.lean:902) is a PASS-THROUGH while its three harder neighbors (:928, :1425, :1674) are genuinely proven — construct the lifted soundness proof. FULL DESIGN (worked out by the orchestrator — follow it): mirror liftContext_rbr_soundness (:1425, only ~50 lines) NOT the heavy knowledgeSoundness one. Steps: intro WitIn WitOut witIn outerP outerStmtIn hOuterStmtIn; build the inner adversary innerPLens : Context.Lens InnerStmtIn InnerStmtOut OuterStmtIn OuterStmtOut WitIn WitOut WitIn WitOut := { stmt := (fun _ => outerStmtIn) ⇆ (fun _ _ => (default : InnerStmtOut)), wit := Prod.snd ⇆ (fun _ => Prod.snd) }; innerP := outerP.liftContext innerPLens (the constant-projection trick: the inner prover ignores its input and replays outerP on the fixed outerStmtIn); specialize h to (lens.proj outerStmtIn) ∉ innerLangIn via lensSound.proj_sound; refine le_trans ?_ h'. Then the run comparison: Reduction.run (Execution.lean:174) = do pr ← prover.run; so? ← liftM (verifier.run stmt pr.1).run; return ⟨pr, ← so?.getM⟩. Both sides factor through the common core (outer prover + INNER verifier): outer computation = (fun x => (x.1, lens.lift outerStmtIn x.2)) <$> core ((V.liftContext lens).verify o tr = do vI ← V.verify (lens.proj o) tr; return lens.lift o vI — map after bind; (Option.map f so).getM = f <$> so.getM by cases); inner computation = (fun x => ((x.1.1, <innerPLens-lift of ctx>), x.2)) <$> core (via Prover.liftContext_run :695). Establish both as OptionT.ext equalities with the OptionT simp set. Then push the maps through the simulateQ/StateT wrapper (simulateQ_map, StateT.run'/map) and use probEvent_map to turn both sides into probEvent over the SAME wrapped core; close with probEvent_mono: for x in support, lens.lift outerStmtIn x.2 ∈ outerLangOut → x.2 ∈ innerLangOut by contrapositive of lensSound.lift_sound with compat := ⟨x.1.1, x.2 ∈ support (V.run (lens.proj outerStmtIn) x.1.1)⟩; the support extraction through the OptionT/simulateQ stack is EXACTLY the block at :525-:545 of the same file (OptionT.mem_support_run_simulateQ_run'_subset + support_bind decomposition; getM forces so? = some x.2 since failure has empty support). AFTER discharging, update the one consumer OracleReduction/LiftContext/OracleReduction.lean:276 (it threads the residual hypothesis — remove it there too). (2) M: appendRbrSoundnessPhase2Residual (AppendRbrKeystone.lean:282) — the doomed-escape route's bricks are ALL PROVEN in AppendRbrSoundnessPhase2Proof.lean (doom:131, doom_toFun:147, appendDoomed:158, appendDoomed_toFun_le/gt:169/182, verify_notMem_lang_of_full_false:89); assemble them per the route field. (3) Then Verifier.appendRbrSoundnessResidual (Append.lean:1008) discharges via append_rbrSoundness_keystone (AppendRbrKeystone.lean:320) + your phase-2 — write the discharge theorem for the det-V1/msg-seam/Subsingleton-σ regime. (4) M: appendSoundnessResidual challenge-seam case (Append.lean:957) — replay AppendSoundnessMsgProof's canonical chain with evalDist_run'_challengeSeam_left/right (AppendSoundnessSeamTransfer.lean, proven) replacing the msg bridges. Commit each separately.`],
  ['fiatshamir-co25', `YOUR SCOPE: DuplexSponge FiatShamir CO25 residuals (triage scope 'OracleReduction', FiatShamir items). (1) Lemma5_14HonestResidual + Lemma5_16HonestResidual (KeyLemmaFoundations.lean:648,655) — same shape as the PROVEN Lemma 5.12: confine honest fork/time bad events to BadEventDS.E with the dedup'd-trace engine in Lemma512Honest.lean / BadEvents.lean; study the 5.12 proof first and mirror it. (2) Hyb34StepResidual (KeyLemmaHybrids.lean:681) — already reduced in VerifierReplay.lean:~905 to two tvDist legs (Hyb3 vs Hyb3Strict; Hyb3Strict vs Hyb4 with eagerSimulatedProver) — prove the legs. (3) Lemma5_8EagerBirthdayResidual (BirthdayBound.lean:417) — numeric bricks proven; do the 3 steps in the def's own docstring. These follow the CO25 paper (duplex-sponge security); in-tree docstrings carry the statements — work from them.`],
  ['fri-completeness', `YOUR SCOPE: FRI completeness chain (triage scopes 'Whir/Stir/Fri' + 'ToMathlib' FRI items). (1) ToMathlib/FriComplete.lean:106,131 — foldRoundPerfectCompletenessResidual + finalFoldRoundPerfectCompletenessResidual: mirror the PROVEN Binius sibling foldOracleReduction_perfectCompleteness (~150-line 2-message monadic unrolling — find it, port the skeleton); algebraic core = RoundConsistency lemmas (grep ToMathlib for foldα/round-consistency bricks). (2) Fri/Spec/Completeness.lean:29 queryRoundPerfectCompletenessResidual — honest FRI query-round completeness; the run-unrolling infra exists (unroll_n_message keystone used by Stir/Whir — grep for it). (3) reductionPerfectCompletenessResidual (:84): NOTE the current plumbing LAUNDERS — reduction_perfectCompleteness_of_phases (ToMathlib/FriCompleteCompose.lean:72) is literally 'exact hResidual' DISCARDING hFold/hQuery: replace it with a real composition via the proven append/seqCompose perfect-completeness keystones (SeqComposePerfectCompletenessThreaded.lean / AppendPerfectCompletenessProof.lean), consuming the per-round facts from (1)+(2). Commit each separately.`],
  ['data-tomathlib', `YOUR SCOPE: Data/CodingTheory + ToMathlib S/M items (triage scopes 'Data/CodingTheory' + 'ToMathlib'). NOTE: item (1) of the original plan (the #141 vacuity discharge) is reassigned to the prizemath-red-repair agent — skip it. (2) M: MonicHighYResidual (ToMathlib/GSGradedBundle.lean:283) — witness-bundle discharger exists (FaithfulFrontierWitness.lean:121) + hmonic at unit-leadingCoeff endpoints (BranchCollapse.lean:133); close the remaining general-monic half per the route. (3) M: diffStackMCAResidualBelowUDR (ProximityGap/Errors.lean:1597) — RS form discharges via GSWitnessLowerBound (ToMathlib/L46GSLowerBound.lean) + machinery in ToMathlib/L46DiffStackRS.lean; assemble the RS discharge (the abstract-code form is documented FALSE — do not chase it). (4) If time: BoundaryCardLatticeThresholdResidual via LatticeCoeffPolyExtraction (L — attempt only the next reduction layer honestly).`],
  ['logup-outer', `YOUR SCOPE: the Logup malicious-prover outer-soundness leg (triage scopes 'Logup' + structural-audit Logup items). The open leg: OuterSoundnessResidual (SubPhaseSplit.lean:70) reduced to run-unfolding via outerSoundnessResidual_real_of_runUnfolding (OuterSoundnessReal.lean:466) and the marginal bridge (OuterRunSamplesChallenge.lean:185). Project memory: the malicious-prover outer TEMPLATE exists; marginalBridge_holds + OuterRunSamplesChallenge_holds are wired on main; the honest WIP for the marginal bridge trailing-drop was /tmp/MarginalBridgeProof.WIP.lean (384 lines, may be gone — check). Assemble the outer-soundness discharge, then the apexes LogupSoundnessFullResidual / LogupSoundnessUncondResidual (LogupSoundnessClose.lean:170 + LogupSoundnessUncond.lean). Also: the 6 'provable' Logup items in the triage report — read their routes and close what you can. WARNING: this cone had deletion-laundering history — verify every cited lemma exists (grep 'theorem <name>') before building on it.`],
  ['whir-stir', `YOUR SCOPE: the 10 Whir 'provable' items + Stir items (triage scope 'Whir/Stir/Fri' — read its full item list in audit/triage-2026-06-10.json and attack every provable item per its route field, S before M before L). Context: whir_rbr_soundness was historically statement-only (#113); recent landings (ThresholdKSF scaffolding, pair-generator MCA on unique-decoding window, Johnson MCA small-field) may make several routes short — verify what exists NOW first. For Stir: stirCheckingCABridge was previously fake-instantiated by a deleted false axiom — if its route marks it provable, prove honestly; if it needs the open Johnson-CA gate, leave conditional and say so. Commit each item separately.`],
  ['dead-cleanup', `YOUR SCOPE: the 20 'dead' items across all triage reports (filter audit/triage-2026-06-10.json for status=='dead'). For each: (1) re-verify deadness — grep that NOTHING consumes the decl (importers, applications); (2) if truly unconsumed exploration/duplicate: delete the decl (or whole file if the file is only that); (3) if it is a refuted-as-stated surface that adapters still reference (e.g. BoundaryCardResidual family, uniformEpsMCAgsPrizeBoundConjecture): do NOT delete — instead ensure the docstring says REFUTED/FALSE with the refuting theorem named (the triage route names it), and fix any consumer docstring that presents it as live (e.g. epsMCAgs_prizeBound_conjecture_of_uniform at MCAGS.lean:582 consumes a refuted Prop — its docstring must say so). (4) For deletions: lake env lean any file you touched + the files that imported the deleted decl's module; update ArkLib.lean ONLY by removing deleted-module imports (it is generated — keep edits minimal). Commit in small batches with clear messages. Be conservative: when in doubt, document instead of delete.`],
  ['prizemath-red-repair', `YOUR SCOPE (UPDATED 05:50 — the red-on-main repair is DONE by the orchestrator, commit 8c0f1d42a: PrizeMath repaired to inline-existential conclusions, 8-arg theorem renamed epsMCAgs_prizeBound_perInput_holds, vacuity discharge landed axiom-clean; PrizeMath/LowOutput/UniformVacuity all verified green). YOUR ACTUAL WORK — the quarantine + refutation-keep importer sweep and the remaining GrandChallenges items: (1) lake build ArkLib.MCAGSRefutationCore_keep and each ArkLib/Data/CodingTheory/Quarantine/Candidate*.lean module that imports GrandChallenge141PrizeMath (grep importers); repair any references to the renamed epsMCAgs_prizeBound_conjecture_holds (now epsMCAgs_prizeBound_perInput_holds for the 8-arg form; the 2-arg name belongs to GrandChallenge141UniformResolved) or to the three restated conclusions. (2) Triage-listed GrandChallenges provable items: CS25BreakdownBelowConjectureBound (MCAConjectureRefutation.lean:47 — the in-tree reduction cs25BreakdownBelowBound_of_breakdownFamily is proven; the external input is rs_epsCA_breakdown_cs25 (CapacityBounds.lean:666, honest external debt) — do NOT fabricate it; instead check ProximityGap/ExternalDebt.lean conventions and make sure the chain is wired so the single named external Prop is the only gap, and document precisely what CS25 statement is needed in PAPERS_NEEDED.md). (3) diffStackMCAResidualBelowUDR RS-form discharge IF the data-tomathlib agent hasn't claimed it (coordinate via git log). (4) BoundaryCardLatticeThresholdResidual next reduction layer (L item — honest partial progress only). Commit each item separately.`],
]

const results = await parallel(jobs.map(([label, job]) => () =>
  agent(PRE + '\n\nYOUR ASSIGNMENT:\n' + job + '\n\nWork the list, commit+push each verified item, return the structured report.',
    { label: 'wave1:' + label, phase: 'Wave1', schema: SCHEMA })
))

phase('Wave2')

const wave1Summary = results.map((r, i) => {
  if (!r) return jobs[i][0] + ': DIED (no report)'
  return jobs[i][0] + ': done=' + JSON.stringify(r.done) + ' partial=' + JSON.stringify(r.partial) + ' notes=' + (r.notes || '').slice(0, 400)
}).join('\n')

const wave2Jobs = [
  ['L-bciks20-cone', `YOUR SCOPE (wave 2, L-effort): the Data/CodingTheory 'provable' L items — read audit/triage-2026-06-10.json report scope 'Data/CodingTheory' and attack every status=='provable' item not closed by wave 1, prioritizing those whose route says the bricks are already proven (e.g. BoundaryCardLatticeThresholdResidual via LatticeCoeffPolyExtraction; diffStackMCAResidualBelowUDR RS-form via GSWitnessLowerBound + L46DiffStackRS machinery; CS25BreakdownBelowConjectureBound's in-tree reduction layers). Make each remaining open core ONE named Prop with everything else proven around it.`],
  ['L-fiatshamir-deep', `YOUR SCOPE (wave 2, L-effort): remaining DuplexSponge CO25 items from the triage 'OracleReduction' report not closed by wave 1 (deep legs of Lemma 5.8/5.14/5.16, Hyb hybrids, any remaining KeyLemma residuals). Build on whatever wave 1's fiatshamir-co25 agent landed (check git log --grep=audit-wave1).`],
  ['L-binius-prop4212', `YOUR SCOPE (wave 2, L-effort): the Binius L items — Prop4212Case1Residual / Prop4212Case2Residual and any of the 13 residual classes wave 1 left open (check git log + grep for new instances). Routes in the triage 'Binius' report; DP24 2024/504 sections cited per item.`],
  ['L-append-seams', `YOUR SCOPE (wave 2, L-effort): remaining OracleReduction seam L items — appendSoundnessResidual challenge-seam general case and any append/seqCompose pass-throughs that wave 1's keystone landings now make dischargeable wholesale (re-grep the pass-through list in the triage 'interfaces-passthroughs' report; each newly-provable pass-through should become a real theorem or be deleted in favor of the proven form).`],
  ['open-research-audit', `YOUR SCOPE (wave 2): the ~20 status=='open-research' items across all triage reports. For EACH: (1) verify every in-tree consumer takes it as an explicit hypothesis/conditional (grep consumers; no consumer may present it as established); (2) verify the def's docstring says OPEN with the precise open core and paper refs (fix if stale — comment-only edits are safe); (3) confirm no *_holds theorem secretly discharges a vacuous reformulation (the #141 epsMCAgsPrizeUniformConjecture pattern — if you find another vacuously-true-as-stated 'open' Prop, prove it and re-point the docs at the genuinely open form, exactly as GrandChallenge141UniformVacuity.lean did); (4) append any missing paper to PAPERS_NEEDED.md. Commit per file. This is the honest end-state pass: open math stays open, but nothing pretends.`],
]

const results2 = await parallel(wave2Jobs.map(([label, job]) => () =>
  agent(PRE + '\n\nWAVE 1 OUTCOME SUMMARY (build on it, do not redo):\n' + wave1Summary + '\n\nYOUR ASSIGNMENT:\n' + job + '\n\nWork the list, commit+push each verified item, return the structured report.',
    { label: 'wave2:' + label, phase: 'Wave2', schema: SCHEMA })
))

return {
  wave1: results.map((r, i) => ({ job: jobs[i][0], report: r })),
  wave2: results2.map((r, i) => ({ job: wave2Jobs[i][0], report: r })),
}